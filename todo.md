```text
  你现在要帮我为当前项目搭建一套“本地多 AI agent 并行开发”工作流，重点是：

  1. 使用 `git worktree`
  2. 可选使用 `tmux`
  3. 适配“每个任务都可能需要先讨论和确认需求”的场景
  4. 方案要尽量轻量、实用，不要过度设计

  项目信息：
  - 项目路径：`/Users/zhangjun/私藏/projects/探索/浏览器插件/浏览器ai助手/
  CodePilot`
  - 这是一个 Next.js + Electron 项目
  - 之前已经踩过“父目录也有 package.json / node_modules，导致依赖解析串层”的坑
  - 所以你需要优先考虑：是否建议我把仓库迁到一个更干净的父目录下
  - 我不要求必须用 tmux，多个终端窗口/标签页也可以
  - 但如果 tmux 有明显价值，你可以把它作为增强方案提供
  - 我的核心诉求是：并行多个任务，但每个任务都可能先要讨论、澄清、确认需求，不能一
  上来就盲写代码

  你要给我的输出，不是泛泛建议，而是可直接落地的方案，包含这些部分：

  A. 目录与仓库建议
  - 是否应该迁移到干净目录
  - 推荐目录结构
  - 为什么这样可以避免之前那种依赖解析问题

  B. worktree 方案
  - 如何给每个任务创建独立 worktree
  - 分支命名规范
  - 哪些文件/类型的改动不能并行
  - 如何避免多个 agent 互相覆盖

  C. 会话/终端组织方式
  - 不使用 tmux 时，如何组织多个终端窗口和多个 AI 会话
  - 使用 tmux 时，如何组织 session / window / pane
  - 推荐的最小配置：例如“1 个主会话 + 2 个开发 agent + 1 个 review agent”

  D. 任务流转机制
  - 因为每个任务可能先讨论需求，所以你要设计“需求澄清 -> 任务冻结 -> 开发 ->
  review”的流程
  - 需要给我一个任务卡模板，最少包含：
    - 任务名
    - 目标
    - 允许修改目录
    - 禁止修改文件
    - 验收标准
    - 未决问题
  - 说明什么情况下任务可以交给 agent 开工，什么情况下必须先继续讨论

  E. 实操脚本/模板
  请尽量直接产出可用内容，例如：
  - 创建 worktree 的脚本
  - 初始化多个工作目录的脚本
  - 可选的 tmux 启动脚本
  - 一个 `tasks/` 目录的任务卡模板
  - 一个简短的协作规则文档模板

  F. 约束
  - 不要使用 destructive git 命令
  - 不要假设多个 agent 可以共用同一个工作区
  - 不要建议多个 agent 同时修改 `package-lock.json`、数据库 migration、全局配置
  - 方案要适合本地开发，不依赖复杂的云端平台

  执行方式：
  - 先检查当前仓库结构和父目录情况
  - 然后给出最小可落地方案
  - 如果合适，可以直接在仓库里创建：
    - `docs/multi-agent-workflow.md`
    - `tasks/_template.md`
    - `scripts/setup-worktrees.sh`
    - `scripts/start-agents-tmux.sh`（如果你认为 tmux 方案值得保留）
  - 但先以“最小可用”为目标，不要一次性铺太大

  补充偏好：
  - 我更在意可执行性，不在意花哨
  - 我希望未来能很自然地把任务分给不同 AI 会话
  - 主会话负责需求确认，子会话负责执行，review 会话负责验收

  如果你想更强一点，也可以把这段一起附上：

  额外要求：
  - 两套都要明确优缺点
  - 最后给一个你推荐的默认方案

  如果你只想给另一个 AI 更短的版本，可以用这个：

  请为当前项目设计一套可落地的本地多 AI agent 并行开发工作流。

  要求：
  3. 适合“任务需要先讨论需求，再冻结后执行”的模式
  4. 先给无 tmux 方案，再给 tmux 增强方案
  5. 输出要包含：
     - 干净目录建议
     - worktree 目录和分支规范
     - 多终端/多会话组织方式
     - 任务卡模板
     - 协作规则
     - 可直接使用的脚本模板
```
