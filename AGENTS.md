# AGENTS.md

Repository-level collaboration rules for coding agents working in this project.

## Communication Rules

- Do not say a design document is "completed" unless its core promised capabilities are actually implemented.
- Always distinguish these four states when reporting progress:
  - `文档完整度`: `未开始` / `部分完成` / `基本完成` / `完整完成`
  - `主链状态`: `未打通` / `已打通`
  - `UI 可验收范围`: what the user can actually verify from the product UI today
  - `剩余缺口`: the major unimplemented capability gaps
- If the main path works but the document is not fully implemented, say `部分完成` or `基本完成`, never just say `完成`.
- If the user is a UI-only tester, communicate in terms of pages, buttons, visible states, results, and user actions. Do not default to internal code names, test names, workflow ids, file paths, or implementation jargon unless the user explicitly asks for them.
- When the user asks "是否完成", answer with the strict standard by default, not the POC standard.
- If there is uncertainty, verify first or explicitly say what is confirmed versus inferred.

## Anti-Confusion Checklist

Before replying with any status summary, check:

1. Am I confusing `主链打通` with `文档完整实现`?
2. Am I confusing `内部已验证` with `用户可通过 UI 验收`?
3. Am I using implementation identifiers where the user asked for product-facing language?
4. If I say `完成`, would a strict reviewer reasonably agree?

If any answer is "no", revise the response before sending it.

## Default Status Template

When the user asks for the status of a design doc or module, prefer this shape:

- `文档完整度`:
- `主链状态`:
- `UI 可验收范围`:
- `剩余缺口`:

## Current Standing Rule From User

- This user prefers strict completion language.
- This user can only perform UI testing unless they say otherwise.
- Avoid mixing architecture progress, implementation progress, and UI acceptance progress in one vague "done" statement.

## Goal Tracking Rules

- `AGENTS.md` is the source of truth for project-level goal tracking in this repo.
- Keep these three sections up to date whenever scope, milestones, or completion status materially change:
  - `大目标`
  - `阶段性目标和成果`
  - `当前状态进度`
- Do not wait until the end of a large phase. If the agent changes the practical definition of "next", lands a milestone, or finds a blocking gap, update `AGENTS.md` in the same workstream.
- Status updates in chat should stay consistent with the latest `AGENTS.md`.
- Use strict status language in `当前状态进度`; do not mark a goal complete if only the main path or POC path is working.

## Task Lifecycle Safety Rules

These rules exist because a prior bug let Workflow tasks disappear from the UI while the underlying OpenWorkflow run kept executing. Do not repeat that mistake.

- Product verbs must be implemented by product semantics, not by table names:
  - `停止 / 取消执行`: must interrupt the active runtime, stop agent/subagent work where possible, and write terminal state back to every visible execution record.
  - `关闭 / 暂停任务`: must stop future triggers and also cancel currently running executions, unless the UI explicitly says "仅停止后续触发".
  - `删除任务`: must first cancel all running executions for that task, verify they are terminal or no longer cancellable, and only then remove the visible task/schedule records.
- For Workflow tasks, lifecycle code must account for all relevant state layers:
  - `scheduled_workflows`: task definition and future trigger state.
  - `schedule_run_history` and `schedule_run_steps`: user-visible execution records.
  - `workflow_task_mapping` and `workflow_executions`: Lumos projection from task/session to workflow run.
  - `~/.lumos/workflows.db` / OpenWorkflow `workflow_runs` and `step_attempts`: actual engine state.
  - in-memory runtime controllers such as Scheduling `activeExecutions` and workflow subagent abort controllers.
- Do not implement lifecycle behavior ad hoc inside UI components or thin route handlers. Route handlers should call a lifecycle/control service, e.g. `src/lib/workflow/schedule-run-control.ts` for scheduled Workflow runs, or create an equivalent service before adding new lifecycle actions.
- If an execution history row is missing but a workflow projection or OpenWorkflow run is still `running`, code must include a conservative cleanup path that can still find and cancel the orphaned execution.
- Lifecycle acceptance must check more than the UI:
  - User action is visible in UI as `已取消` / `失败` / terminal state.
  - There are no `schedule_run_history.status = 'running'` rows for the target task.
  - Matching `workflow_executions` rows are terminal.
  - Matching OpenWorkflow `workflow_runs` are not `pending / running / sleeping`.
  - One-time tasks do not auto-trigger again after cancellation.
- Never report "已删除", "已关闭", or "已停止" for a task unless the runtime side is confirmed or the remaining uncertainty is explicitly stated.

## Electron Startup And Cache Safety Rules

These rules exist because a prior version-upgrade path tried to delete Chromium Service Worker storage while another Electron process still held the LevelDB lock, causing repeated startup errors like `Failed to delete the database: Database IO error`.

- Electron must have a single-instance guard before startup work that touches `app.getPath('userData')`, browser sessions, browser bridge runtime files, or SQLite databases.
- Do not clear `serviceworkers`, `cachestorage`, cookies, local storage, or other browser/login storage during normal startup or version upgrade unless the user explicitly chooses a destructive reset action.
- Version-upgrade maintenance may clear HTTP cache with `session.defaultSession.clearCache()`, but it must not block app startup if cache cleanup fails.
- Always write the current app version after a best-effort upgrade-maintenance attempt; otherwise the app can get stuck retrying the same failed cleanup on every launch.
- Treat `~/.lumos` as shared runtime state. Before adding startup cleanup, ask what other process/session might currently own the file or database being touched.
- When startup logs mention Chromium storage/database deletion, verify whether an old Electron process is still running and holding `Service Worker/Database/LOCK` before changing unrelated code.

## 大目标

- 按“完整实现”标准落地 `03 / 04 / 05 / 06` 任务架构文档，而不只是打通最小闭环。
- 让用户能基于产品界面和明确的阶段结论判断哪些能力已经可验收，哪些还只是内部链路可用。
- 在不破坏 `01 ~ 06` 主链边界的前提下，为后续系统能力增长补齐 `07 动态能力扩展` 的独立架构定义。
- `07 动态能力扩展 / 能力生成器` 的产品目标按“小白用户可完成能力开发、安装、更新、使用”收口：优先让用户用自然语言表达需求，由 Lumos 自主生成可安装方案、处理同名更新、路径迁移、基础自检和清晰失败提示；需要凭据、危险权限或外部账号授权时再打扰用户。
- 主 Agent 需要逐步收敛为 Lumos 的“管家”入口：优先具备全局只读状态查看、历史搜索、任意会话摘要、明显问题诊断和小白可理解的下一步引导；删除、覆盖、支付、发 IM、敏感导出、批量操作、审批 / 回滚 / 治理等高风险能力必须后置，并在专门权限与确认链路完成前不进入自动执行范围。
- 以“独立模块优先、聊天与工作流复用其服务”的原则补齐 `08 DeepSearch`，利用内置浏览器共享登录态为反爬站点提供可验收的深度研究能力。
- 建立本地浏览器 Provider / Browser Context 架构，让 Lumos 后续可在不复制账号资产的前提下接管用户已有的指纹浏览器 profile，并保证 chat、Workflow、Agent 浏览器工具不会串号。
- 应用平台当前先从“AI 自由生成应用”收敛到“内置应用 + 稳定运行时 + 可验证安装/打开/配置”主路径；在内置应用可验收前，不把 AI 创建器的真实 Claude SDK 生成链路、工具循环和迭代发布能力标为完整完成。
- 微信助手当前新增 UI 验收目标：收口 `AI 推荐` 来源可读性、每日微信总结报告弹窗可滚动和可操作、报告删除能力，以及应用内 AI 对话框统一规范；其中 AI 推荐必须尽量展示微信里可见的群名 / 联系人名 / 发言人和消息上下文，不直接暴露 wxid、openim id 或群 ID；自动化页局部 AI 对话框需要移除，后续统一复用工作流、资料库同款底部全局 AI 对话框。
- 微信助手 `近期话题` 新目标：从当前手动白名单即时提取，升级为按“凌晨 4 点为一天分界”的每日自动分析和按日入库；查看时支持选择日期范围，按消息来源 / 会话分组展示话题，并能从话题进入相关原始消息上下文或跳到对应对话附近，最终以 UI 可验收为准。
- 支付模块先收敛到“余额充值”主链：用户充值后增加 Lumos 余额 / new-api token 额度，不做月卡、订阅或会员套餐主线。
- `lumos-web` 生产后台已切到 HTTPS：`http://lumos.miki.zj.cn` 会 301 到 `https://lumos.miki.zj.cn`，证书使用 Let's Encrypt + 现有 Nginx 自动续期方案。
- 知识库本地索引必须按跨平台标准收口：macOS 本地开发可索引不等于 Windows exe 可索引；打包版必须在 macOS / Windows 安装包里都能完成“导入内容 -> 向量化 -> 搜索命中”验收。
- 桌面端更新包体积需要逐步从“每次下载完整安装包”收敛到“主程序包 + 外置稳定运行资源缓存”；Node runtime / Python runtime / Git Bash / 本地模型这类稳定大资源应优先支持按资源清单独立校验、下载和复用，完整安装包只保留为兜底路径。
- 保持架构边界不扩张：
  - DSL v1 不支持 subworkflow
  - Phase 1 只支持 `agent / browser / notification`
  - 不执行任意 LLM 生成的 TypeScript
  - Scheduling Layer 产出 DSL
  - Workflow MCP 校验并编译
  - Workflow Engine 只执行编译产物
  - `08 DeepSearch` 先作为独立模块 / service，不直接耦合进 Workflow 主链

## 阶段性目标和成果

- 阶段 1：最小闭环
  - 成果：`generate_workflow -> compile -> engine load -> register -> run` 已打通。
  - 成果：单步 `agent`、真实 `browser + notification`、取消主链都已具备可运行实现和自动化验证。
- 阶段 2：严格验收收口
  - 成果：已建立按“文档完整度 / 主链状态 / UI 可验收范围 / 剩余缺口”汇报的规则。
  - 成果：真实 browser smoke、取消 smoke、loader 回归测试已稳定。
  - 成果：调度层已从“固定单步模板”升级为“规划器先决策，再分流到 simple / workflow 执行”。
  - 成果：`StageWorker -> workflow subagent runtime -> Workflow Engine / simple execution` 的真实取消信号已打通，不再只是本地状态取消。
  - 成果：任务记录与测试页已能展示真实调度决策，包括执行方式、预计耗时、判断依据与计划步骤。
  - 成果：调度层已补齐模型分析的超时、重试、回退诊断；测试页可直接创建和取消调度测试任务。
  - 成果：主 Agent 对话下发的任务已补实现类、网页搜索类、搜索后汇总报告类与导出诉求识别，不再一律退回通用两步流。
  - 成果：simple execution 与 workflow agent step 已补运行时超时透传，减少“任务已创建但长期挂起不收口”的问题。
  - 成果：workflow 浏览器运行时已补四处关键收口：`ctx.browser.waitFor()` 现在会把请求体里的 `timeoutMs` 同步透传到 browser bridge HTTP 传输超时，不再被客户端固定 30 秒误杀；同时当脚本把 `waitFor` 写成 `10000 / 15000ms` 这类高脆弱值时，运行时也会自动抬到更安全的下限，减少登录页和慢页面反复秒超时；`/v1/pages/navigate`、新建页、快照、截图等桥接请求也已补接口级更长默认传输超时，不再一律沿用 30 秒；另外同一段 `ctx.browser` 执行里由 `navigate/selectPage/newPage/currentPage` 获得的 `pageId` 也会持续传递到后续 `snapshot/click/fill/waitFor/evaluate/screenshot`，减少同站点多标签页场景下误落到错误页面；同时 `chrome-devtools` MCP 现在在多标签页场景下会强制要求显式 `pageId`，并在 `list_pages` 返回里增加活动页与相似页签警告，降低纯 Agent 浏览器路径误选 tab 的概率。
  - 成果：workflow `code-only` 浏览器步骤现已补上失败留痕；当脚本返回 `success:false` 或直接抛错时，运行时会自动保存当前页面快照、失败截图与代码调试日志到步骤输出目录，并把这些路径挂回结果元数据；正式 `Workflow` 详情页也已补上“打开失败截图 / 页面快照 / 调试日志”入口，减少“节点失败但现场已经丢失”的黑盒排障成本。
  - 成果：workflow `code-only` 浏览器下载步骤现已补上下载产物收口；当步骤在执行过程中触发浏览器真实下载时，运行时会把本次新下载文件自动复制到当前步骤 `output` 目录，使正式“执行记录 > 结果文件”tab 能稳定收录，而不再只依赖脚本摘要里口头回传的绝对路径。
  - 成果：Scheduling planner 已切到与主 Agent 一致的 Claude SDK 规划路径；同时 workflow / simple execution 也已补上任务会话的 `provider / model / workingDirectory` 透传，减少“主对话能用但工作流规划或执行走到别的 provider / 目录”的断链问题；最新还已把 planner 默认超时从 30 秒放宽到 90 秒，并把 Claude SDK 超时误报的 `aborted by user` 文案归一为明确的规划超时诊断；另外当 Claude SDK 没有填充 `structured_output`、但返回了纯 JSON 文本或单个 JSON code fence 时，规划器也已改为先做严格 JSON 解析再进入 schema 校验，减少这类网关兼容场景下的误失败；同时规划响应 schema 现已容忍 `detectedUrl: null` / `detectedUrls: []` 这类空位输出，并在入库前归一化为“未提供”，避免研究类任务因可选 URL 字段的空值格式直接失败；此外，planner 输出的 `workflowDsl` 现已改成按 step type 的严格结构化 schema，browser step 只允许当前引擎真实支持的 `navigate/click/fill/screenshot` 输入字段，且 DSL 校验失败原因会显式回灌到下一次 LLM 重试，减少模型连续三次复用同一类非法节点形状；现在还进一步补上了 planner 语义校验：长篇 plain-text 报告综合步骤的超时下限、`researcher` 只读角色不得被要求落文件、以及 `md-converter` 这类导出能力优先消费上游 `output.summary` 而不是假设 temp 文件存在；另外 Claude SDK 与 AI SDK 两条 LLM 调用链现在也已统一走 provider-aware 的模型解析，不再让聊天、planner、workflow agent 各自散落一套模型别名/目录匹配逻辑，且正式调度诊断已开始同时记录请求模型与实际解析模型。
  - 成果：workflow 编译/执行链路已补默认 step timeout 与外层收口缓冲；当 DSL 未显式声明 `policy.timeoutMs` 时，编译产物现在会为 `agent / notification / capability / wait` 生成默认超时，并把这些 timeout 写进 manifest；workflow outer timeout 也已改为按 step timeout 求和后再加额外缓冲，减少“步骤产物已经写完，但整体任务卡在外层超时边界被判失败”的问题。
  - 成果：workflow 运行时已补齐 `notification / capability / wait` 的真实 runtime binding，编译产物也会显式解构这些 handler，不再出现“DSL 校验允许节点类型，但执行期缺少运行时绑定”的断链。
  - 成果：workflow `code-only` 浏览器执行链路现已默认走后台模式；任务运行时的 `ctx.browser` 会把 `background: true` 透传到 browser bridge，减少代码节点执行时反复切前台浏览器标签页对用户操作的打扰，同时保留手动调试入口的前台行为。
  - 待完成：补齐使 `03 / 04 / 05 / 06` 能按“完整实现”标准过验收的缺口。
- 阶段 3：UI 验收对齐
  - 成果：已确认用户当前只能做 UI 测试，后续汇报必须产品化表达。
  - 成果：已确认 `06 执行代理层` 不能只停留在运行时实现，必须补正式 UI 才能按“完整实现”标准验收。
  - 成果：`06` 已新增正式工作流角色配置 UI，并已接到真实调度/执行配置源，不再只是测试页或硬编码。
  - 成果：已新增正式 `Workflow` 页面入口，把任务创建、调度判断、执行计划和角色快照收进产品界面，不再只依赖 `/task-management-test`。
  - 成果：`Workflow` 正式页已补上真实最终输出展示，并能区分“原始规划步骤”和“实际执行步骤”；当任务回退为 simple execution 时，不再把旧工作流步骤错误展示为当前执行状态。
  - 成果：正式 `Workflow` 编辑页已补一轮保存收口；现在删除最后一个节点后仍可按“空白草稿”正常保存，且删除节点时会同步清理残留的依赖/控制流引用，减少“界面上节点已删、点击保存后仍一直显示未保存”的假失败。
  - 成果：正式 `Workflow` 编辑主路径已开始收敛到原生 `v3 nodes/edges`；当前详情编辑页与新建构建器页的页级真源、JSON 文本与保存载荷都已统一使用正式 `v3` DSL，旧 `steps / dependsOn` 视图模型仅保留在图形/可视化子组件边界，通过适配器进出，不再继续作为页面级保存真源。
  - 成果：正式 `Workflow` 列表页已补上批量导入 / 导出第一版；用户现在可以多选工作流后导出，也可以导出全部工作流；导入接口同时兼容旧单工作流包 `lumos-workflow/v1` 和新批量包 `lumos-workflow-bundle/v1`，页面导入也支持一次选择多个旧单包文件并合并成批量导入请求；批量导入时会复用同一批 preset ID 映射，减少同一批包内共享代理预设被重复创建。
  - 成果：已修正调度层对中文否定语义的一个显性误判，`不需要通知` 不再被当成通知需求。
  - 成果：已补 OpenWorkflow / backend-sqlite 的 Next 服务端外部包配置，收口当前开发环境下的一个工作流引擎兼容风险。
  - 成果：已收紧 workflow agent step 的输出合同，默认只交付结构化文本结果，不再允许虚报未落盘的 artifact 文件，减少多步代理工作流的伪失败。
  - 成果：已为文本型 stage 补上结构化输出失败兜底；当 Claude Code 连续无法收敛到 JSON schema 时，运行时会退到纯文本交付模式并安全包装为 stage 结果，避免主链卡死在格式层。
  - 成果：已修正 Workflow DSL 编译产物中的 step output 引用语义；`steps.someStep.output.summary` 现在会正确读取上一步 `output.summary`，不再把下游步骤喂成空输入。
  - 成果：正式 `Workflow AI 助手 / Builder` 已补上“稳定工作流”收口；当前生成与修改 DSL 时会强制追加稳定性规则、对 `shared/*_output.md` 猜路径 / `context` 直挂 `steps.x.output.summary` / 代码节点绝对路径 / 非结构化字段下游直读等高风险模式做正式校验，并在返回 DSL 前自动带着校验问题自修一轮，减少 AI 写出“能解析但运行脆弱”的工作流。
  - 成果：主 Agent 对话里的任务完成通知已改为直接写入真实执行结果；浏览器截图/文件路径不再经过模型改写，聊天区可直接识别并预览。
  - 成果：正式 `Workflow` 页面已新增只读工作流流程图，能按层展示步骤依赖、并行分支与当前状态，复杂流程不再只能靠文字列表理解。
  - 成果：已修正复杂并行浏览器工作流的两处主链问题：中文标点分隔的多 URL 现在能被稳定识别为独立分支；并行浏览器分支也已补上独立 pageId 绑定，不再共用活动页。
  - 成果：已新增“前置分析 → 并行浏览器分支 → 汇总代理 → 最终通知”的混合复杂工作流规划路径，并接入正式 `Workflow` 页面快捷入口。
  - 成果：混合复杂工作流已完成一轮真实 UI 验收，当前主链可以稳定跑通“前置分析 → 三路并行浏览器 → 三路截图 → 汇总代理 → 最终通知”。
  - 成果：已收敛任务完成系统通知的重复正文；当汇总代理结果与最终通知正文相同，系统完成通知不再第三次原样重复整份报告，改为保留完成状态与附件信息。
  - 成果：正式 `Workflow` 页面已补上运行态详情面板；现在可直接看到真实运行状态、当前动作、运行中步骤、已跳过步骤、开始/结束时间，以及失败或取消原因。
  - 成果：正式 `Workflow` 页的步骤卡已从“只有计划”升级为“计划 + 实际状态 + 关键结果/失败原因 + 关键运行信息”，浏览器/通知/代理步骤都能在同页验收。
  - 成果：`06` 已新增独立的正式 `Workflow Roles` 页面，并把执行角色/规划角色分组展示；正式 `Workflow` 页面也已直接跳转到该入口，不再要求用户先进入团队设置再找对应标签。
  - 成果：当前正式 `Next build` 已恢复通过；`/workflow/agents` 已进入最终路由清单，正式角色页不再停留在“代码已写但整包构建未过”的状态。
  - 成果：正式 `Workflow` 详情页已把“规划角色、当前运行角色、任务内角色分配”收进同页展示；现在能直接从任务详情看到调度代理、执行角色、系统 browser/notification 能力和各自边界，不必再切到测试页或配置页自行对照。
  - 成果：正式 `Workflow` 详情页已继续补进代理会话、任务/规划/执行记忆槽、隔离工作目录、输出目录、请求模型、耗时与 Token/API 调用等运行态资源信息，`06` 的独立会话与资源边界已开始能在产品界面直接验收。
  - 成果：正式 `Workflow` 详情页的 `03 调度判断` 已升级为调度诊断视图，当前可直接验收调度受理状态、初始判定与实际执行差异、浏览器/通知/多步/并行触发条件、规划产物校验结果、原始规划步骤，以及模型分析/执行前回退记录。
  - 成果：正式 `Workflow Roles` 页已新增“运行中代理会话”面板，并接入真实活跃代理会话数据；现在除静态角色配置外，也能在正式 UI 直接看到当前活跃会话、生命周期状态、隔离目录、记忆槽、能力边界，并可发起单代理中断。
  - 成果：正式 `Workflow` 详情页里的浏览器步骤现已支持截图直接预览，并提供截图文件 / 详细结果的正式打开入口；`05` 的浏览器产物验收不再只依赖绝对路径文本。
  - 成果：正式“执行记录 > 结果文件”tab 已继续补上浏览器下载文件的正式验收体验；现在除步骤 output 目录下的文本/图片外，浏览器真实下载得到的 `.xlsx/.pdf/.docx` 等文件也会随步骤产物进入该 tab，并以“打开本地文件 / 下载原文件”的方式展示，不再误用文本预览或因未复制进 output 而完全不可见。
  - 成果：正式 `Workflow` 详情页现已继续补齐 agent / code 节点的执行留痕；当前工作流步骤运行时会把步骤结果摘要同步写入 `schedule_run_steps`、旧 run 详情也会回退解析会话消息补摘要，且 agent / code 节点都会额外把 summary 落到本步骤 `output` 目录并写入 `_lumos_step_input.json` 输入快照，使“步骤详情 / 完整输入上下文 / 结果文件”不再只对浏览器节点完整、也不再出现“节点跑完但没有可见输出物”的黑盒状态。
  - 成果：正式 `Workflow` 任务的关闭 / 删除 / 执行记录停止已补真实执行收口；现在关闭任务会取消同任务下正在运行的 workflow，删除任务前也会先取消底层执行，执行记录页新增“停止执行”，一次性任务取消后会自动暂停，且当历史 run 记录已被删但 workflow projection 仍残留 running 时也会按任务名兜底取消，减少“UI 看似删除但底层还在跑”的断链。
  - 成果：正式 `Workflow` 执行记录页已补第一版节点级重跑；失败记录可点击“从失败节点重跑”，用户也可在工作流结构图选中具体执行节点后点击“从此节点重跑”。重跑会新建一条执行记录，不改写旧记录，并复用原执行中目标节点上游已经完成的步骤输出，目标节点和其下游会重新执行；自动失败节点定位按每个节点的最新终态判断，避免被同一节点早期重试失败误导。
  - 成果：按 `01 / 02 / 03` 设计文档要求，主 Agent 复杂请求已补上正式下发闭环：命中复杂任务时会直接创建 Task Management 任务并返回交接确认，不再在主对话里自己执行整项任务；同时 `createTask` 生成的任务会正式回写来源用户消息与助手确认消息。
  - 成果：按用户最新要求，主 Agent 页已去掉临时任务面板，任务标签也已从全局左侧导航撤下，改为出现在聊天界面右侧的轻量任务标签；用户可直接点击标签跳到标准 `Workflow` 任务详情查看报告，不看详情时仍由主 Agent 在对话里汇报结果。
  - 成果：按用户最新确认，主 Agent 已开始接入 Lumos“管家”第一阶段只读能力；当前主 Agent 会话会获得内置 `lumos-butler` 工具，可读取全局状态摘要、搜索历史会话 / 消息 / 任务 / Workflow / DeepSearch / 能力记录，并查看任意会话的最近消息和关联任务；同时已新增 `/main-agent/butler` 只读总览页，可查看更完整的诊断、运行资源、最近会话 / Workflow / DeepSearch、能力和浏览器概览，并可在同页搜索历史会话、消息、任务、Workflow、DeepSearch 和能力记录；按最新界面收口，主 Agent 聊天页顶部“Lumos 管家状态”条已移除，聊天页不再默认展示管家状态面板。最新又把主 Agent 的只读 Workflow 查询补到任务 / 计划任务 / 执行记录 / 单次执行步骤 / 运行中 Agent 会话，主 Agent 不应再回答“工作流没有查询接口”。该阶段只做诊断和引导，不包含删除、覆盖、支付、发 IM、敏感导出、批量操作、审批 / 回滚 / 治理等高风险动作。
  - 成果：图片模块已新增 `Nano banana2（ToAPIs）` 的第一阶段接入；当前正式预设会把 ToAPIs 的“上传参考图 -> 创建异步任务 -> 轮询状态 -> 下载结果”接进统一图片生成主链，不再误复用 Google 官方 Gemini 原生协议；同时已开始支持文生图、图生图/多参考图、极端宽高比与 `resolution` 元数据透传，便于先承接与万相相近的一批电商出图/改图场景；图片参数 UI 也已从旧确认卡片扩到真实聊天入口与图片服务商编辑弹窗，当前会按图片模块正在使用的服务商动态展示可用比例、分辨率、生成张数上限、参考图上限与专有高级参数入口，优先适配 `Nano banana2（ToAPIs）` 的极端宽高比和高参考图上限；同时图片服务商现已支持在设置里保存默认比例/分辨率/张数，并在真正的生成运行时自动生效；Pro 图片生成工具已移除旧的“每会话最多 10 张”运行时限制与结果字段，改为按张计费口径，聊天侧不再应根据历史 `generation_count / generation_limit` 推导剩余额度；最新还已修正 OpenAI-compatible 图片服务商的参考图编辑 multipart 字段，避免 `gpt-image-*` 网关忽略 `image[]` 后报 `image is required for edits`，并把图片生成业务失败改为 `success:false` JSON 返回，减少并行工具调用时被 SDK 放大成 `Sibling tool call errored`。
  - 成果：图片生成的 Lumos 云配额扣费请求已补 Node 侧代理支持；当桌面端/工作流进程设置了 `HTTPS_PROXY / HTTP_PROXY` 时，配额接口会显式走代理并遵守 `NO_PROXY`，减少“浏览器或 curl 可访问云端，但工作流生图在扣费前因 `fetch failed / ECONNRESET / ENOTFOUND` 失败”的断链。
  - 成果：已定位 `745879e0` 这类电商场景图工作流在 `generate-scenes` 节点反复失败的直接原因：失败发生在场景图生成师的 Claude 兼容模型首轮请求阶段，trace 中没有 `generate_image` 工具调用，说明还没进入真实图片服务商；该节点会把抠图和多张参考图作为上游图片二进制一起注入模型上下文，兼容网关长时间处理后返回 `bad_response_status_code 500`。当前运行时已对 `generate-scenes` / 场景图生成师这类“场景图生成”节点跳过原始多图二进制注入，只保留上游文件路径供 `generate_image.reference_image_paths` 使用；同时步骤 trace / metadata 会记录实际解析模型，workflow-agent 预设的 `model` 字段也会进入执行层。该修正已通过新增单测和相关 ESLint，但仍需用户在正式 `Workflow` 执行记录里从失败节点重跑完成 UI 验收，才能说这条场景图工作流已恢复。
  - 成果：支付模块已按用户最新要求收敛为“充值余额”第一阶段主链；当前桌面端充值弹窗不再展示月卡/套餐购买，用户可输入金额，当前仅展示支付宝支付，创建订单后直接在弹窗展示二维码，并会自动检测到账后显示“支付成功 / 余额已到账”；订单创建由桌面端转发到 `lumos-web`，后台按易支付签名规则生成支付链接，并在异步/同步回调中校验签名、支付状态和金额后给对应 new-api token 增加额度，同时记录订单到账状态以减少重复回调导致的重复入账风险。
  - 成果：Lumos Cloud DeepSeek 计费差异已完成三轮生产排查和收口；第一轮确认 new-api `CacheRatio` 缺少 `deepseek-v4-flash / deepseek-v4-pro` 别名并修正历史缓存折扣；第二轮确认工作流任务里的 Claude Code Task/subagent 仍会向 DeepSeek 渠道发出 `claude-haiku-4-5*` 辅助调用，导致按 Claude Haiku 倍率放大扣费，生产已补 DeepSeek 兜底别名并修正受影响历史日志、小时统计、token 余额、用户余额/used quota 和渠道 used quota，两轮累计返还 `172,383,838` quota（¥344.7677）；第三轮按后台“聊天服务商加价”重新核对 `lumos_chat_providers.model_catalog.markup_percent -> new-api ModelRatio/CompletionRatio/CacheRatio`，确认 `deepseek-v4-flash` 原价 ¥1/¥2 每百万 token + 40% 加价已正确同步为 `ModelRatio=0.7 / CompletionRatio=2`，`deepseek-v4-pro` 原价 ¥12/¥24 + 20% 加价已同步为 `ModelRatio=7.2 / CompletionRatio=2`，生产 `CacheRatio` 按 DeepSeek 2026-04-26 后的当前官方 cache hit 价格比例保持为 `deepseek-v4-flash=0.02`、`deepseek-v4-pro=0.0083333333`、DeepSeek 兜底 `claude-haiku-4-5* = 0.02`，使缓存命中也按上游当前成本比例叠加后台加价计费；排查期间短暂误设为旧 launch 价比例的 58 条请求已补回 `19,116` quota（¥0.038232）。客户端侧也已补 Claude Agent SDK 内部模型防护，非真实 Claude resolved model 会把 `ANTHROPIC_SMALL_FAST_MODEL / ANTHROPIC_DEFAULT_HAIKU_MODEL / ANTHROPIC_DEFAULT_SONNET_MODEL / ANTHROPIC_DEFAULT_OPUS_MODEL / CLAUDE_CODE_SUBAGENT_MODEL` 全部固定到当前 resolved model，减少工作流 agent / subagent 继续产生 `claude-haiku-*` 账单行；仍需补正式自动化计费回归，防止未来后台保存服务商时遗漏 CacheRatio 审计。
  - 成果：微信导出 / 本地微信读取已修正两处重装后读取断点；此前读取链路硬性要求存在 `message_0.db`，但当前用户本机微信修复后实际保留的是 `message_1.db ~ message_5.db`，导致 Lumos 报 `FileNotFoundError: 未找到 WeChat 数据目录`。当前已改为识别任意 `message_[0-9].db`；随后又定位到“会话列表可见但详情无消息”的原因是详情读取仍只用 `key.txt` 单密钥过滤消息分片，而当前可读的 `message_1.db` 需要按自身 salt 从 `wechat_keys.json` 取密钥。现已把消息库查询、表探测和发件人识别改为按库取密钥，并用本机 `list_sessions` 与 `read_chat` 只读查询确认会话列表和详情消息都能返回；最新又确认“左侧会话列表显示最新消息，但详情旧/空”的直接原因是左侧读取 `session.db` 摘要库，右侧读取 `message_*.db` 正文库，而当前本机 5 个正式消息分片中只有 `message_1.db` 可解密，`message_2.db ~ message_5.db` 缺少已保存密钥映射。现已在 `read_chat` 和独立 `diagnostics` 返回里补上消息库覆盖诊断，并在微信页面新增“修复消息读取不完整”入口：用户可在 Lumos 里检测消息库可读数量，点击“开始修复”触发 macOS 管理员授权、临时重签名并重开微信，随后 UI 会自动等待并进入“重新提取消息库密钥”；若微信已经是临时签名状态，按钮会明确显示“重新提取消息库密钥”，不再让用户误以为输入密码就是全部修复完成；提取脚本也已改为增量合并已有密钥，避免重提取时覆盖现有可读分片。另外已修正 `codesign -dv` 签名探测只读 stdout 的问题，避免微信已经是 `adhoc` 仍被状态接口误判为 `unknown`，并避免提取接口误报“微信仍是官方签名”。该修正只解决 Lumos 的分片识别、密钥选择、可见诊断和产品内修复入口，不代表微信原始数据库损坏、丢失聊天记录或缺少新分片密钥已经被 Lumos 修复。
  - 成果：知识库向量化的长期跨平台问题已正式记录并定位；Windows exe 报错 `embedding model initialization failed: Cannot read properties of undefined (reading 'create')` 时，诊断已显示模型文件实际存在于用户 runtime cache 和安装目录，因此不应继续按“模型文件缺失”方向排查。当前判断为 Electron/Next packaged server 中 portable `transformers.web.js` 在 Node 进程里误选 `onnxruntime-node` 空后端，导致 `InferenceSession.create` 为空；修复方向是让打包版索引统一走本地 `onnxruntime-web` WASM 后端，并显式绑定打包内 `ort-wasm-simd-threaded` 文件路径。
  - 待完成：知识库索引仍需完成新安装包级验收；必须分别在 macOS 和 Windows exe 上验证“导入内容 -> 向量化成功 -> 搜索命中”，在此之前不能把跨平台索引问题标为完整完成。
  - 成果：桌面端更新包体积治理已开始第一阶段前置改造；运行时已新增统一资源解析层，Node runtime / Python runtime / Git Bash / 本地 embedding 模型 / MCP runtime path 可优先从 `LUMOS_EXTERNAL_RESOURCES_DIR` 或 `~/.lumos/runtime-resources` 查找，再回退到安装包资源和开发态 `resources/`；同时已新增发布侧 runtime resources manifest 生成脚本，可先产出 Node/Python/Git-Bash/models 的文件清单、大小与 sha512，为后续独立资源下载、校验和缓存复用做准备。
  - 待完成：主安装包尚未真正瘦身；还需要补正式资源下载/校验/恢复 UI、资源包发布与 CDN/更新接口接入、首次安装缺资源兜底、跨平台打包验收，然后才能从 `electron-builder` 主包里移除稳定大资源。
  - 成果：已新增 `07-dynamic-capability-extension-design.md`，把“用户通过 LLM 动态新增系统能力，并让工作流后续可正式使用”定义为独立横切架构，不再硬塞进 `03 ~ 06` 任一单层文档。
  - 成果：已为 `07` 补上正式产品入口；左侧侧边栏现已新增“节点开发”菜单，并有独立 `/workflow/nodes` 页面用于展示当前正式节点边界、07 的能力建设方向与剩余缺口。
  - 成果：已按用户最新确认收敛 `07` 的产品形态：`新增能力` 继续以聊天页为主入口，尽量不扩 UI；AI 需先和用户对话确认需求，再直接生成两类待发布能力（`代码节点` / `Prompt 节点`）；“草稿”只保留为内部实现概念，不作为 Phase 1 主要产品心智。
  - 成果：`07` 已补上第一段正式主链：当前可在“新增能力”聊天页完成 `对话确认 -> 生成待发布能力 -> 发布`，能力管理列表和详情页也已切到“待发布 / 已发布”正式语义；其中 `Prompt 节点` 已进一步接入调度层发现，用户在任务里明确写出能力 ID 或名称时，会被规划进工作流 agent 步骤中使用。
  - 成果：`07` 已继续补上代码节点的第一条正式调用桥：当任务里明确提到某个已发布 `代码节点`，并同时提供结构化 JSON 参数时，调度层会生成真实 `capability` 步骤执行该节点；正式 `Workflow` 详情页也已开始把这类步骤显示为“系统能力节点”，并展示能力 ID 等运行态信息。
  - 成果：已开始兼容历史遗留能力文件；`~/.lumos/capabilities` 下已有的旧 `ts/md` 能力现已能进入当前能力发现范围，不再只停留在“文件存在但主链不可见”的状态。
  - 成果：主 Agent 的一个高频导出场景已补上第一条自动接能力的链路；当任务要求“整理报告/正文并导出 PDF”且存在可识别的格式转换代码节点时，调度层会在正文生成后自动追加能力步骤执行导出，不再一律回退为“PDF 导出需求已记录”占位话术。
  - 成果：`新增能力` 聊天助手已补上真实能力清单提示；后续只有在当前真实可发现能力列表里存在时，才应对用户说“已经有这个能力”。
  - 成果：`能力生成器` 的 Skill / MCP 安装链路已补第一轮体验收口；现在同名 Skill / MCP 会自动走更新而不是只显示 `exists`，分享包导入默认按覆盖更新处理，导出 / 导入 / 生成安装方案会把 Lumos 数据目录、用户目录、Python runtime 和 runtime resources 尽量写成可迁移占位符，生成的 Python MCP 模板和内置 JS MCP 入口也会按 `inputSchema` 对字符串化的 number / integer / boolean / array / object 参数做基础类型兜底；对于生成器安装的 stdio MCP，安装后还会执行 `initialize -> tools/list` 的启动自检，并在 UI 里用“已安装 / 已更新 / 自检失败”等用户可理解状态展示。
  - 成果：正式 `能力 > MCP 服务器` 管理页已补第一版手动健康检查体验；用户现在可以对单个 MCP 点“检测服务器”，也可以点“检测全部”，页面会展示“未检测 / 检测中 / 可用 / 失败”等状态和失败原因；手动新增、更新或同名覆盖 MCP 后也会自动跑一次基础启动检测。同时保存表单会自动拆分误填在 command 输入框里的整条 `npx -y ...` 命令，减少小白用户因 command / args 分不清导致的启动失败。最新还已把 MCP 健康检查结果持久化到本地库，刷新页面后仍能看到上次检测状态、失败原因、检测时间和工具数量；能力生成器安装后的 stdio MCP 自检也会同步写回该状态；MCP 配置也已新增“按需启动 / 长连接声明”运行方式意图和 `auto / Node / Python / Bun / 自定义` 运行时声明，并同步进入管理页表单、列表徽标、导入导出包、内置 MCP 配置和能力生成器计划格式。远程 HTTP / SSE MCP 检测已从基础连通升级为第一版协议级检测：HTTP 会执行 `initialize -> notifications/initialized -> tools/list`，旧 SSE 会先识别 `endpoint` 事件再执行同样的初始化和工具列表检查，`401 / 403 / 404` 不再被误标为可用。
  - 待完成：聊天侧任务标签目前已满足“轻入口 + 点开看正式详情”，但更细的展示策略和交互规则仍可能继续调整，还不是最终完整产品形态。
  - 待完成：把内部完成度和 UI 可见能力进一步对齐，减少“内部已通但 UI 不可验”的区域。
  - 待完成：补齐 `06` 的正式执行代理 UI，并与现有团队设置页分层，避免和旧 team-run 角色预设混淆。
  - 待完成：把 `03` 调度信息与 `06` 运行态角色信息继续收进正式详情页，而不只留在测试页或配置页。
  - 待完成：`07` 仍未完整打通；虽然对话式确认、能力生成、发布入口、`Prompt 节点` / `代码节点` 的显式任务引用、报告导出到 PDF 的自动接能力主链，以及能力生成器的同名覆盖更新、路径变量化、安装后基础自检、参数类型兜底、MCP 管理页手动健康检查、健康状态持久化、运行方式/运行时声明和第一版远程 MCP 协议检测都已具备第一段可验收实现，但更通用的自动发现、自然语言参数提取、审批、回滚、`keep_alive` MCP 后台守护 / 崩溃自动重启、Bun 运行时托管或自动安装、受保护远程 MCP 的登录授权引导和更完整运行时治理都还没有完成。
- 阶段 4：DeepSearch 独立模块设计
  - 成果：已确认 `08 DeepSearch` 不应先耦合到 Workflow，而应先做独立模块，再由聊天和 Workflow 复用。
  - 成果：已确认产品需要在左侧侧边栏 `扩展` 下新增正式 `DeepSearch` 页，用于登录态配置、历史记录和抓取内容查看。
  - 成果：已新增 `08-deepsearch-requirements-design.md` 与 `08-deepsearch-architecture-design.md`，明确产品需求、模块边界、登录态主链和对外调用形态。
  - 成果：已新增 `08-deepsearch-bb-browser-integration-design.md`，明确 `bb-browser` 更适合作为能力样板而不是直接作为正式运行时接入，并收敛出 Lumos 应吸收的 `site adapter / session fetch / compact snapshot / network capture` 方向；结合源码复核后，方案已进一步具体化为在 Lumos 内增加 `bb-site compatibility runtime`，优先承接经过审查的 Tier 1 / Tier 2 adapter。
  - 成果：已新增 `08-deepsearch-deployment-and-local-usage-design.md`，明确 DeepSearch 的 Phase 1 正式形态应为 Lumos 内置模块；本地使用方式应是“通过本地 Lumos 实例直接使用”，而不是先做成独立安装的外部 tool。
  - 成果：已新增 `08-deepsearch-phase-1-implementation-design.md`，把 `08` 的第一阶段交付范围、实现顺序、站点优先级和严格 UI 验收标准显式写清。
  - 成果：已新增 `08-deepsearch-ui-and-interaction-design.md`，把正式页的布局、按钮、状态、详情区、当前页接管确认、等待登录恢复、暂停/恢复/取消和 `partial` 展示方式进一步落成可实现交互。
  - 成果：已新增 `08-deepsearch-data-and-api-design.md`，把 `run / run page / checkpoint / record / artifact / site state` 数据边界，以及 `DeepSearch Service / tool facade / Workflow capability` 的接口合同进一步写清。
  - 成果：已新增 `08-deepsearch-engineering-implementation-design.md`，把当前仓库里的真实目录落点、`/extensions` 页 tab 接入、browser bridge 复用、DB 迁移文件位置和开发顺序进一步拆成工程可执行方案。
  - 成果：已把两条最新架构决策正式写回 `08` 文档：DeepSearch 可以正式接管用户当前浏览器中的活动页；同时正式支持 `strict / best_effort` 两种执行语义，非严格模式下允许先跑能跑的站点并以 `partial` 收口。
  - 成果：已统一 `08` 文档中的运行状态枚举、恢复语义和 Phase 边界；当前口径收敛为 `pending / running / waiting_login / paused / completed / partial / failed / cancelled`，其中 Workflow 正式复用进入 Phase 2，而不是继续和 Phase 1 混写。
  - 成果：`08` 已开始正式代码实现；当前左侧 `扩展` 页中已新增 `DeepSearch` tab，并已落地站点登录态管理 UI、DeepSearch 本地 SQLite 表、`/api/deepsearch/sites` 与 `/api/deepsearch/runs`、抓取记录历史列表、详情面板，以及 `strict / best_effort`、`takeover_active_page / managed_page` 的正式参数落库与展示。
  - 成果：`08` 已继续补上第一条真实浏览器运行时接线；当前 DeepSearch 已新增共享 browser bridge client、独立 DeepSearch service、`/api/deepsearch/runtime/page-binding` 预览接口，以及“当前活动页可否接管”的正式 UI 预览，不再只靠静态文案假设浏览器状态。
  - 成果：`08` 已把“接管当前活动页”从预览升级为 run 级正式绑定；当前在 `扩展 > DeepSearch` 创建 takeover 任务时，会尝试锁定浏览器当前活动页并落库到 `run page` 记录，详情面板也可直接验收页面标题、URL、pageId、绑定类型和绑定时间。
  - 成果：`08` 已继续把 run 从“只做绑定和草案”推进到“真实执行基础页面快照”；当前创建或恢复可执行任务后，DeepSearch 会通过 browser bridge 真正选中/创建页面，抓取页面 snapshot 与 screenshot，并把摘录和本地截图路径回写到任务详情。
  - 成果：`08` 已补上第一版站点级共享登录探测；当前 DeepSearch 会通过 browser bridge 读取内置浏览器共享 cookie，对预置站点执行 auth cookie 检查，把结果写入站点状态，并在 run 执行前真实决定是否进入 `waiting_login` 或继续执行；正式页站点卡片也已新增“检查登录态”按钮，可直接验收探测结果。
  - 成果：`08` 已补上第一版登录恢复动作；当前站点卡片和 `waiting_login` 详情都可直接打开站点登录页、重新检查登录态，并且 takeover 模式在恢复执行前会尝试重新绑定当前活动页，不再只停留在“提示用户自己处理”的文案层。
  - 成果：`08` 已补上第一版结果主链结构化持久化；当前运行结果会正式落到 `deepsearch_records / deepsearch_artifacts`，并新增 artifact 读取 API 与详情页记录/正文/截图查看，不再只靠 detail markdown 塞摘录和文件路径。
  - 成果：`08` 已补上第一版聊天 tool facade；当前通过 Lumos 内置 `deepsearch` MCP facade，把 `start / get_result / pause / resume / cancel` 统一接到同一 DeepSearch service，聊天侧也会注入当前会话 `sessionId` 并在相关诉求下优先提示模型调用该高层能力；同时 `扩展 > DeepSearch` 已支持 `runId` 深链，聊天结果可以直接落到对应 run 详情验收。
  - 成果：`08` 已补上第一版站点 adapter runtime，并优先接入 `zhihu`；当前对于知乎页面，运行时会优先区分问题详情页、文章详情页和列表页，尝试展开“阅读全文”，抽取问题/回答或文章正文，写回更接近真实页面结构的 `contentState / snippet / structured_json`，同时保留失败时回退到通用正文抓取的兜底。
  - 成果：`08` 已继续把知乎搜索结果页推进到“同一 run 自动跟进详情页”；当前当 seed 页被识别为知乎 `list_page` 时，会自动挑选最多 3 个详情 URL 创建托管页并继续抓取，其中已补上 `zhuanlan.zhihu.com/p/...` 专栏正文地址的正式支持；相关站点路由规则已补单测，最新整包 `Next build` 也再次通过。
  - 成果：`08` 已继续补到正式 UI 可验层；当前 `扩展 > DeepSearch` 的任务详情里，绑定页面与抓取记录已改为按同一页面链路联动展示，用户可以直接看到哪一页是搜索页、哪几页是自动跟进的详情页，以及每个绑定页下面实际产出的正文/截图/结构化快照，不再需要在“绑定页面”和“抓取记录”两块之间手工对照。
  - 成果：`08` 已把 `waiting_login` 的恢复编排从页面脚本下沉到独立 service / API；当前正式页后台自动恢复不再自己串联“逐站点 recheck + 逐任务 resume”，而是统一走服务端 `探测 -> 判定 -> 恢复 -> 回写 runs/sites`；同时抓取历史卡片也已补上每个任务的状态说明，用户不打开详情也能直接看到当前卡点。
  - 成果：`08` 已补上第二条认证源主链；当前除直接复用内置浏览器共享登录态外，用户在站点配置里提供的 cookie 也会被解析并尽力导入到内置浏览器，再进入统一登录探测与恢复流程，不再只是数据库里的备注字段。
  - 成果：`08` 已继续补上第一版“显式页面验证”校验；当前在保存站点 cookie 或手动点击“检查登录态”时，系统除看 cookie 命中外，还会对部分站点打开验证页做一次真实页面级判断，用于识别“cookie 长得像已登录、但页面仍落到登录态”的假阳性；同时这类站点在后台 cookie-only 轮询下也不会立刻被重新放行为 `connected`，直到下一次显式校验通过。
  - 成果：已收口 DeepSearch 登录态链路里的三处关键回归风险：后台 `waiting_login` 恢复轮询不再反复重导用户保存的 cookie 以免覆盖浏览器里更新后的真实登录态；“站点 ready” 现已只认 live probe 的 `connected`，不再把手工 `cookieStatus=valid` 误当成可运行；同时内部 `PAGE_VALIDATION_BLOCKED` 哨兵也已从正式 UI 文案中隐藏，保存站点配置只做轻量 cookie 探测，抢焦点的页面级验证收敛到显式“检查登录态”动作。
  - 成果：`08` 已继续收口一轮正式 UI 与浏览器接线缺陷；当前 `扩展 > DeepSearch` 页面已重排为“站点接入 / 抓取发起 / 历史与详情”的分区结构，不再把站点、任务和结果硬堆在同一长列；同时 browser bridge 现已把真实异常信息回传给 DeepSearch，且“打开登录页”链路里非关键的页面稳定/CDP 检查失败不再直接把整次打开动作打成 `INTERNAL_ERROR`；另外手动 `resume` 与聊天 tool `resume` 也已默认停止重导旧 cookie，避免用户刚完成共享登录后又被过期配置覆盖，而 takeover 模式在当前活动页已切到无关站点时也会主动清空旧绑定，回到“等待可接管页面”而不是带着错误页面继续执行。
  - 成果：`08` 已补上聊天触发 DeepSearch 时的非打扰后台浏览器约束；当前托管页、站点隐藏页、显式页面校验页和由后台自动化页触发的新页 / 弹窗都会继承后台属性，不再通过 `content-browser:open-url-in-tab` 打开右侧浏览器面板，也不会作为用户可见页签持久化；同时聊天流不再在收到 `chrome-devtools` 工具调用时自动展开右侧面板，普通聊天、桥接会话与 StageWorker 加载 browser MCP 时也会默认注入后台模式，减少模型误用原始浏览器工具访问知乎/微信公众号时抢占用户界面的风险；只有用户显式点击“打开登录页”这类登录操作才保留前台打开行为。
  - 成果：`08` 已开始从“浏览器型反爬站点”向“免登录公开资料源”扩展；当前已新增 `中文维基文库` 站点 adapter 原型，并接入默认站点清单、免登录判定与聊天侧站点别名，DeepSearch 现可沿同一 adapter 执行链对中文维基文库执行搜索和机器可读正文提取，为后续图书/古籍型资料源接入建立第一条正式主线。
  - 成果：`08` 已继续补上第二条中文公开全文源主线；当前已新增 `Chinese Text Project` 站点 adapter 原型，并接入默认站点清单、免登录判定与聊天侧站点别名，DeepSearch 现可沿同一 adapter 执行链对 CTP 执行标题检索、`readlink -> gettext` 正文提取与保守 HTML 回退，为古籍/经史子集类资料搜索继续扩面。
  - 成果：`08` 已补上第一条英文公开图书主线；当前已新增 `Project Gutenberg` 站点 adapter 原型，并接入默认站点清单、免登录判定与聊天侧站点别名，DeepSearch 现可沿同一 adapter 执行链对 Gutenberg 执行官方 OPDS 书目搜索和官方 plain-text 全文提取，为英文公共领域图书搜索建立正式接线。
  - 成果：`08` 已补上第一条公开论文主线；当前已新增 `Europe PMC` 站点 adapter 原型，并接入默认站点清单、免登录判定与聊天侧站点别名，DeepSearch 现可沿同一 adapter 执行链对 Europe PMC 执行官方论文搜索、开放全文 XML 提取与摘要兜底，为生命科学论文搜索建立正式接线。
  - 成果：`08` 已继续补上高质量结构化论文全文主线；当前已新增 `PMC BioC` 站点 adapter 原型，并接入默认站点清单、免登录判定与聊天侧站点别名，DeepSearch 现可沿同一 adapter 执行链对 PMC BioC 执行开放获取论文搜索与结构化全文 JSON 提取，为高质量医学/生命科学全文研究继续扩面。
  - 成果：已修正 DeepSearch run 创建层的站点 ready 判断分叉；`Project Gutenberg / 中文维基文库 / CTP / Europe PMC / PMC BioC / 微信文章` 这类免登录公开源现在不会因旧 `cookie_status=missing` 被错误打成 `waiting_login`，知乎等登录站点仍只认 live probe 的 `connected`，不会因手工 cookie 字段绕过登录探测。本轮真实接口验证中，知乎当前本机状态为 `connected`，公开源 run 已不再误报登录阻塞；但单独运行 Next dev server 时仍缺少 Electron browser bridge runtime，因此真实页面抓取不会继续执行。
  - 待完成：补齐更强的自动登录完成检测与自动回收、执行期更细粒度页面控制、更强的完整正文抽取，以及 Workflow capability facade 的正式实现。
- 阶段 5：浏览器 Provider / 指纹浏览器接入
  - 成果：已新增 `docs/browser-provider-design.md`，明确浏览器接入不是第三方 MCP/plugin，而是 Lumos 内置的本地 Browser Context 能力；核心目标是让 chat / Workflow / Agent 浏览器工具绑定到明确浏览器身份，且不复制用户在指纹浏览器里的 cookie、登录态、指纹或代理资产。
  - 成果：已开始落地第一段基础架构：新增 `BrowserProvider / BrowserAutomationSession` 两层抽象，先把现有内置浏览器包装为 `embedded:default` 默认上下文；browser bridge、chrome-devtools MCP、共享 browser runtime client 和 workflow `ctx.browser` 已开始支持 `browserContextId` 透传，旧路径默认仍回到内置浏览器。
  - 成果：已补上第一版 `ExternalCDPProvider` 兜底能力；当本地配置 `LUMOS_EXTERNAL_CDP_ENDPOINT` 或 `LUMOS_BROWSER_EXTERNAL_CDP_ENDPOINT` 指向 Chromium DevTools HTTP / browser websocket endpoint 时，bridge 可通过 `external-cdp:default` 上下文对外部 Chromium 执行最小 tab / navigate / snapshot / evaluate / screenshot 自动化。当前仍是环境变量 POC，不是正式设置页接入。
  - 成果：已补上第一版 `AdsPowerProvider` 单 profile POC；当本地配置 `LUMOS_ADSPOWER_USER_ID` 或 `LUMOS_ADSPOWER_PROFILE_ID` 时，会通过 AdsPower Local API 启动对应 profile，并复用返回的 `data.ws.puppeteer` CDP endpoint 暴露为 `adspower:<profileId>` 浏览器上下文。当前仍是环境变量 POC，不是正式设置页接入。
  - 成果：已按设计约束把 DeepSearch 的 browser bridge 调用显式固定到 `embedded:default`，避免环境变量 POC 或后续用户选择第三方 profile 时把 DeepSearch 的公开内容抓取链路误路由到店铺账号浏览器。
  - 成果：已新增本地持久化第一版：`browser_provider_configs` / `browser_profile_aliases` 两张表、`chat_sessions.browser_context_id` 字段，以及由 Next 设置 API 写出的 `~/.lumos/runtime/browser-providers.json`；Electron `BrowserProviderRegistry` 会读取该运行时配置并在签名变化时热加载，避免只停留在环境变量 POC。
  - 成果：已新增正式 `设置 > 浏览器` / `/settings/browsers` 第一版页面；用户可在产品界面看到内置浏览器，添加 / 编辑 / 删除 `AdsPower` 或 `通用 CDP` 接入，填写 Local API / profile_id / CDP endpoint / API Key / 备注，并点击“测试”看到连接状态和发现的 profile / page 摘要；当前添加 / 编辑 AdsPower 时也可先通过 Local API 发现 profile 列表，支持单个绑定到当前配置或批量导入为多条浏览器配置，减少手抄 `user_id` 的出错风险；第三方浏览器列表和发现到的 Profile 列表也已支持按名称 / 别名 / Profile ID / Context / 分组搜索，并可按 Provider / 启用状态 / AI 操作中 / AdsPower 分组筛选；批量导入和单个绑定会把 AdsPower 分组 / 序号写入备注。
  - 成果：已补 AdsPower 多 Profile 分组视图与手动同步第一版；`设置 > 浏览器` 会按 AdsPower 分组展示第三方浏览器卡片，并提供“同步 AdsPower”按钮，从本机 Local API 分页拉取最多 500 个 Profile，先展示新增 / 更新 / 不变 / 跳过的同步预览，用户确认后才会自动创建缺失配置、刷新已存在配置的 profile 名称 / 分组 / 序号，同时保留用户自定义显示名和人工备注。
  - 成果：chat 顶部已新增浏览器上下文选择器；用户可在会话标题旁看到当前使用的浏览器上下文，手动切换到已配置的第三方 context，切换会持久化到当前会话并在后续聊天浏览器工具调用中注入 `x-lumos-browser-context-id`，服务端也会用会话字段兜底。
  - 成果：工作流任务已新增浏览器选择第一版；用户在新建 / 编辑任务时可选择内置浏览器或已配置的 AdsPower / CDP context，任务列表和任务详情会显示本任务使用的浏览器，执行时会把同一个 `browserContextId` 写入工作流 session、`__lumosRuntime`、代码节点 `ctx.browser` 和 StageWorker 的 `chrome_devtools` MCP 环境。
  - 成果：已修正工作流浏览器绑定的几个验收断点：任务创建 / 编辑会在服务端校验所选浏览器是否存在且启用；“立即运行”接口不再接受隐藏 `browserContextId` 覆盖，避免实际执行和任务 UI 不一致；每条执行记录会保存当次实际浏览器快照；任务列表、任务详情、执行历史和执行详情页会显示配置名称（例如 `AdsPower · 浏览器1`），不再只显示 `adspower:<profileId>`。
  - 成果：chat route 已补第一版 profile 名精确匹配；当用户在消息里明确说出已配置的浏览器显示名 / Profile 显示名 / profile_id，例如“浏览器1”，服务端会自动把当前会话切到对应 browser context；显式浏览器操作请求会隔离到 `chrome_devtools` MCP，禁用 `Bash` / `Task` / WebFetch / WebSearch / 常规文件工具，并强制开启新的 Claude SDK 会话，避免模型退回系统默认 Chrome 或复用没有浏览器工具的旧会话。
  - 成果：已修正聊天前端旧请求头覆盖服务端匹配结果的问题；此前用户说“打开浏览器1，访问知乎”时，服务端会话已是 `adspower:k1c1fbjj`，但 MCP env 仍优先使用旧的 `x-lumos-browser-context-id: embedded:default`，导致真实工具结果落到内置浏览器。当前显式 profile 匹配优先，且 MCP env 注入优先使用服务端解析后的 `browserContextId`。
  - 成果：已用当前本机 AdsPower 配置验证 `浏览器1` 的 bridge / MCP 直连链路：`adspower:k1c1fbjj` 可以通过 Lumos browser bridge 和 `chrome_devtools` MCP 列出页面，并成功在该 profile 中打开 `https://www.baidu.com/` 与 `https://www.zhihu.com/`。这证明 provider / bridge / MCP 层在当前机器上可用，但还需要用户从聊天 UI 重新发起一次确认 LLM 主链不再走系统 Chrome 兜底。
  - 成果：已定位聊天 UI 里“chrome-devtools 工具不可用”的两处真实原因：Claude SDK 实际可调用的 MCP 工具名不能使用 `mcp__chrome-devtools__...` 这种连字符形式；同时 `chrome_devtools` MCP 进程曾依赖未安装的 `@modelcontextprotocol/sdk`，导致进程启动即退出。当前已把 SDK 注册名映射为 `chrome_devtools`，把聊天 / 工作流代理生成提示改为 `mcp__chrome_devtools__...`，并把 `chrome_devtools` MCP 改成项目内轻量 stdio JSON-RPC 实现。按用户要求，显式浏览器操作请求也会跳过 DeepSearch MCP，避免浏览器失败后绕路到 DeepSearch。
  - 成果：已修正多浏览器上下文下的站点页缓存串用风险；bridge `/v1/site-pages/*` 的持久页缓存现在按 `browserContextId + domain` 隔离，避免内置浏览器和 AdsPower / CDP 访问同一站点时复用错误 `pageId`。
  - 成果：已补浏览器配置生命周期第一版防护；启用配置时会校验 AdsPower profile_id 或 CDP endpoint，聊天会话切换和工作流执行前会复用同一校验，删除、停用或修改会改变 context 的 profile_id 时也会先检查是否仍有聊天会话或工作流任务引用，避免 UI 仍指向已失效浏览器。
  - 成果：已补 Profile 别名第一版；`设置 > 浏览器` 可维护别名，chat route 自动匹配浏览器时会同时检查显示名、Profile 显示名、profile_id 和别名。
  - 成果：已补 AdsPower context 唯一性防护；同一个 `profile_id` 不允许重复创建成多个 `adspower:<profileId>` 配置，批量导入时会自动跳过已存在的 profile。
  - 成果：已补引用提示第一版；`设置 > 浏览器` 的浏览器卡片会展示当前被聊天会话 / 工作流任务引用的数量，删除、停用或改 profile_id 被阻止时也会返回同一类引用信息。该能力只是引用防护和可见提示，还不是运行态占用锁。
  - 成果：已补运行态占用锁第一版；browser bridge 会对非内置浏览器上下文的写操作按 session / workflow owner 做内存租约，同一 owner 可连续续租，其他 owner 在租约未过期时会先做最长 10 秒短等待，若期间对方释放则自动接续，仍未释放时返回 `BROWSER_CONTEXT_IN_USE`、`waitedMs` 和 `retryAfterMs`，且 `chrome_devtools` MCP 会把 bridge 返回的中文冲突说明一并暴露；`设置 > 浏览器` 也已能读取运行态占用状态并提供“释放占用”按钮，用于用户手动接回或清理卡住的租约，页面会定时刷新占用状态。该能力还没有完整 FIFO 排队、冲突弹窗和任务内接手确认，因此不能算完整占用协调。
  - 成果：已补运行态占用冲突 UI 第一版；聊天页能识别 `BROWSER_CONTEXT_IN_USE` 工具错误并显示“浏览器正在被占用”横幅，提供释放占用、释放并重试、切回内置浏览器；工作流执行详情页也能在占用失败时显示同类提示并提供释放占用 / 返回任务详情入口。该能力仍不是完整排队系统。
  - 待完成：完整 FIFO 等待队列 / 更完整的任务内接手确认、AdsPower 自动周期同步 / 大账号分页实机验收、外部浏览器实机验收、跨平台打包验收仍未完成；当前第三方指纹浏览器接入主链已打通，但还不能按完整实现标准关闭全部缺口。
- 阶段 5：应用平台 / AI 创建器
  - 成果：应用列表、新建应用弹窗、草稿详情页、已安装应用打开链路、`.lumos-app` 本地包导入，以及 workflow 转应用的基础链路已具备第一版实现。
  - 成果：应用开发界面已按用户最新要求收敛为“主区域 `预览 / 代码 / 需求 / 项目状态 / 详情` 切换，底部直接复用工作流编辑同源 `BottomChatPanel + ChatView + MessageInput` 对话栈承载 `应用开发助手`”；不再使用“左侧结构 + 中间预览 + 右侧调整/检查/文件”的开发者工作台布局，也不再把 AI 对话固定在右侧。
  - 成果：应用开发助手已接入第一版真实 AI 对话接口；用户在底部对话框发送需求后，会调用配置的文本生成服务商，返回可写入的 `app.json / routes.json / data-schema.json / pages/*.json / workflows/*.json` 文件并先做 schema 自检，通过后写入草稿并驱动上方预览刷新，不再用“已记录需求”的假回复代替。
  - 成果：应用开发助手已继续补上第一版流式执行通道；底部对话框发送后会通过 SSE 展示真实执行状态，后端先尝试流式模型输出并在结构化结果不稳定时回落到严格结构化输出，避免 UI 长时间只显示“生成中”。
  - 成果：应用开发助手已从“一次性 JSON 生成”升级为服务端工具循环；AI 每轮返回 `upsert_story / write_file / validate_app / finish` actions，服务端真实执行 Story 新增/更新、文件暂存与 `validate_app` 交叉校验，最多 4 轮把校验错误回灌给模型修复，只有无 error 时才写入草稿文件。
  - 成果：应用开发助手的工具循环已进入底部对话可见层；用户现在能看到“写入文件 / 应用自检 / 需修复”的真实执行步骤和相关文件，不再只看到不可读的工具 JSON 或单一“生成中”状态。
  - 成果：应用开发界面已继续收敛掉内部开发痕迹；空白草稿预览不再展示 `页面 / 数据 / 安装` 阶段卡，也不再暴露“生成/刷新画布”的本地草图按钮；底部应用开发助手已从自定义聊天 UI 改为复用工作流编辑同源 `ChatView / MessageInput`，不再只是套一个相似的底部外壳；详情 tab 也已去掉开发进度和安装检查，只保留可编辑的应用基本信息。
  - 成果：应用开发页已新增独立 `需求` tab 和 `项目状态` tab；需求已从单个文本草稿升级为一条条 Story，可在 UI 中编辑标题、用户故事、状态、优先级、验收条件、关联页面和关联数据集合并保存，项目状态集中展示草稿状态、文件概览和安装前检查，避免再挤进详情页。
  - 成果：应用开发页 `需求` tab 已从“每条 Story 一个大表单卡片”的混乱形态重设计为“左侧 Story 列表 + 右侧单条编辑器”；列表只展示标题、状态、优先级和关键摘要，右侧集中编辑当前 Story，减少重复边框和输入框堆叠。
  - 成果：应用开发页 `代码` tab 已补文件版本查看、当前版/上一版对照和“回滚上一版”入口；回滚会写入工具消息并刷新当前草稿，减少 AI 改坏文件后只能重开的风险。
  - 成果：`设置 > AI助手` 已新增“应用开发助手”配置项，可单独设置应用开发对话使用的服务商、模型和系统提示词。
  - 成果：应用开发助手已接入第一版应用开发 SOP prompt，要求先做需求访谈，把需求拆成用户能理解的 Story，并通过 `pending_confirmation / confirmed / in_progress / implemented / accepted` 等状态推进；Story 已有独立 `lumos_app_builder_stories` 存储和 API，不再只藏在对话文本或 prompt blob 里。
  - 成果：应用开发助手已补上工具循环 structured output 兼容与沙箱预览关键收口；当模型把 `acceptanceCriteria / relatedPages / relatedCollections / set_non_goals.items` 输出成单字符串，或把优先级输出成 `P1` 这类格式时，服务端会先归一化再校验，减少“response did not match schema”直接失败；同时 builder 预览 appId 现已能从 `bs_...` 可逆映射到 `builder-bs-u-...`，React / ReactDOM / scheduler runtime 也已改为 ESM 预构建，减少 `app not found` 和 `Dynamic require of "react" is not supported` 这类预览启动错误；本轮已用“用户输入主题，用 DeepSearch 搜资料，然后出报告”真实 API 自测跑到 Story 落库、Demo 文件生成与编译通过，但生成的 demo 仍是模拟 DeepSearch 搜索，不等于完整真实 DeepSearch 集成。
  - 成果：应用新建入口已补第一版模板起点；用户可选择 `空白应用 / 客户跟进 / 周报助手 / 资料整理`，非空模板会在创建草稿时直接写入可预览的 `app.json / routes.json / data-schema.json / pages/*.json`，底部 AI 会带着模板上下文继续迭代。
  - 成果：本地开发草图现已进入服务端接口，不再由浏览器页面直接拼包；草图会生成最小 `app.json / routes.json / data-schema.json / pages/*.json`，可在 UI 里预览页面、查看 JSON 文件、通过安装前检查，并保存安装成可打开的本地应用。
  - 成果：应用 Builder 安装接口已接入标准安装流程；当当前草稿无额外权限时可直接安装，有权限时复用授权弹窗，同时修正了 Next 路由环境下 app schema 目录解析问题，避免安装时因 `/ROOT/resources/app-schemas` 找不到 schema 而失败。
  - 成果：Electron 开发启动脚本已改为统一端口编排；当 3000 被其他项目占用时，会自动选择可用端口并同时传给 Next、wait 和 Electron，避免 Next 自动跳到 3002 但 Electron 仍等待 3000 的错配，同时 `LUMOS_EDITION=pro` 会映射到 `NEXT_PUBLIC_LUMOS_EDITION=pro`。
  - 成果：按用户最新产品方向，应用平台已新增第一版内置 `微信助手` Demo 入口；应用列表可直接打开内置微信助手，不走 AI 自由生成应用链路。当前页面已从早期 `智能分析 / 内容洞察 / 内置任务 / 微信助手 / 数据授权` 五页签，收敛为新的 `概况 / 跟进 / 自动化 / 设置` 产品形态；`概况` 复用本机微信读取授权和本地 mirror，同步后可展示互动 Top、近期话题、久未联系和自定义报表入口；`设置` 可保存 AI 服务商、提示词、话题白名单、概况显示项和排除名单，未接到真实自动运行链路的分析频率 / 通知渠道控件已从正式设置页移除。本轮已把 `跟进` 从 mock/前端内存改为真实 `wechat_assistant_todos` 存储：AI 分析按钮会调用真实 `/api/apps/builtin/wechat/ai-analysis`，AI 建议可采纳/忽略，手动新建、编辑标题/概览/下一步、完成和删除都走后端并可刷新保留；同时已把 `自动化` 从 mock/前端内存改为本机持久化 API，并接入 Lumos Scheduler / Workflow：新建、开关、编辑和删除会同步背后的计划任务，已接入调度的自动化可在微信助手里直接“立即运行”并跳转查看执行记录；其中 `每日微信总结` 已从静态通知升级为“生成微信报告 agent 节点 -> 系统通知节点”的工作流，运行时会同步本机微信 mirror、生成 markdown 报告文件和通知摘要；自动化页还新增了 `最近结果` 面板，会从微信助手自己的 reports API 汇总最新运行摘要和报告预览，减少用户必须先跳到 Workflow 才知道结果的断层；最新又新增 `wechat_assistant_reports` 报告归档表，每日总结生成成功或失败时会写入微信助手自己的报告归档，删除自动化或底层 Workflow 记录不存在时，最近结果仍可展示已归档报告且不再提供失效跳转；最新还新增报告下载接口和“下载报告”入口，归档报告可导出 Markdown，而不只是在卡片里看截断预览；微信助手对话也已补第一版运行管理能力，用户说“查看自动化列表 / 立即运行某个提醒或每日总结 / 修改某个提醒时间 / 修改提醒内容或每日总结要求 / 诊断某个提醒状态或失败原因 / 暂停 / 开启 / 删除某个自动化”会触发真实自动化管理，并把状态回到自动化卡片与最近结果；最新又修正了多条自动化时名称不明确会误操作第一条的风险，删除 / 暂停 / 开启 / 运行 / 修改 / 诊断必须明确命中名称或当前只有一条规则，同时修正了“每日微信总结”目标名里的“每日”被误判为改时间的问题；“即时微信总结”已改成手动触发专用规则，避免创建后一分钟再自动重复执行；自动化空列表现在会显示真实可启用的内置任务卡“每日微信总结”，点击后会创建真实 Workflow 调度任务；另外跟进删除、自动化删除和微信镜像“清空重建”都已补用户确认；最新还修正了设置保存 schema 漏掉 `topicExtractor` 的问题，用户在设置里编辑“近期话题”提示词不再被保存流程静默丢弃；本轮继续修正一次性提醒的底层调度语义：编辑一次性提醒会把新的执行时间同步到底层 `next_run_at`，到点自动触发时会先关闭后续触发并清空下一次时间，减少一次性提醒在长运行期间被重复拉起；微信助手自动化编辑 UI 也已同步收口，一次性提醒现在直接显示“执行时间”日期时间控件，修改后会同步真实执行时间、展示文案和底层兼容 cron 字段，不再要求用户手动理解 cron；自动化页的最近结果也已补运行中自动刷新，用户点击“立即运行”后页面会短轮询新记录，若报告仍是运行中会继续静默刷新报告和自动化卡片直到进入终态，减少“实际已结束但 UI 停在运行中”的验收误差；同时 `每日微信总结` 已新增第一版 LLM 增强报告层，设置页新增可编辑的“每日总结报告”提示词，定时任务会把统计、待办和最近消息片段交给用户配置的文本生成服务商生成 Markdown 报告，若无服务商或生成失败则自动回退到基础统计报告并保留失败原因。
  - 成果：微信助手自动化页的 `最近结果` 已新增 `查看报告` 弹窗；列表只保留短报告预览，完整 Markdown 进入可滚动弹窗，并复用正式工作流输出同源的 Markdown 渲染能力展示标题、列表、代码块和表格等正文结构；弹窗内保留下载 Markdown 和打开执行记录入口，减少长报告撑破卡片和用户必须下载才能验收全文的问题。
  - 成果：微信助手报告查看链路继续收口；最近结果列表仍只返回短预览，但 `查看报告` 弹窗会打开时拉取下载接口里的完整 Markdown，报告归档也不再被列表预览的 3000 字截断覆盖，避免用户在弹窗或下载里只能拿到不完整日报。
  - 成果：微信助手报告预览与弹窗正文继续加固；最近结果列表里的 Markdown 预览和弹窗里的完整 Markdown 都已补换行和容器内约束，避免长文本、表格或代码把内容撑出框外；`打开执行记录` 跳转也已统一 encode schedule/run id，降低特殊字符导致的链接失效风险。
  - 成果：微信助手报告 UI 的纯格式化逻辑已独立到 `report-ui.ts`，报告链接、预览截断、下载文件名和安全名称都可单独测试，不再依赖渲染组件；这让报告相关的回归测试可以在 node 环境稳定跑通，减少 `streamdown` 之类渲染依赖把测试环境拖炸的风险。
  - 成果：微信助手对话面板已补本地历史持久化和底部跟随；用户在自动化页用微信助手查看、运行、修改、暂停、开启或删除自动化后，刷新页面仍能看到最近对话记录，消息较多时新回复会自动定位到底部，也可通过清空按钮重置历史。
  - 成果：微信助手对话里的消息搜索结果现在可一键跳到概况页并自动带上关键词，进入后会直接用“全部历史”执行同一个搜索词，不再要求用户手动重输一遍。
  - 成果：微信助手自动化编辑继续收口用户体验；定期任务不再默认暴露 cron 表达式，常用规则已改为 `每天 / 每周 / 每 N 小时` 表单，并按选择自动生成真实调度 cron 和人话时间文案；只有无法识别的旧规则才显示高级 cron 编辑。
  - 成果：微信助手自动化页 `最近结果` 的关键词搜索已补完整报告正文匹配，不再只搜索摘要、错误和文件名；接口现在保留短 `reportMarkdown` 供卡片预览，同时返回完整 `searchText` 供前端搜索，避免列表预览截断 3000 字后导致搜索能力名不副实。
  - 成果：微信助手自动化编辑保存链路已补防抖冲刷；用户刚修改时间/内容后立即点 `立即运行`，前端会先保存当前待保存改动再触发底层 Workflow，减少触发旧规则的风险；删除自动化前也会清理同条规则的待保存补丁，避免删除后又弹出保存失败或刷新回滚的误导。
  - 成果：微信助手 `跟进` 页也已补同类防抖保存收口；编辑跟进后马上标记完成/归档会先保存当前文本补丁再改状态，删除跟进前会清理同条待保存补丁，减少“刚删掉又因为延迟保存报错或刷新”的误导。
  - 成果：微信同步入口已补运行态判断；`starting` 阶段和后端已存在 `inFlight` 同步都会被 UI 当作同步中，不再在页面重载或刚启动同步时继续显示 `立即同步 / 清空重建` 这类可重复操作入口。
  - 成果：微信助手 `近期话题` 面板也已接入后端 `inFlight` 运行态；页面刷新后如果后台仍在提取私聊或群聊话题，对应面板会继续显示分析中，并阻止重复生成。
  - 成果：微信助手 `近期话题` 生成已补 AI JSON 格式异常兜底；当模型返回 malformed JSON 导致结构化解析失败时，单批分析不会再把英文 `Expected ',' or '}'...` 这类底层错误直接暴露给用户，而是使用本地消息关键词生成带“本地兜底”说明的话题结果，用户可稍后重新生成获得更准确 AI 归纳。
  - 成果：微信助手 `近期话题` 条目右侧的消息入口继续做可点击性收口；每个话题的消息条数和 `消息` 按钮现在固定为右侧竖向工具区，减少窄屏或长标题下“消息上下文”入口挤在同一行、不好点击的问题。
  - 成果：微信助手旧版页签文案继续清理；概况空状态、近期话题白名单弹窗和排除名单弹窗不再提示用户去已不存在的 `数据授权` 页签，而是指向页面上方的数据授权区域。
  - 成果：微信助手 `概况` 页的自定义报表已去掉写死 mock 数据和误导性 `AI` 文案；现在“常用表情统计 / 深夜聊天统计 / 疑似承诺事项 / 近 7 天活跃对话”都从本机微信 mirror 的真实消息流派生，缺少对应数据时显示空状态，不再展示固定示例数字；最新又把概况数据源的会话展示名和承诺文本接入统一微信名/正文清洗，互动 Top、久未联系和自定义报表可见字段不会因 session display 缺失或只剩内部 ID 而直接显示 wxid / openim / chatroom id。
  - 成果：微信助手旧版 mock 残留继续清理；未被正式页面引用的 `mock-people / mock-relations-data` 已移除，关系/跟进/自动化类型注释也从 demo/mock 口径改为正式产品域口径，降低后续误接回假数据的风险。
  - 成果：微信助手 `概况` 页自定义统计报表已新增本机持久化；用户生成的报表卡会写入 localStorage，刷新页面后仍保留最近 20 张，删除后也会同步移除。
  - 成果：微信助手 `跟进` 页的 `分析微信消息` 已真正接入微信助手自己的 AI 设置；现在会优先使用 `设置 > AI助手` 中选择的 provider、model 和“跟进提取”提示词，不再绕过设置页直接走全局默认 provider / 固定 prompt。
  - 成果：微信助手 `概况` 页的 `近期话题` 提取也已对齐同一套 AI 设置语义；当设置选择“跟随全局默认”时会使用全局默认文本服务商，显式选择 provider/model 时会使用微信助手自己的选择，不再把 `providerId: null` 误判为未配置。
  - 成果：微信助手 `跟进` 页的 `分析微信消息` 继续补齐设置生效范围；现在会在发送给 LLM 前应用设置里的排除名单、分析窗口和灵敏度，避免被排除的会话仍进入模型输入，也避免用户调整窗口天数后分析仍按固定 14 天裁剪。
  - 成果：微信助手状态接口的非 macOS 提示已去掉 `Demo` 口径，改为明确说明“当前版本先支持 macOS 本机微信消息读取，Windows 支持后续补齐”；旧 `/api/apps/builtin/wechat/analysis` 兼容分析模块也已在进入分析构建器前统一清洗 session/message 的展示名和正文，避免后续复用旧接口时把 wxid / openim / chatroom id 重新带回用户可见字段。
  - 成果：微信助手 `概况` 页已新增第一版聊天记录搜索；用户可输入关键词，选择 `全部 / 私聊 / 群聊` 和 `近 30 天 / 近 90 天 / 近 1 年 / 全部历史`，直接检索本机已同步的微信 mirror 消息并查看会话名、发送方、时间和消息片段；搜索范围同时覆盖消息正文、会话显示名和 wxid，方便用户按联系人 / 群名找历史；搜索结果已补 `查看上下文`，可打开同一会话命中时间前后的真实文本消息；该搜索不调用 AI，也不会把搜索内容发送给模型。
  - 成果：微信助手对话也已接入第一版本机消息搜索能力；用户在助手里说“查一下微信里关于合同的消息”“微信里有没有某个关键词”时，会直接查本机 mirror，返回前 5 条结果摘要，并提示到概况页继续查看更多结果和上下文；搜索回复会把会话名、发言人和正文里的内部 ID 再做一次产品化清洗，避免把“查看自动化列表 / 某个总结状态”误判为消息搜索，也避免在助手回复里直接暴露群 ID / openim id / wxid。
  - 成果：微信助手对话返回的动作已进入正式 UI；助手回复里的 `去概况页搜索 / 去跟进页 / 刷新自动化` 等 action 会渲染为可点击按钮，点击后会切换到对应页签或刷新相关列表；其中 `去跟进页` 会切到 `跟进` 并主动刷新列表，若动作来自新增或完成某条跟进，还会携带目标跟进 ID 并选中对应任务，不再只是后端返回但前端忽略。
  - 成果：微信助手底部全局对话框已按用户要求改为直接复用资料库 / 工作流编辑同源的 `BottomChatPanel + ChatView + MessageInput`；不再只是套相似外壳，也不再使用微信助手自定义聊天面板。为了避免统一 UI 后丢掉产品能力，微信助手会话已新增专属内置工具，只在该会话里挂载，当前可让模型查看微信助手同步/跟进/自动化状态、搜索本机微信消息、按可见标题解析跟进或自动化候选、查看 / 新增 / 完成 / 删除跟进、查看 / 创建 / 立即运行自动化、修改自动化时间或提醒/报告内容、诊断自动化状态、暂停 / 开启自动化、批量暂停 / 开启自动化，以及删除自动化；执行后的页面数据会在流式回复完成后触发刷新。
  - 成果：微信助手底部对话的成功 / 刷新失败反馈已拆开；后端创建、修改、运行、删除自动化成功后会先保留成功回复，如果只是前端随后刷新列表失败，不再追加“微信助手暂时不可用”误导用户，而是在操作提示区显示“操作已完成，但刷新页面状态失败，请手动刷新”。
  - 成果：微信助手自动化页已去掉重复的 `快速创建` 二级对话框；自然语言创建、搜索和管理统一收敛到底部全局 `微信助手` 对话框，右上角只保留标准 `新建` 按钮，减少用户面对两个相似输入框的困惑。
  - 成果：微信助手底部全局对话框的输入区已从自写 `Textarea + 发送按钮` 经 `PromptInput` 过渡到完整 `ChatView / MessageInput` 复用，视觉外观、回车发送、模型选择、附件、知识库按钮、流式消息、历史消息和忙碌态都与工作流 / 资料库底部输入框同源；最新还把共用底部面板的收起图标改为正式 `lucide-react` 图标。
  - 成果：微信助手底部 AI 对话框已补独立且可管理的 Agent 提示词；`设置 > AI 提示词` 里新增 `微信助手对话`，用户可直接编辑该 Agent 如何搜索微信、管理跟进和自动化，以及如何避免暴露内部 ID；微信助手底部打开时会确保专属 ChatView session 存在并刷新当前提示词，如果提示词发生变化会清掉旧 Claude SDK 续聊 id，让下一次回复按新提示词和当前工具集启动，同时继续保留微信助手会话标记，以便只在该会话挂载专属工具。
  - 成果：微信助手底部全局对话框继续补齐跟进任务管理第一段；用户现在可以用自然语言新增跟进任务、查看待处理跟进列表、把明确命名的跟进任务标记完成，并可一键跳转到 `跟进` 页继续补充联系人、提醒时间和下一步，不再只有自动化能通过对话管理。
  - 成果：微信助手 `跟进` 详情页继续补齐可验收体验；状态下拉现在完整支持 `待处理 / 进行中 / 已完成 / 已归档`，其中 `进行中` 已打通到底层待办存储和迁移，不再刷新后退回待处理；`挂个提醒` 也从固定“明天 09:00”改为弹窗确认提醒时间和提醒内容，创建成功后会在详情页显示反馈，并可直接跳到 `自动化` 页查看对应真实提醒规则。
  - 成果：微信助手 `跟进` 新建多联系人断层已收口；新建跟进弹窗不再只展示前 16 个联系人，而是支持按联系人、群名搜索并显示已选联系人，底层仍保留按 wxid 兜底匹配能力但不在用户可见占位文案里暴露该术语；后端待办存储新增完整涉及联系人数组，手动新建和后续编辑都能保存多名联系人，旧单联系人数据仍会按 `source_wxid` 兜底显示，不会迁移后丢失。跟进页联系人来源也已从“概况里的活跃会话”扩展为“概况活跃会话 + 微信通讯录懒加载 + 已有跟进 wxid 兜底”，低频联系人或不在概况窗口内的联系人也能搜索选择，并能在详情页显示；通讯录懒加载也已修正缓存语义，只有真实 ready 后才缓存，避免用户完成授权/恢复密钥后联系人选择器仍停留在旧空结果；最新又把 contacts API、近期话题白名单弹窗和排除名单弹窗的 fallback 展示名接入同一套微信名清洗逻辑，联系人/群展示不会因为 display 缺失或只剩内部 ID 而直接显示 wxid / openim / chatroom id。
  - 成果：微信助手 `AI 推荐` 卡片的来源展示已从一句“来源”文本改成可扫描的字段式信息；每条推荐现在直接显示 `会话 / 发言人 / 相关消息`，并继续复用统一微信名和正文清洗逻辑，优先显示通讯录或 mirror 里的可见群名 / 联系人名，找不到时使用“微信群聊 / 微信联系人 / 群成员 / 对方”这类产品占位，不把 `wxid / openim / chatroom / 长数字群 ID` 露给用户判断。
  - 成果：微信助手自动化手动触发体验继续收口；当某条自动化正在触发时，其它自动化的 `立即运行` 会显示 `等待中` 并禁用，重复点击会给出“已有自动化正在触发”的明确反馈，不再静默无反应；触发失败时也会清掉旧成功提示，减少用户误判。
  - 成果：微信助手自动化列表顶部已补“正在触发”状态条；当某条任务还在触发中时，页面会明确显示当前自动化名称和等待状态，而不只是在单个按钮上变灰。
  - 成果：微信助手 `设置 > 跟进与提醒 > 默认提醒时段` 已接入跟进详情里的 `挂个提醒`；如果跟进本身没有未来截止时间，提醒弹窗会按设置里的小时生成下一次提醒时间，不再固定写死为明天 09:00。
  - 成果：微信助手设置页保存失败体验已补收口；设置保存失败后页面会保留失败 payload，并显示 `重试保存` 按钮，用户不必再随便改一次配置才能重新触发保存。
  - 成果：微信助手 AI 服务商选择已补 `local_auth` 防护；设置页的文本生成服务商候选会过滤掉只能本地鉴权、不能执行轻量文本生成的服务商，若历史设置里仍保存了不可用服务商，UI 会显示“当前选择不可用”并提供“改为跟随全局默认”入口；`跟进 / AI 推荐`、`近期话题` 和 `每日微信总结` 运行前也会统一解析可用 provider/model。若全局默认或显式配置落到不可用本地服务商，跟进与话题分析会进入明确的无可用服务商状态，日报会回退到基础统计报告并在报告里保留 AI 跳过原因，不再把 “服务商本地当前使用 local_auth，暂不支持轻量文本生成” 暴露成生成链路崩溃。
  - 成果：微信助手跟进与自动化编辑的保存失败体验已补收口；保存失败后页面会保留当前本地编辑并显示 `重试保存`，后续刷新也会尽量保留仍待保存的本地改动；如果同一条记录保存失败后用户继续编辑其它字段，下一次保存会合并旧失败补丁，不再因后续保存成功而把前一个未保存字段回滚。
  - 成果：微信助手 `跟进 / AI 推荐` 的前端接口响应校验继续加固；跟进列表加载、采纳/忽略候选、手动创建、编辑保存现在都会同时检查 HTTP 状态和返回体里是否真的有 `todos / todo`，后端异常或返回不完整 JSON 时会进入明确错误状态，不再把空对象当成保存成功写进 UI。
  - 成果：微信助手 `概况` 页的 `重新分析概况` 已从同步流程中拆出；现在只会重新刷新概况快照，不再误触发整套微信同步，避免按钮语义和实际行为不一致。
  - 成果：微信助手 `概况 / 近期话题` 的前端加载失败处理继续加固；两个 hook 现在都会先检查接口状态和关键字段，接口异常或返回不完整 JSON 时会保留当前状态并显示明确中文错误，不再把 `undefined` 当成有效概况或话题数据塞进页面。
  - 成果：微信助手可点击主链的前端响应防护继续加固；底部全局 `微信助手` 对话、自动化列表 / 创建 / 保存 / 触发、同步状态 / 启动同步、联系人懒加载、设置保存、最近报告列表、聊天记录搜索 / 上下文、近期话题消息上下文、跟进页 `AI 推荐` 分析都会处理非 JSON、HTTP 失败和关键字段缺失，不再把空对象或缺字段响应当成成功写入 UI。同步进度也会在单个 DB 完成事件后保留已写入/已读取计数，报告列表里的 Workflow 跳转文案统一为 `打开执行记录`。
  - 成果：微信助手状态加载也已补结构校验；如果状态接口返回 200 但缺少 `export / im` 等关键字段，页面会显示“状态加载失败”，不会继续把不完整对象传给授权横幅导致预览崩溃。
  - 成果：微信助手同步条失败态继续收口；如果同步状态接口本身加载失败，概况页不再直接隐藏同步入口，而是显示 `微信同步状态加载失败`、具体原因和 `重试` 按钮。
  - 成果：微信助手同步条在已有旧状态但刷新失败时也会显式提示；页面会说明 `微信同步状态刷新失败`、保留上次同步时间，并提供 `重试`，不再假装当前状态一定是最新。
  - 成果：微信助手首次自动同步触发条件已收紧；只要本机曾经成功完成过同步，或本地 mirror 已有消息，就不会再因为 `cursorTs` 没推进而在每次进入微信助手时自动重跑同步。
  - 成果：微信 IM 运行时监听器已补空轮询降噪和退避；当 `getupdates` 连续快速返回空消息时，不再每轮向 Electron stdout 打印 `iter#... POST getupdates / ret=undefined msgs=0`；普通 monitor 运行日志现在默认只写入 `~/.lumos/wechat-debug.log`，不再进入终端 stdout，只有显式设置 `LUMOS_WECHAT_MONITOR_CONSOLE=1` 才会打印到终端；逐轮调试日志仍需额外设置 `LUMOS_WECHAT_MONITOR_DEBUG=1`。最新还给 `~/.lumos/wechat-debug.log` 增加默认约 5MB 的轮转上限，保留上一份 `.1`，避免长期运行把微信调试日志无限写大。
  - 成果：微信助手 `近期话题` 每日归档第一版已落代码：业务日按本地凌晨 4 点切分，调度 tick 会尝试为上一完整业务日按 `personal / group` 自动生成并写入 `topic_daily_summaries / topic_daily_sources`；概况页话题接口已支持 `from / to` 日期范围查询，前端已新增日期范围控件，并把话题按微信联系人 / 群聊来源分组展示；每个话题已新增 `消息` 入口，可打开同一来源、同一日期范围内的相关原始消息上下文弹窗。若当时未配置服务商或白名单导致自动跳过，系统不会把 skipped 永久当成完成，会按冷却时间允许后续重试。
  - 成果：微信助手 `近期话题` 每日归档失败态继续收口；如果上一完整业务日的自动分析因临时异常进入 `failed`，现在也会像 `skipped` 一样只在冷却窗口内阻止重复触发，冷却后允许调度自动重试，减少 04:00 单次网络或服务商故障导致当天话题永久缺失。
  - 成果：微信助手 `近期话题` 每日归档运行态也已补陈旧锁处理；如果应用在某天话题分析 `running` 中途退出或崩溃，旧的 running 日归档不会永久阻止后续自动补生成，超过运行态保护窗口后会允许调度重新分析。
  - 成果：微信助手 `近期话题` 失败 / 中断状态在 UI 上也已产品化；当所选日期范围只有失败归档时，卡片会显示“生成失败”和可重试入口；当旧归档仍是 running 但当前进程没有真实运行任务时，卡片会提示“上次分析未完成”，引导用户重新生成所选结束日，而不是泛化显示“尚未生成”。
  - 成果：微信助手 `近期话题` 日期范围的部分失败也会显示提示；如果范围内有些日期已有话题结果、另一些日期分析失败，结果列表顶部会提示存在部分失败和失败原因，不再让用户误以为整个范围都已完整分析。
  - 成果：微信助手 `近期话题` 日期范围输入已补前端归一化；如果用户把开始日期选到结束日期之后，前端会直接交换成正确范围，避免后端按正确范围返回结果但输入框仍显示倒置日期。
  - 成果：微信助手 `近期话题` 自动归档运行态已补前端轮询；当后端在 04:00 自动归档或页面刷新后仍在运行话题提取时，概况页会定时刷新状态，避免卡片长时间停在“分析中”而不自动切到结果或失败态。
  - 成果：微信助手 `近期话题 / 聊天记录搜索` 的来源展示继续收口；话题生成 prompt、话题来源卡片、话题消息上下文弹窗、聊天记录搜索结果和搜索上下文弹窗都会对会话名、群发言人名和消息正文做内部 ID 清理，旧数据里形如 `openim / chatroom / 长数字群 ID` 的前缀不应再直接暴露到这些可见区域；本轮还把 `mirror-store` 读层的搜索结果、消息上下文、话题来源归档都接入会话名 / 群成员名清洗，减少旧归档或绕过组件清洗时重新泄露内部 ID 的风险；话题手动生成按钮也改为“生成所选结束日”，明确日期范围用于查看，手动生成会处理当前范围最后一天的日归档。
  - 成果：微信助手 `近期话题` 白名单隐私语义已补接口级收口；即使历史日归档里还留着某个联系人 / 群的旧话题，当前设置里已移出白名单或加入排除名单的来源不会再从近期话题列表返回，话题 `消息` 上下文接口也会直接按 404 隐藏，不会读取并返回该会话附近原始消息。
  - 成果：微信助手 `近期话题` 空状态继续收口；前端白名单计数改为按“白名单扣除排除名单”后的有效数量展示，避免某个会话既在白名单又在排除名单时仍显示“已选择”；手动生成后如果后端返回 `whitelist_empty / no_provider / no_model / no_messages / in_progress`，卡片会显示对应中文原因和正确操作入口，而不是继续停留在泛化“尚未生成”。
  - 成果：微信助手 `近期话题` 生成链路继续收口；无论手动生成还是每日自动归档，进入 AI 话题提取前都会先尝试同步本机微信 mirror，避免 04:00 日归档或用户手动补生成时分析旧数据。若同步失败则进入明确失败态；若未授权、未恢复密钥或当前平台不支持导致同步不可用，前端会显示“微信消息尚未同步成功”的可理解原因，不再继续拿过期镜像调用话题 AI。
  - 成果：微信助手 `近期话题 / 每日微信总结` 的可见内容清理继续收紧；AI 返回的话题标题、话题摘要、参与人、历史归档读取结果、日报 AI 增强 Markdown、确定性日报待办来源、日报通知和送入日报 LLM 的最近消息片段都会再做一次内部 ID 清洗，减少旧数据或模型输出把 `openim / chatroom / 长数字群 ID` 带进话题卡片、报告弹窗、下载 Markdown 或通知摘要的概率。
  - 成果：微信助手 `设置 > AI 提示词 > 跟进识别` 的默认提示词已从旧 schema 收敛到当前真实后端 schema；默认提示词现在要求模型输出 `events / todos`，并明确 `contactWxid` 只能填写输入会话标题里的 `source` 临时编号，禁止输出 wxid / openim / chatroom / 长数字群 ID，减少用户重置默认提示词后又回到旧字段和内部 ID 语义的风险。
  - 成果：微信助手 `AI 推荐` 来源可读性已补第一轮源头收口；发送给 LLM 的跟进提取 prompt 不再暴露真实 `wxid / openim / chatroom / 长数字群 ID`，而是使用临时 `chat_1` 这类 source 编号，落库前再按 source 映射回真实会话；同时对模型输出、来源原文和会话名做统一内部 ID 清洗，减少“整理节前遗留问题清单发到 45434442516 客户群”“25984985930267888@openim: ...”这类内部标识进入卡片标题、原因和原文预览的概率。
  - 成果：微信助手 `AI 推荐` 内部 ID 清洗继续收紧；以前部分 UI / 入库映射采用“清理后为空就回退原文”的写法，遇到整段都是 `openim/wxid/长数字 ID` 时仍可能把技术 ID 显示回来；现在推荐标题、原因、原文、搜索上下文、话题上下文和发送给话题 LLM 的消息文本都会在清理为空时使用安全占位文案，不再回退到原始内部 ID。
  - 成果：微信助手群消息发言人链路已补第一版；macOS 微信导出 `sync_stream` 会从每个 message DB 的 `Name2Id.rowid` 反查 `real_sender_id` 对应的 `user_name`，并用通讯录备注 / 昵称解析出 `sender_display`；本地 mirror 的 `messages` 表新增 `sender_wxid / sender_display`，新增同步写入该字段后，AI 跟进提取 prompt、`AI 推荐` 卡片、跟进详情原文引用、聊天记录搜索结果 / 上下文弹窗，以及近期话题的消息上下文弹窗都会优先展示群内真实发言人名。已有旧 mirror 数据不会凭空补出发言人，用户需要重新同步新消息，或在可接受重建成本时执行“清空重建”后再分析历史消息。
  - 成果：微信助手 `AI 推荐` 来源可读性继续收口；当底层 mirror 已有 `sender_display` 时，新生成的候选会显示“某群 / 张三说的”这类来源，而不是只显示“群成员说的”；若微信库或通讯录无法解析发言人，仍会降级为“群成员说的”，不暴露内部 ID。
  - 成果：微信助手 `AI 推荐` 采纳后的跟进详情也已接入同一套来源清理逻辑；候选转为正式跟进后，详情里的标题、概览、下一步、对话引用发送方和原文不再直接沿用 `sourceDisplay / sourceText` 原始值，减少卡片阶段已清理、进入详情后又暴露 `openim / wxid / 长数字 ID` 的断层。
  - 成果：微信助手 AI 待办建议入库层也已补内部 ID 清洗；模型输出或 Python 读取层传来的 `sourceText / sourceDisplay / sourceSenderDisplay / byWhenText / text` 会在写入 `wechat_assistant_todos` 前清理 `openim / chatroom / wxid / 长数字 ID`，减少后续报告、导出或旧接口绕过前端清洗时重新泄露内部标识的风险。
  - 成果：微信助手待办读取层也已补旧数据兜底清洗；`todos` API 现在在读取 `wechat_assistant_todos` 时，会对非手动待办的 `text / sourceText / sourceDisplay / sourceSenderDisplay / byWhenText / summary / nextStep` 再做一次产品化清洗，确保历史 AI 建议即使早期写入过内部 ID，也不会在 UI 或导出里重新暴露。
  - 成果：微信助手 `AI 推荐` 卡片展示进一步收口；当候选能在联系人/群列表里命中真实名称时，来源名现在会优先用真实联系人名，再退回清洗后的占位名，避免“微信联系人 / 微信群聊”盖掉已经可识别的群名或联系人名。
  - 成果：微信助手每日总结 `查看报告` 弹窗继续加固；完整报告正文仍在弹窗内独立滚动，底部按钮固定在正文滚动区外；`下载 Markdown` 已改为前端 fetch 后触发本地文件下载，不再依赖 Electron 对 `<a download>` 的支持；打开执行记录时会先关闭报告弹窗，降低正文层遮挡或焦点残留导致按钮看似不可点的风险。报告列表删除入口和归档删除接口已保留。
  - 成果：微信助手每日总结报告删除入口继续收口；`查看报告` 详情弹窗底部也新增 `删除报告`，用户不必先关闭弹窗回列表找删除按钮；删除仍复用同一条归档软删除接口，并已补 live 报告 tombstone 兜底，避免归档写入失败时按钮看起来能点但实际删不掉；`下载 Markdown` 现在也会在归档行缺失时回查 live 报告列表，避免刚运行出的报告因归档同步失败而下载 404；删除成功后会关闭详情弹窗并刷新最近结果列表。
  - 成果：微信助手 `最近结果` 后端聚合继续加固；当某条底层 Workflow 执行记录详情损坏或临时读取失败时，reports API 会跳过该 live 记录并继续返回其它 live / archived 报告，不再因为一条坏执行记录导致整个自动化报告列表加载失败。
  - 成果：微信助手自动化删除语义继续按任务生命周期规则收紧；删除自动化或删除 Workflow 调度任务前，现在会检查取消运行中执行的结果，若还有执行未确认停止，则保留自动化 / 调度记录并返回明确错误，不再让 UI 先消失而底层 Workflow 可能继续运行；批量删除也会返回未确认停止数量。
  - 待完成：上述 `AI 推荐`、报告弹窗、报告删除和全局底部微信助手对话框都仍需用户在真实 UI 里验收；尤其需要用真实微信群聊数据确认推荐卡片不会再暴露内部 ID，并在重新同步或清空重建后确认群消息能显示真实发言人；同时还要确认报告弹窗滚动、下载和执行记录跳转在 Electron 预览窗口里可点击。
  - 待完成：`近期话题` 每日归档还没有经过用户真实 UI 验收和长时间自动调度验收；还需要确认 04:00 后自动归档上一天、日期范围切换、来源分组、消息上下文弹窗在真实微信数据下都可用，并继续补“跳到完整会话附近”的更深入口。
  - 待完成：当前真实 AI 接入仍不是完整 Claude SDK AppBuilder agent runtime；虽然服务端工具循环、基础自检修复、第一版模板起点和可见工具轨迹已经落地，但 M3 workflow bridge 与更完整的应用发布治理仍未完成；按用户最新决策，下一步先清理现有应用平台测试数据，把开发重点切到内置应用和稳定运行时，AI 自由生成应用暂不作为当前验收主线。

## 当前状态进度

- `主 Agent / Lumos 管家`
  - 文档完整度：`部分完成`
  - 主链状态：`已打通`
  - 当前进展：已确认主 Agent 需要作为 Lumos 的用户侧“管家”，优先获得全局只读查看能力，而不是先做复杂审批、回滚或治理。本轮已新增内置 `lumos-butler` 只读工具，并只注入主 Agent 会话；当前可读取服务商、MCP、Skill、能力包、Workflow、会话、知识库、DeepSearch、IM、浏览器和运行时资源的全局状态摘要，生成面向小白用户的问题诊断；也可搜索历史会话 / 消息 / 任务 / Workflow / DeepSearch / 能力记录，并查看任意会话的最近消息和关联任务。最新只读 Workflow 查询已补到 `list_workflow_tasks / get_workflow_task / list_workflow_runs / get_workflow_run / list_active_workflow_agents`，可以看到任务管理任务、计划任务、执行记录、步骤摘要和当前进程内运行中 Agent，并给出可点击的 Workflow 详情入口；运行中 Agent 的跳转会优先反查真实执行记录或任务详情，避免把底层 engine run id 误当成页面 schedule id。当前保留 `/main-agent/butler` 只读总览页，可查看全局健康摘要、关键数量、问题提示、运行资源、最近记录、浏览器概览和可跳转入口；主 Agent 聊天页顶部“Lumos 管家状态”条已按最新要求移除。工具输出会对服务商密钥等敏感字段做脱敏，主 Agent 系统提示也明确当前阶段不能声称已执行删除、覆盖、支付、发 IM、敏感导出或批量治理动作。
  - UI 可验收范围：用户进入 `主 Agent` 聊天页不再看到顶部“Lumos 管家状态”面板；`/main-agent/butler` 总览页当前可查看问题诊断、运行资源、最近会话、最近 Workflow、DeepSearch、能力、浏览器状态，也可按全部 / 会话 / 消息 / 任务 / Workflow / DeepSearch / 能力搜索历史记录并跳转到对应页面。用户也可以继续在对话里直接问“帮我看看 Lumos 当前哪里有问题”“有哪些工作流任务”“哪个工作流还在跑”“刚才那个任务为什么失败”“哪个 Agent 卡住了”“找一下上次那个 PDF / 任务 / 会话”“某个能力或 MCP 是否可用”“打开某个历史会话的摘要”等，主 Agent 应先调用管家工具，再用页面、按钮、状态和下一步动作解释结果。
  - 当前缺口：还未提供低风险一键修复动作；删除 / 覆盖 / 发 IM / 支付 / 敏感导出 / 批量操作 / 审批 / 回滚 / 治理仍未进入自动执行范围；当前 Workflow 查询仍是只读诊断，不包含聊天侧停止 / 重跑 / 删除等控制动作；全局状态摘要还可以继续补更细的资源校验、运行态占用、计费健康和跨平台打包诊断，因此不能按“完整实现”标准验收。
- `浏览器 Provider / 指纹浏览器接入`
  - 文档完整度：`基本完成`
  - 主链状态：`已打通`
  - 当前进展：已确认需求核心是“浏览器身份 / profile 上下文”而不是单纯增加一个 CDP 调用工具；当前已建立默认内置浏览器上下文和全链路 `browserContextId` 承载位，并补上 `external-cdp:default` Provider、`adspower:<profileId>` Provider、本地 provider 配置表、运行时配置文件同步、Electron registry 热加载、chat 会话级 `browser_context_id` 持久化和请求注入、工作流任务级 `browser_context_id` 持久化、服务端校验、执行记录快照与执行透传、第一版按已配置 profile 名 / 别名精确匹配自动切换、浏览器请求禁用非浏览器工具 / DeepSearch 兜底、强制新 SDK 会话、服务端解析 context 优先于旧 embedded 请求头、`chrome_devtools` SDK 工具名映射和轻量 stdio MCP 实现，以及站点页缓存按 browser context 隔离、配置生命周期引用防护、AdsPower Profile 发现 / 单个绑定 / 批量导入第一版、context 唯一性防护、设置页引用提示第一版、非内置浏览器运行态占用锁和短等待第一版、设置页“释放占用”第一版、聊天 / 工作流执行详情占用冲突提示第一版、设置页多浏览器 / 多 Profile 搜索筛选和 AdsPower 分组筛选第一版，以及 AdsPower 多 Profile 分组展示 / 手动同步预览第一版。当前用户已确认聊天和工作流都可以使用所选浏览器，浏览器 Provider 分支主链可以进入合并收口。
  - UI 可验收范围：用户现在可以在 `设置 > 浏览器` 或 `/settings/browsers` 看到内置浏览器、添加 / 编辑 / 删除 AdsPower 或通用 CDP 接入、维护 Profile 别名、点击“发现 Profile”从 AdsPower Local API 拉取 profile 并单个绑定或批量导入、点击“同步 AdsPower”预览本机 Profile 列表会新增 / 更新 / 不变的配置并确认应用、在浏览器列表和发现 Profile 列表里搜索/筛选（包括 AdsPower 分组筛选）、按 AdsPower 分组查看第三方浏览器卡片、点击“测试”查看连接结果，也能看到某个浏览器是否被聊天会话或工作流任务引用；当第三方浏览器正在被运行态租约占用时，设置页会显示“AI 操作中”，并提供“释放占用”按钮；也可以在聊天会话标题旁看到当前浏览器上下文并手动切换，或在消息里明确说“用 浏览器1 / 别名”触发服务端精确匹配自动切换；工作流新建 / 编辑任务弹窗现在也有“浏览器”下拉，任务列表、任务详情、执行历史和执行详情会显示本任务 / 本次执行实际使用的浏览器名称；如果聊天或工作流浏览器工具遇到占用冲突，系统会短等待并显示可操作提示，用户可释放占用或切回内置浏览器。
  - 当前缺口：完整 FIFO 等待队列 / 更完整任务内接手确认、AdsPower 自动周期同步 / 大账号分页实机验收、External CDP / AdsPower 更系统的实机验收、跨平台打包验证仍未完成，因此不能按“完整实现”标准宣称浏览器 Provider 全部完成。
- `03 调度层`
  - 文档完整度：`基本完成`
  - 主链状态：`已打通`
  - 当前进展：已落地“模型分析优先”的规划器，支持 simple / workflow 决策、受限多步 DSL 生成、受限并行浏览器计划生成，并把调度决策、重试诊断正式落库；现在除测试页外，也已进入正式 `Workflow` 页面；近期已补否定语义修正，减少 `不需要通知` 这类上下文导致的误判；正式页还新增了只读流程图，复杂依赖与并行路径可直接在产品界面查看；同时已修正中文标点场景下的多 URL 提取，三路及以上并行浏览器规划不再误拆成错误地址；现已新增“前置分析 + 并行浏览器 + 汇总结论”的混合复杂工作流启发式规划，并已通过一轮真实 UI 验收；最新已把调度代理角色、规划来源、模型、超时/重试、任务内角色分配，以及调度受理状态、触发条件、规划产物校验、原始规划步骤和回退记录收进正式任务详情页；另外主 Agent 聊天侧也已补上复杂请求到 Task Management 的强制下发与来源回写，并把会话任务标签放进聊天界面侧边，任务可按来源消息稳定回查和跳转详情；针对主 Agent 新建任务，启发式规划现已补实现 / 搜索 / 报告 / 导出诉求拆解，不再默认压成通用两步代理流；同时已补上“会话未显式选模型时优先回退到默认 provider/model 再尝试 LLM 规划”的入口，以及“调研 + 安全问题/方案 + 导出”类任务优先走“搜索取证 -> 汇总 -> 导出”流的启发式规则；按用户最新要求，调度正式主链现已改为“必须先经过 LLM 规划”，不再允许在 LLM 不可用、超时或 workflow 生成/校验/提交失败时静默回退到 heuristic 结果或 simple execution，失败会直接暴露并落库到任务错误与调度诊断中；另外已把 Anthropic 结构化规划切到更兼容网关的 Claude SDK 结构化输出路径，并补上 session 级 `provider / model / workingDirectory` 透传，减少“主对话能规划但 workflow planner 命中别的 provider API”的断链；同时继续保留 provider 状态码/响应体摘录诊断，减少仅显示 `Invalid JSON response` 的黑盒失败；最新对 Claude SDK 的超时异常也已做语义归一，并把默认 planner 超时放宽到 90 秒，减少把真实超时误显示成 `Claude Code process aborted by user`；此外，当 Claude SDK 未返回 `structured_output` 但正文里给出了纯 JSON 或单个 JSON code fence 时，规划器现已会先做严格 JSON 解析并再走 schema 校验，不再把这类结构化结果直接当成失败；而对于研究类任务里常见的 `detectedUrl: null` / `detectedUrls: []` 空值输出，schema 也已会自动归一化为缺省字段，不再把整个规划直接判错；现在 planner 还会把 step 级输入合同直接编码进结构化 schema 与 prompt 示例里，并在 DSL 校验失败后把具体错误回灌给下一轮重试，减少研究类任务连续生成 `browser.query` / `browser.prompt` 这类引擎不支持的节点输入；同时对“60 秒长报告综合”“read-only researcher 被要求写 temp 文件”“md-converter 依赖硬编码 temp 路径”这类语义错误，planner 也会先拒绝并要求 LLM 重新产出更符合当前运行时边界的 workflow；最新还已把 Claude SDK 与 AI SDK 共用的 provider-model 解析收敛到统一入口，正式调度诊断会同时记录请求模型与实际解析模型，减少“聊天能用、调度或工作流代理因模型别名/目录不一致而单独失败”的分叉黑盒。
  - 当前缺口：真实调度智能仍需继续增强；当前仍以受限结构化规划为主，且主产品中的更多上下游页面还未全部显示调度细节；聊天侧任务标签后的更完整产品交互仍可能继续收口；虽然现在已能把“未走模型规划”的原因写进调度诊断，并取消静默回退，但节点级更细的原始规划日志还未全部产品化
- `04 流程编译层`
  - 文档完整度：`基本完成`
  - 主链状态：`已打通`
  - 当前进展：已修正 `steps.<stepId>.output.*` 的编译期引用解析错误，避免多步 agent workflow 中下游步骤读取到空值；同时当 DSL 未显式声明 `policy.timeoutMs` 时，编译产物现在会为 `agent / notification / capability / wait` 生成默认超时并写入 manifest，且编译后的 workflow module 也会显式解构 `notificationStep / capabilityStep / waitStep`，不再只解构 `agentStep`
  - 当前缺口：仍有最终形态与运行时覆盖范围上的收口工作
- `05 流程执行层`
  - 文档完整度：`基本完成`
  - 主链状态：`已打通`
  - 当前进展：正式工作流页已开始展示真实执行输出和实际执行步骤，不再只展示 DSL 规划视角；同时已补 OpenWorkflow sqlite backend 的 Next 外部包配置以降低开发环境兼容风险；workflow agent step 现已默认收紧为文本结果交付，避免因声明不存在的 artifact 而导致执行伪失败；文本型 stage 还具备结构化输出失败后的纯文本兜底，降低真实执行时因 JSON schema 收敛失败造成的主链中断；任务完成后写回主 Agent 对话的结果消息也已改为稳定直写，可保留浏览器截图的真实绝对路径并在聊天区直接预览；并行浏览器分支现已为每个分支创建独立页面并把 pageId 显式传递到后续截图步骤，减少复杂工作流中的串页风险；workflow agent step 现已支持受控 context 依赖输入，汇总代理可以读取并行分支结果做真实汇总；当汇总代理与最终通知正文相同，任务完成系统通知也已收敛为简短提示，避免在对话里第三次重复整份长报告；正式页现在还能展示 workflow 投影返回的真实运行态，包括运行中/跳过步骤、失败原因和关键步骤结果；浏览器步骤还已支持截图直出预览和产物打开入口；混合复杂工作流执行主链已完成一轮真实 UI 验收；最新 simple execution 与 workflow agent step 已补运行时超时透传，同时浏览器搜索步骤还能把页面摘录传给后续汇总代理使用；此外，编译产物与执行提交层也已补上任务级 runtime 元数据注入，workflow step 现在可稳定拿到来源 task/session 的 `taskId / sessionId / requestedModel / workingDirectory`；browser bridge 代码模式运行时还已补上 `waitFor` 传输超时透传、短等待自动抬底与同实例 `pageId` 粘连，减少登录页/收藏页这类慢页面场景下被 `10000 / 15000 / 30000ms` 的客户端等待或多标签页错页共同放大失败概率；同时 `/v1/pages/navigate`、新建页、快照、截图这些桥接请求也已补接口级更长默认传输超时；workflow `code-only` 浏览器步骤现在还会在失败时自动保存页面快照、失败截图与调试日志，并把这些调试产物通过正式 `Workflow` 详情页暴露出来，减少浏览器代码节点的黑盒排障；最新这条 `code-only` 浏览器链路也已默认改成后台执行，运行中的页面操作不再主动切前台浏览器标签页，只在手动调试入口保留前台行为；workflow outer timeout 也已改为按 manifest step timeout 求和并追加缓冲，`notification / capability / wait` step 的真实 runtime binding 也已补齐；另外 `chrome-devtools` MCP 在多标签页场景下已改为要求显式 `pageId`，且 `list_pages` 结果会附带活动页和相似页签警告，纯 Agent 浏览器路径的串页风险也开始收口；最新正式任务关闭 / 删除 / 执行记录停止已能向底层 workflow cancel 收口，并会同步执行历史和投影状态为已取消，避免删除 UI 记录后底层执行继续残留；本轮还补上执行记录节点级重跑第一版，用户可从失败节点或选中节点新建重跑执行记录，并复用原执行里目标节点上游已完成输出。
  - UI 可验收范围：用户进入 `Workflow > 任务详情 > 执行记录`，失败记录顶部会出现“从失败节点重跑”；在“工作流结构”图里点选某个实际执行节点后，顶部会出现“从此节点重跑”。点击后会跳转到新执行记录，旧执行记录保留不变；新记录会从所选节点及其下游继续跑，上游已完成节点优先复用旧结果。
  - 当前缺口：浏览器与通知能力仍有工程化收尾项，尚未按“完整实现”关闭；节点级重跑目前是第一版主链，流程控制节点暂不支持直接作为重跑起点，复杂分支的差异预览、重跑前参数修改、复用/重跑节点的 UI 明示和更多真实失败样本验收仍需继续完成；工作流引擎在真实 UI 开发环境中的重新验证仍需继续完成
- `06 执行代理层`
  - 文档完整度：`部分完成`
  - 主链状态：`已打通`
  - 当前进展：底层 agent abort 已打通，simple execution 与 workflow cancel 都会向活动中的 agent 执行传播中断信号；正式工作流角色配置 UI 已接入 Scheduling / Workflow SubAgent 的真实配置源，且现在除团队设置页外，已新增独立的 `Workflow Roles` 正式入口，并按“执行角色 / 规划角色”分组，减少与旧团队预设混淆；正式 `Workflow` 页面也已能显示任务计划引用到的角色快照、实际输出、实际执行步骤，以及运行态详情（当前动作、运行中步骤、已跳过步骤、失败或取消原因）；最新又补进了“当前运行角色”“任务内角色分配”以及会话/资源视图，用户现在可直接在正式任务详情看到代理会话、任务/规划/执行记忆槽、隔离工作目录、输出目录、请求模型、耗时与 Token/API 调用；正式 `Workflow Roles` 页面也已新增活跃代理会话面板，可直接查看当前会话生命周期状态并发起单代理中断；另外 workflow subagent 与 StageWorker 已改为优先继承任务 session 的 provider/model/workspace，不再默认回落到全局 active provider 或 `process.cwd()`；最新执行代理链也已补上统一的 provider-aware 模型解析与失败诊断，`requestedModel` 和实际 `resolvedModel` 会随运行时收口，减少 Claude 风格别名与 gateway 实际模型 ID 不一致时的黑盒失败；正式执行记录页现已提供“停止执行”，关闭或删除任务也会先中断运行中的 workflow / agent 链路，并对已丢失 run history 但仍 running 的执行投影做兜底取消
  - 当前缺口：完整代理生命周期与更强的长期资源治理仍未全部落地；虽然正式页面已能验收活跃会话、核心会话隔离、资源边界与单代理中断，但更长期的会话续跑、自动回收、限额治理还未全部产品化可验
- `07 动态能力扩展`
  - 文档完整度：`基本完成`
  - 主链状态：`已打通`
  - 当前进展：已新增独立架构文档，明确“动态新增系统能力”不并入 `03 ~ 06`，而是作为横切能力单独定义；最新已按用户要求把产品目标进一步收敛为“尽量复用现有聊天式新增页，不引入复杂草稿流，AI 先确认需求，再直接生成两类待发布能力：`代码节点` 与 `Prompt 节点`，最后由用户发布”；同时文档已进一步明确 Phase 1 的最小改 UI 实施方案：保留当前能力列表页、聊天式新增页和详情页，只调整行为为“对话确认 -> 生成待发布能力 -> 发布 -> 正式可用”；当前产品侧已实现这条第一段主链：`Prompt 节点` 在任务明确提到能力 ID / 名称时，会进入 workflow agent step 的 `tools`；`代码节点` 在任务明确提到能力 ID / 名称并提供结构化 JSON 参数时，会进入真实 `capability` 步骤执行；历史遗留的本地能力文件也已开始进入当前发现范围；正式 `Workflow` 详情页也已开始展示“系统能力节点”和对应能力 ID；此外，报告/正文导出 PDF 场景现在已能在检测到可用格式转换能力时自动追加能力步骤，不再一律停在“需求已记录”的占位结果；最新又已补上正文类 workflow agent step 的纯文本交付模式、Claude SDK `result.result` 文本读取，以及 `md-converter` 在缺少 `pdflatex` 时回退到本机 `weasyprint` 生成 PDF；“给我一份 Claude 使用技巧报告，并导出 PDF” 这条真实任务现已完成一轮端到端验收并产出实际 PDF 文件；本轮还针对 `能力生成器` 的安装使用体验补了第一轮 P0 收口：同名 Skill / MCP 自动覆盖更新，分享包导入默认覆盖，导出 / 导入 / 生成安装计划会使用 `[DATA_DIR] / [PYTHON_PATH] / [RUNTIME_PATH] / ${USER_HOME}` 等可迁移占位符，MCP 运行时会解析这些占位符，生成 Python MCP 模板与内置 JS MCP 会按 `inputSchema` 对字符串化参数做基础类型兜底，生成器安装 stdio MCP 后会做启动自检并在 UI 显示“已安装 / 已更新 / 自检失败”等可理解状态；正式 `能力 > MCP 服务器` 页也已新增“检测服务器 / 检测全部”手动健康检查入口，手动保存或同名覆盖后会自动执行基础启动检测，并能把失败原因直接显示在 MCP 卡片上；检测结果已持久化，刷新页面后仍保留上次检测状态、检测时间、失败原因和发现的工具数量，生成器安装后的自检结果也会同步进入 MCP 管理页；最新 MCP 配置已支持运行方式和运行时声明，用户能在表单里选择“按需启动 / 长连接声明”和 `auto / Node / Python / Bun / 自定义`，列表页也会直接展示对应徽标，导入导出与能力生成器计划会保留这些声明；远程 HTTP / SSE MCP 检测已升级为第一版协议级检查，会执行初始化与工具列表读取，减少“URL 能访问但不是可用 MCP”的假成功。
  - 当前缺口：`07` 仍未完整打通；当前可验收的是 UI 里的“对话确认 / 生成 / 发布”、两类能力的显式任务引用、报告到 PDF 的一条自动导出主链，以及能力生成器的一键安装/更新、路径迁移占位符、stdio MCP 基础自检、MCP 管理页手动健康检查、健康状态持久化、运行方式/运行时声明和第一版远程 MCP 协议检测。更通用的自动发现、自然语言参数提取、审批、回滚、配额/沙箱治理、`keep_alive` MCP 后台守护与崩溃自动重启、Bun 运行时托管或自动安装、受保护远程 MCP 的登录授权引导、更完整 MCP 状态面板和更完整运行态可视化仍未完成，因此还不能按“完整实现”标准验收
- `08 DeepSearch 独立模块`
  - 文档完整度：`基本完成`
  - 主链状态：`未打通`
  - 当前进展：已确认 DeepSearch 需要先作为独立模块建设，而不是先耦合进 Workflow；产品入口已收敛为左侧侧边栏 `扩展` 内的 `DeepSearch` tab，而不是单独一级路由；文档已明确 Phase 1 必须先补站点登录态检查、登录引导、登录后恢复执行、历史抓取记录、详细内容查看，以及面向聊天 / Workflow 的高层服务复用边界；同时也已明确核心形态应为内置模块 / service，外部独立 MCP 仍不是第一产品落点；另外针对 `bb-browser` 是否应直接接入的问题，也已补完单独评估文档，结论已进一步收敛为“保留 Lumos 内置浏览器为唯一正式运行时，在内部实现 `bb-site compatibility runtime`，只吸收 `site adapter / session fetch / compact snapshot / network capture` 等能力模型，并优先支持经过审查的 Tier 1 / Tier 2 adapter”；最新又已补上“部署与本地使用形态”“Phase 1 实现拆解”“UI 与交互设计”“数据与 API 设计”“工程落地拆解”五份子文档，把“内置在 Lumos、本地通过 Lumos 实例直接使用、先做 run/artifact/UI/登录态主链、后再扩外部 facade、正式页如何交互、service/tool 数据合同如何对齐、当前仓库里具体该改哪些文件”进一步写实；同时已把“正式接管当前活动页”“`strict / best_effort` 分离”“非严格模式以 `partial` 收口”“统一 run 状态和 resume 语义”“Workflow 正式复用进入 Phase 2”这些之前分散的结论写回主文档和补充文档，避免 08 内部继续口径分裂；在代码侧，当前已落地 `扩展 > DeepSearch` 正式页签、站点登录态配置弹窗、DeepSearch 本地 SQLite 表、DeepSearch runs API、抓取历史列表、详情页和暂停/继续/取消的本地状态控制；同时也已补上共享 browser bridge client、DeepSearch service 和“当前活动页接管预览”API/UI；随后又把 takeover run 的“活动页锁定”正式落到 `run page` 持久化模型中，创建任务时会尝试捕获当前活动页并在详情页展示具体绑定信息；之后创建或恢复可执行任务后，还会真正通过内置浏览器执行通用页面接管 / 托管页创建、抓取页面摘录和截图，并把结果写回任务详情；随后又补上共享 cookie 级站点探测与正式 `waiting_login` 收口；再进一步补上了站点级“打开登录页”、`waiting_login` 恢复引导，以及 takeover 模式恢复前的当前活动页重新绑定；随后已把执行结果正式落到 `records / artifacts`，并在详情页提供记录摘要、正文 artifact、截图 artifact 和截图预览；之后继续补上第一版聊天高层 facade，内置 `deepsearch` MCP server 已进入内置 MCP 列表并默认启用，聊天系统提示会在相关诉求下优先调用 `start / get_result / pause / resume / cancel`，tool 结果直接复用 run/artifact 读模型，并提供跳转 `扩展 > DeepSearch` 的 `runId` 深链；最新又补上第一版 `zhihu` 站点 adapter runtime，当前知乎问题页、文章页和列表页会优先走站点级提取逻辑，尝试展开正文并生成更贴近真实页面结构的 `contentState / snippet / structured_json`，同时保留通用抓取兜底；再进一步，当 seed 页被识别为知乎搜索结果页时，同一 run 里还会自动挑选最多 3 个详情页继续抓取，且现在已正式覆盖 `zhuanlan.zhihu.com/p/...` 专栏正文地址，同时托管搜索页角色也已显式收敛为 `search`；相关站点路由单测已通过，最新整包 `Next build` 也再次通过；而在正式 UI 上，任务详情现已按绑定页面链路联动展示关联记录，用户可以直接看出搜索页、自动跟进详情页及其各自产物，同时运行中的任务也会自动轮询刷新；此外，`waiting_login` 的自动恢复编排现已下沉到独立 DeepSearch service / API，前端不再自己串联逐站点 recheck 与逐任务 resume，而是统一走服务端 `探测 -> 判定 -> 恢复 -> 回写`，抓取历史列表也已补上每个任务的状态说明；现在第二条认证源也已打通，用户提供的 cookie 会被解析并尽力导入到内置浏览器，再进入与共享登录态一致的登录探测链；同时页面级验证语义已进一步收敛：保存 cookie 现在只做轻量 cookie 探测，真正会打开验证页的动作只保留在显式“检查登录态”，后台 `waiting_login` 恢复也不再重复重导用户保存的 cookie，从而避免覆盖浏览器里更新后的真实登录态；另外正式 UI 和 tool 的“站点 ready” 现已只认 live probe 的 `connected`，不再把手工 `cookieStatus=valid` 误当成可运行，同时内部 `PAGE_VALIDATION_BLOCKED` 哨兵也不再直接展示给用户；最新又已把正式页重排成“站点接入 / 抓取发起 / 历史与详情”的分区结构，站点卡片、当前站点详情、操作入口和运行结果不再堆成单列长表单；同时 browser bridge 现已向 DeepSearch 回传真实错误信息，而“打开登录页”链路里的非关键页面稳定/CDP 检查失败也不再直接打断整个动作；另外手动与 tool `resume` 都已默认停止重导旧 cookie，takeover 模式在当前活动页切到无关页面时也会主动清空旧绑定并回到“等待可接管页面”；最新还已补上聊天触发 DeepSearch 时的非打扰后台浏览器约束，托管页、站点隐藏页、显式页面校验页和由后台自动化页派生的新页 / 弹窗都会保持后台，聊天流也不再因 `chrome-devtools` 工具调用自动展开右侧面板，普通聊天、桥接会话与 StageWorker 加载 browser MCP 时都会默认使用后台模式，减少模型误用原始浏览器工具时影响用户操作；同时当前还已新增五条“免登录公开资料源” adapter 主线，`中文维基文库`、`Chinese Text Project`、`Project Gutenberg`、`Europe PMC` 和 `PMC BioC` 现在都已进入默认站点清单、免登录站点判定、聊天侧站点别名和 adapter registry，DeepSearch 可沿现有 adapter 执行链对这五类公开文本源执行搜索和机器可读正文提取，为中英文图书/古籍/论文型 DeepSearch 扩展建立可运行原型
  - 当前缺口：自动登录完成检测、执行期更细粒度页面控制、更强的完整正文抽取、Workflow capability facade、更多站点 adapter、`bb-site compatibility runtime`、受控 session fetch、compact snapshot/ref、CDP observer 和 network capture 都还未落地；此外，知乎“搜索结果页 -> 多详情页”虽然现在已补上同 run 自动跟进、搜索页/详情页角色区分、UI 联动展示、运行态自动刷新、规则级单测，以及 `waiting_login` 阶段的自动检测和自动继续，并已新增服务端恢复编排、第一版用户 cookie 导入浏览器与第一版显式页面验证，但真实浏览器下的更多抓取质量调优与更系统的 UI 验收仍需继续完成；而 `中文维基文库` / `Chinese Text Project` / `Project Gutenberg` / `Europe PMC` / `PMC BioC` 这五条新公开资料源目前也都还只是第一版 adapter 原型，尚未补正式 UI 文案、运行结果专项验收和更多同类资料源扩展，因此 DeepSearch 核心主链仍未正式打通；当前可验收的是产品壳层、本地数据层、接管预览、run 级页面绑定、基础页面快照、第一版共享登录探测、第一版登录恢复、第一版 artifact-backed result、第一版聊天 tool 复用、第一版知乎页面提取、第一版知乎搜索结果自动跟进详情页、第一版页面链路可视化、第一版运行态自动刷新、第一版等待登录自动恢复、第一版服务端恢复编排、第一版用户 cookie 导入浏览器、第一版显式页面验证、第一版聊天触发 DeepSearch 不自动打开右侧浏览器面板，以及第一版 `中文维基文库` / `Chinese Text Project` / `Project Gutenberg` / `Europe PMC` / `PMC BioC` adapter 接线，不是完整深度研究能力
- `应用平台 / AI 创建器`
  - 文档完整度：`部分完成`
  - 主链状态：`未打通`
  - UI 可验收范围：用户现在可从侧边栏进入 `应用`，在应用列表点 `新建应用`，选择 `空白应用 / 客户跟进 / 周报助手 / 资料整理` 起点，填写应用名和描述后进入应用开发界面；空白起点预览保持空白，不再出现 `页面 / 数据 / 安装` 这类内部阶段卡；非空模板会立即生成可预览草稿；开发界面主区域可在 `预览 / 代码 / 需求 / 项目状态 / 详情` 间切换，其中 `需求` tab 已改为左侧 Story 列表 + 右侧单条编辑器，可编辑标题、用户故事、状态、优先级、验收条件、关联页面和关联数据集合；`项目状态` 展示草稿状态、文件概览和安装前检查，`详情` 只保留可编辑的应用基本信息；底部对话区直接使用工作流编辑同源的可折叠 `ChatView / MessageInput`，不是右侧对话，也不是自定义仿制聊天框；底部发送需求后会通过同一套聊天消息流显示 AI 生成进度和工具步骤；后端服务端工具循环会真实执行 `upsert_story / write_file / validate_app / finish`，可先把需求写入 Story，再把通过自检的文件写入草稿并刷新预览；`代码` tab 可查看当前文件、加载历史版本、对照当前版/上一版，并一键回滚上一版；`设置 > AI助手` 可看到并编辑“应用开发助手”的服务商、模型和系统提示词；仍可点击 `保存并安装` 生成可打开的本地应用；当前 UI 里应打开 `DeepSearch 报告测试` 草稿预览页验证“输入主题 -> 生成模拟报告 -> 左侧历史记录出现 -> 点击历史恢复报告 -> 报告预览不再撑出卡片”；该草稿内部 manifest 名称为 `DeepSearch 报告生成`，不要把内部名称误报为 UI 入口名称。内置应用方向现在可额外验证：在 `应用` 列表打开 `微信助手`，看到 `概况 / 跟进 / 自动化 / 设置` 四个页签；完成微信读取授权后，`概况` 可点击同步/重建并查看互动 Top、AI 近期话题、久未联系和自定义报表入口；`跟进` 可点击 `分析微信消息` 生成 AI 候选，采纳后进入跟进清单，也可手动新建、编辑、完成和删除，刷新页面后仍应保留，删除会先确认；`自动化` 空列表会显示 `每日微信总结` 内置任务卡，点击 `启用` 会创建真实调度任务；也可新建提醒、用“一句话创建”调用微信助手 Agent、开关、编辑 cron/描述/提醒内容和删除，刷新页面后仍应保留，删除会先确认；一次性提醒点 `编辑` 时应看到 `执行时间` 日期时间输入，修改后卡片里的下次执行时间和人话描述会同步更新；已接入调度的自动化会显示“已接入调度”，可点击 `立即运行`，也可在微信助手对话里说“查看自动化列表 / 立即运行某个提醒 / 把某个提醒改成每天 10 点 / 把某个提醒的提醒内容改成检查合同回款 / 把每日微信总结的总结要求改成只总结待办和未回复消息 / 某个提醒为什么失败 / 暂停某个提醒 / 开启某个提醒 / 删除某个提醒 / 每日总结”，卡片会显示最近运行状态，点击立即运行后自动化卡片和最近结果会在运行中自动刷新；运行完成后会出现 `最新结果` 入口，也可点 `记录` 跳到对应 Workflow 计划任务详情查看执行历史；自动化页底部的 `最近结果` 会展示多条运行摘要，支持按关键词搜索和按 `全部 / 成功 / 运行中 / 失败 / 取消` 筛选，若 run 产出了 `wechat-daily-summary.md` 也会直接预览报告片段，并可点击 `下载报告` 导出完整 Markdown；删除自动化后，已写入微信助手归档的报告仍会显示为“已归档”，不会再跳到已不存在的 Workflow 记录；通过助手输入“每天晚上 9 点总结微信消息”创建的 `每日微信总结` 会生成带报告节点的计划任务；`设置` 可选择 AI 服务商、模型、分析窗口、提示词、话题白名单和排除名单，提示词里现在包含“每日总结报告”；设置页不再展示当前尚未接线的分析频率 / 通知渠道控件。
  - UI 可验收范围补充：自动化页 `最近结果` 里的每条报告现在可点 `查看报告` 在弹窗内查看完整正文；长报告正文在弹窗内滚动，并按 Markdown 样式渲染标题、列表、代码块和表格等结构，列表卡片只显示短预览，不应再撑出结果卡片。
  - UI 可验收范围补充：自动化页里编辑定期任务时，现在应看到 `重复频率 / 执行时间 / 星期 / 间隔小时` 这类普通表单；用户可把任务改成每天、每周某天或每 N 小时执行，不需要手写 cron。若遇到历史高级规则，仍会显示高级 cron 和展示文案以免丢失原规则。
  - UI 可验收范围补充：自动化页 `最近结果` 搜索框现在可以按完整报告正文里的关键词筛选历史报告。
  - UI 可验收范围补充：用户编辑自动化后立刻点击 `立即运行`，应以刚保存的时间/内容触发；删除正在编辑的自动化时，不应再因旧的防抖保存补丁产生额外保存错误。
  - UI 可验收范围补充：用户编辑跟进标题/摘要/下一步后立刻点完成、归档或删除，界面不应丢刚输入的内容，也不应因旧的防抖保存补丁弹出额外保存错误。
  - UI 可验收范围补充：跟进页或自动化页保存失败时，页面顶部应显示 `保存失败` 和 `重试保存`；用户刚输入的跟进标题、摘要、下一步或自动化规则内容应留在界面上，点击 `重试保存` 会重新提交这轮改动；同一条记录保存失败后继续改其它字段，也不应把前一个失败字段回滚。
  - UI 可验收范围补充：`跟进` 页加载、采纳 AI 推荐、忽略推荐、手动新建或保存编辑时，如果接口失败或返回不完整数据，应显示失败原因并保留当前可见状态，不应出现空白跟进、`undefined` 字段或把失败误当成功。
  - UI 可验收范围补充：微信同步刚启动或刷新页面后，只要后端仍在同步，概况页同步条应显示同步中状态，不应允许重复点击立即同步或清空重建。
  - UI 可验收范围补充：近期话题生成中刷新页面后，对应私聊/群聊话题卡片应继续显示分析中状态，不应出现可重复点击的生成入口。
  - UI 可验收范围补充：近期话题点击 `生成话题 / 重新生成` 时，如果 AI 返回格式异常，页面不应显示英文 JSON 解析错误；应生成带“本地兜底”说明的话题结果，或在确实无法生成时显示产品化中文错误。
  - UI 可验收范围补充：如果 `概况` 或 `近期话题` 接口临时失败，页面应显示加载/生成失败原因并保留已有可见内容，不应出现空白卡片、`undefined` 文案或组件异常。
  - 新增 UI 验收目标：近期话题不应只显示一组混合话题列表；用户应能选择日期范围，并看到按微信联系人 / 群聊分组的话题结果。每天的话题分析按凌晨 4 点切日自动入库；点击某个话题，应能查看该话题相关的原始消息上下文，至少能在弹窗里看到命中消息和前后对话片段，后续可继续扩展为跳转到完整会话附近。
  - UI 可验收范围补充：本轮近期话题每日归档第一版已进入可启动验证范围；重启开发服务后，用户可在 `微信助手 > 概况 > 近期话题` 看到日期范围控件、私聊/群聊两个话题区内按联系人或群聊分组的结果，以及每个话题右侧的 `消息` 入口。若当前日期范围没有归档数据，可先点 `生成所选结束日` 生成当前日期范围最后一天；系统自动归档上一完整业务日仍需要等待 04:00 后或通过后端调度长期观察确认。
  - 新增 UI 验收目标：`跟进` 页的 `AI 推荐` / AI 候选项必须让用户看得出“来自哪个微信群 / 私聊、是谁说的、原文是什么”；类似“整理节前遗留问题清单发到客户群”这类推荐，应展示微信里可见的群名或联系人名，不应直接展示 `45434442516`、`25984985930267888@openim` 这类内部 ID。
  - UI 可验收范围补充：本轮已把 `AI 推荐` 的 LLM 输入改成匿名会话 source 编号，并在落库前映射回真实会话；推荐卡片标题、原因、来源名和原文预览会统一清理 `wxid / openim / chatroom / 长数字群 ID`。真实 UI 验收时应重新点击 `跟进 > 分析微信消息` 生成新候选；历史已生成的旧候选可能仍保留旧文案，需要重新分析或忽略后再看新结果。
  - UI 可验收范围补充：如果某条候选或原文几乎只有内部 ID，卡片应显示 `微信待跟进事项 / 来自微信消息的待跟进线索 / 暂无原文` 等安全文案，而不是把 `openim` 或长数字 ID 原样显示出来。
  - UI 可验收范围补充：本轮已补群消息真实发言人字段。完成重新同步或清空重建后，`跟进 > AI 推荐`、`概况 > 聊天记录搜索 > 查看上下文`、`概况 > 近期话题 > 消息` 应优先显示群内发言人备注 / 昵称；如果微信数据里无法反查发言人，则仍降级显示 `群成员`，但不应显示 `openim` 或长数字 ID。最新又补上 `AI 推荐` 自己使用的 `analyze_snapshot` 链路，AI 分析入口会把 `sender_wxid / sender_display` 从 Python 读取层传到 Next 归一化层，再进入待办候选展示，不再只依赖同步 mirror 才有发言人字段。
  - UI 可验收范围补充：`概况 > 近期话题` 的默认日期范围现在以“上一完整业务日”为结束日，避免默认把尚未结束的当天当成日归档来生成；用户仍可手动把结束日期选到今天后补生成。群聊话题卡片里的参与人也会优先显示真实群成员备注 / 昵称和“我”，只有旧数据或无法反查时才降级为 `群成员`。
  - UI 可验收范围补充：`概况 > 近期话题` 和 `概况 > 聊天记录搜索` 当前会继续清理旧消息正文里的内部 ID 前缀；在真实 UI 验收时，搜索结果片段、`查看上下文` 弹窗、话题 `消息` 弹窗不应再直接显示 `25984985930267888@openim:` 这类技术标识。若要补生成历史日期的话题，先把日期范围结束日切到目标日期，再点对应卡片里的 `生成所选结束日`。
  - UI 可验收范围补充：`设置 > 不分析的对话` 的已选标签不再直接显示保存的微信内部 ID；即使旧配置里只剩原始 `wxid / chatroom / openim / 长数字 ID`，设置页也会降级显示为“微信联系人 / 微信群聊”。新建跟进的联系人搜索也不再把 `wxid` 放进可搜索文本，避免用户按技术 ID 心智使用。
  - UI 可验收范围补充：在 `设置 > 近期话题` 从白名单移除某个群 / 联系人或把它加入排除名单后，回到 `概况 > 近期话题`，该来源的历史归档话题也不应继续显示；直接打开旧话题的 `消息` 上下文也应显示找不到，而不是继续展示原始聊天片段。
  - UI 可验收范围补充：如果白名单里的会话也被加入排除名单，近期话题卡片应按有效白名单数量处理；如果最终没有可分析来源，应显示 `没有可分析的白名单对话` 并引导回设置，而不是让用户反复点生成。
  - 新增 UI 验收目标：每日微信总结的 `查看报告` 弹窗必须能上下滚动正文，弹窗底部的 `打开执行记录` 和 `下载 Markdown` 必须可点击；报告列表还需要提供删除入口，删除后列表和归档数据状态必须一致。
  - UI 可验收范围补充：本轮已把 `查看报告` 弹窗底部操作固定在正文滚动区外，`下载 Markdown` 改为前端主动下载文件，`打开执行记录` 点击时会关闭弹窗后跳转。真实 UI 验收时应在 `自动化 > 最近结果` 打开一条 `每日微信总结` 报告，确认正文可上下滚动、下载会生成 `.md` 文件、执行记录可跳转、删除后列表不再显示该报告。
  - UI 可验收范围补充：打开 `自动化 > 最近结果 > 查看报告` 后，弹窗底部现在也应能直接点击 `删除报告`；确认后弹窗关闭，最近结果列表刷新，刚删的报告不再出现。
  - UI 可验收范围补充：自动化页上方当前独立的 `微信助手` 对话框已移除；应用内自然语言创建、搜索和管理应统一走底部全局 `微信助手` 对话框，样式、折叠行为和输入体验复用工作流、资料库同源底部入口。
  - UI 可验收范围补充：用底部 `微信助手` 成功创建 / 修改 / 删除 / 立即运行自动化时，即使后续列表刷新失败，也不应出现“微信助手暂时不可用”这类把成功操作误报成失败的聊天气泡；应保留成功回复并只显示刷新失败提示。
  - UI 可验收范围补充：当微信授权或密钥未就绪时，概况页、近期话题白名单和排除名单弹窗应提示用户回到页面上方的数据授权区域，不再要求寻找旧版 `数据授权` 页签。
  - UI 可验收范围补充：概况页点击 `告诉 Lumos 你想看什么` 后生成的自定义统计报表，应只展示当前同步微信数据能算出的真实统计；如果当前窗口里没有 emoji、深夜聊天或疑似承诺，应看到空状态，而不是固定示例数字。
  - UI 可验收范围补充：概况页生成自定义统计报表后刷新页面，最近生成的报表卡应继续保留；删除某张报表后刷新也不应恢复。
  - UI 可验收范围补充：设置页修改微信助手 AI 服务商、模型或“跟进提取”提示词后，再到跟进页点击 `分析微信消息`，后端应按该设置发起文本生成；若所选 provider 没有可用模型，应显示明确错误，而不是静默使用别的 provider。
  - UI 可验收范围补充：设置页 AI 服务商保持“跟随全局默认”时，只要 Lumos 已有全局默认文本服务商，概况页近期话题卡片不应再提示“尚未配置 AI 服务商”，可以直接对白名单对话生成话题。
  - UI 可验收范围补充：设置页把某个联系人或群加入排除名单后，再点击跟进页 `分析微信消息`，该会话不应进入本次 LLM 分析；调整分析窗口或灵敏度后，新一轮跟进提取也应按最新设置执行。
  - UI 可验收范围补充：概况页现在可在 `聊天记录搜索` 中输入关键词，按 `全部 / 私聊 / 群聊` 和时间范围筛选本机已同步微信消息；搜索结果应显示真实会话名、群聊/私聊、发送方、时间和消息片段，输入联系人名、群名、wxid 或消息正文关键词都应能命中，选择 `全部历史` 时不再只受概况统计窗口限制；点击结果里的 `查看上下文` 应打开同一会话命中时间前后的消息弹窗。
  - UI 可验收范围补充：底部全局 `微信助手` 对话框现在可直接问“查一下微信里关于某某的消息”或“微信里有没有某个关键词”；助手应返回本机消息搜索结果摘要，并显示可点击的 `去概况页搜索` 按钮，点击后切到概况页继续看完整结果和上下文；其它助手动作如 `刷新自动化 / 去跟进页` 也应显示为按钮。
  - UI 可验收范围补充：点 `去概况页搜索` 后，概况页会自动带上刚才的关键词并执行 `全部历史` 搜索，不需要用户再手工输入一次。
  - UI 可验收范围补充：自动化页不再显示单独的 `快速创建` 按钮和第二个“一句话创建”输入框；自然语言能力统一从底部全局 `微信助手` 使用，手动创建仍通过右上角 `新建`。
  - UI 可验收范围补充：当前自然语言能力已经移动到底部统一 `微信助手` 对话框；用户可在任意页签底部输入“查一下微信里关于合同的消息 / 立即运行每日微信总结 / 暂停某个提醒 / 删除某个提醒”，展开后可看到历史消息和动作按钮，收起时只保留统一底部输入条。
  - UI 可验收范围补充：跟进详情页状态下拉应能选择 `待处理 / 进行中 / 已完成 / 已归档`，选择 `进行中` 后刷新页面仍保留；点击 `挂个提醒` 应弹出提醒时间和提醒内容确认框，创建后详情页会显示成功反馈，点击 `去自动化查看` 会切到自动化页并看到该跟进对应的提醒规则。
  - UI 可验收范围补充：跟进页点击 `新建` 后，`涉及` 区域可以搜索联系人、群名或 wxid，选择多名联系人后会显示已选标签；创建后进入跟进详情应看到所有涉及对象，刷新页面后这些对象仍保留，不再只剩第一个联系人。打开新建弹窗时会尝试加载完整微信通讯录，因此不只限于概况页当前统计窗口里的活跃对象；若某个历史跟进只剩 wxid，详情页也会至少显示该 wxid，不会整块消失。
  - UI 可验收范围补充：自动化页点击某条规则的 `立即运行` 后，该规则会显示加载状态；如果此时其它规则也有 `立即运行`，按钮应显示 `等待中` 并不可点击，页面顶部会给出已有触发中的提示。
  - UI 可验收范围补充：自动化页某条规则正在触发时，顶部会显示“正在触发「xxx」”的状态条，明确告诉用户当前是哪条规则在跑。
  - 当前进展：应用开发界面已取消三栏开发者工作台形态，收敛到用户指定的“所见即所得预览 + 代码/需求/项目状态/详情切换 + 底部工作流同源 AI 对话”；本地草图生成已下沉到后端 `blueprint` 接口但不再作为正式界面按钮暴露；最新真实 AI 运行时已抽为独立服务，并补上与 `ChatView` 兼容的 SSE 执行状态、服务端工具循环、工具步骤可视化、校验错误回灌修复、第一版模板起点、文件版本查询和回滚 API，安装接口继续复用标准应用安装流程；Story 已从普通需求文本升级为独立表、API 和需求 tab 可编辑主从界面，应用开发助手 prompt 也已接入 SOP，要求先梳理和确认 Story 再进入正式文件生成；另外开发启动脚本已修正端口错配问题，避免 3000 被其他项目占用时 Electron 仍等待错误端口；此前已修正应用开发助手 provider 解析和底部模型选择器的能力范围，避免 AppBuilder 误选 `local_auth` 的“本地”服务商导致轻量文本生成失败；最新又把应用助手默认代码生成栈收敛到真实可预览的 Lumos UI 栈：默认推荐 `@lumos/ui` 的 shadcn 组件、Tailwind token、`lucide-react` 图标、`@lumos/app` 平台 SDK 以及 `clsx/tailwind-merge/class-variance-authority` 工具，同时明确禁止默认使用尚未打包进 app runtime/importmap 的 `recharts / framer-motion / date-fns / react-hook-form / zod / zustand / @dnd-kit/core / cmdk`，减少“提示词允许但预览加载失败”的断链；此外已把 AI / Agent / Workflow / DeepSearch 从“隐藏实现细节”提升为应用生成硬规则：只要应用调用这些能力，就必须有 manifest 权限、可见设置/管理入口、loading/error/retry 和运行状态，AI 默认走 `ai.complete`，不得默认用当前未稳定接入的 `ai.stream / ai.structured`；本轮继续把能力契约从提示词推进到静态拦截：`ai.complete` 现在必须有可见 AI/Agent 设置入口并能管理 system prompt、输出要求、temperature、maxTokens，`workflow.run` 必须同时声明权限、写入对应 `workflows/<id>.json` 并展示工作流管理/状态入口，`deepsearch.*` 必须展示 DeepSearch 配置/登录权限/证据/错误/重试入口；同时 `ai.complete` 的沙箱预览通道已接到真实 `/api/apps/<id>/ai/complete`，小应用的 Agent 提示词和生成参数会透传到 Lumos 当前全局文本生成服务商。
  - 最新自测：此前已用应用开发 API 创建“DeepSearch 报告测试”草稿，并跑到 Story 落库、Demo 文件生成与编译通过；本轮针对用户当前草稿 `bs_689501a371030f35` / `builder-bs-u-689501a371030f35` 继续补历史记录与报告预览布局，`pages/index.tsx` 当前已到第 6 版；随后用 sandbox loader/protocol 直接加载该 appId，确认 shell、`_app/pages/index.tsx.mjs` 和 extensionless `_app/lib/markdown` 均返回 200；同时补了 AppBuilder provider 解析单测、应用编译器对“默认 Lumos UI 栈可用 / 未打包第三方库不可用”的回归测试，以及 AppBuilder 能力契约检查，能拦截 `ai.complete` 缺权限、`ai.stream` 默认使用、`workflow.run` 缺权限、`deepsearch.*` 缺权限等问题；最新目标 AppBuilder/compile/text-generator/sandbox dispatcher 测试 38 条通过，`npm run typecheck` 通过。严格说明：该 demo 当前仍不是实际调用 DeepSearch 服务完成真实资料检索。内置微信助手此前已完成真实本机接口与浏览器 UI 自测；本轮新形态继续通过 `/apps/wechat-assistant` HTTP 200、`/api/apps/builtin/wechat/status` ready、`/api/apps/builtin/wechat/todos` 返回真实待办、`/api/apps/builtin/wechat/automations` 返回持久化列表验证；最新还用 API 创建临时 `wechat_summary` 自动化，确认背后计划任务 DSL 已是 `generate_report(agent/code handler) -> notify(notification)`，随后删除临时数据；并用临时普通提醒调用微信助手 `POST /automations/:id` 触发接口，确认对应 Workflow run 记录进入 `success` 后删除临时数据；最新一次状态冒烟还确认自动化列表会从 `running` 更新到 `success`，并返回 `latestRunId / lastRunAt` 供卡片展示 `最新结果`；新增 reports API 后又用临时普通提醒确认 `/api/apps/builtin/wechat/reports` 会返回最新运行摘要；随后继续把 reports API 从“每条自动化最新一次”扩到“每条自动化最多 5 条 / 全局最多 20 条”，并用同一临时自动化连续触发两次确认 reports 返回两条历史记录；最新对话触发冒烟也已通过：先创建临时普通提醒，再向 `/api/apps/builtin/wechat/agent` 发送“立即运行 Codex 冒烟对话提醒”，确认助手回复已触发、自动化状态进入 running、reports 随后返回 success 摘要；随后继续用临时自动化验证微信助手对话的“查看列表 / 暂停 / 开启 / 删除”，删除后再次确认自动化和报告列表恢复为空。本轮又新增路由回归测试覆盖多自动化下的模糊删除保护、明确名称暂停、明确名称改时间、明确名称改提醒内容、明确名称改每日总结要求、修改每日微信总结时不把目标名里的“每日”误判为改时间、模糊改时间保护、失败诊断、模糊诊断保护、单条规则泛化删除和即时总结手动触发；最新再补 `wechat_assistant_reports` 迁移和报告归档单测，覆盖同一 run 归档更新、从 workflow session 反查 schedule/run 归档、调度最终状态回写归档，前端卡片也已改为对归档报告展示“已归档”而不是失效 Workflow 跳转；同时补设置 schema 回归测试，确认 `topicExtractor` 提示词不会在保存时被剥离；最新新增报告下载路由回归测试，覆盖 Markdown 下载、无正文时摘要 fallback 和 404；本轮新增底层 `scheduled-workflows` timing 回归测试，覆盖一次性调度显式改期、到点后先关闭避免重复触发、终态后不再计算下一次时间，并在微信助手自动化测试里覆盖一次性提醒改期会同步到底层 `nextRunAt`；本轮还新增 `daily-summary` 回归测试，覆盖无服务商基础报告、配置服务商后的 LLM Markdown 增强、LLM 失败回退、最近消息片段过滤，以及设置保存 `dailyReporter` 提示词；微信助手相关 16 个测试文件 86 条通过，`npm run typecheck` 通过；本机当前接口确认 `/apps/wechat-assistant` 返回 200，`/api/apps/builtin/wechat/status` 为 ready，微信读取授权、密钥、sqlcipher、微信数据目录和内置 MCP 均可用，自动化列表当前为空，因而 UI 应显示 `每日微信总结` 内置任务卡；真实 API 冒烟已创建临时 `Codex 修改冒烟提醒`，再通过微信助手对话把它从每天 21:00 改为每天 22:00，随后删除并确认自动化 / 报告列表已清空；随后又创建临时 `Codex 诊断冒烟提醒`，通过微信助手对话查询状态并返回“已接入调度 / 尚未触发”，随后删除并确认自动化 / 报告列表已清空；本轮 `npm run build` 通过，构建路由清单已包含 `/api/apps/builtin/wechat/reports/[id]/download`，构建仍保留仓库原有 `compiled-module-loader.ts` 动态模块 warning。Playwright 自带 Chromium 未安装，系统 Chrome headless 在本机 Playwright 下 SIGABRT，因此本轮未完成新的真实浏览器截图验收，只完成 HTTP/API/build/test 验证。
  - 最新自测补充：本轮补 `查看报告` 弹窗并把完整正文改为 Markdown 渲染后，`npm run lint -- src/components/apps/builtin/wechat/AutomationsTab.tsx`、`npm run typecheck` 和 `npm run build` 均通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。报告归档与报告下载相关回归测试 `src/lib/wechat-assistant/__tests__/report-archive.test.ts`、`src/app/api/apps/builtin/wechat/reports/[id]/download/__tests__/route.test.ts` 共 2 个测试文件 6 条此前已通过。尚未完成新的真实浏览器截图验收。
  - 最新自测补充：本轮补定期任务普通表单编辑后，`npm run typecheck` 通过；仍需下一轮真实浏览器 UI 验收确认弹窗滚动、移动端布局和 Select 浮层表现。
  - 最新自测补充：针对 `src/components/apps/builtin/wechat/AutomationsTab.tsx` 的组件级 `npm run lint -- src/components/apps/builtin/wechat/AutomationsTab.tsx` 已通过；过程中顺手修正一次性提醒 `datetime-local min` 在 render 中直接调用 `Date.now()` 的 React purity 问题。
  - 最新自测补充：本轮补自动化触发前冲刷待保存改动后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-automations.ts src/components/apps/builtin/wechat/AutomationsTab.tsx`、`npm run typecheck`、`npm run build` 均通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮补跟进 / 自动化保存失败保留本地改动、失败后继续编辑合并旧补丁与可见 `重试保存` 后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-followups.ts src/components/apps/builtin/wechat/use-wechat-automations.ts src/components/apps/builtin/wechat/AutomationsTab.tsx src/components/apps/builtin/wechat/FollowupsTab.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx`、`npm run typecheck`、`npm run build` 均通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。过程中曾并行跑 `typecheck` 和 `build`，`typecheck` 因 `.next/types` 被 build 清理重建而报缺文件，已在 build 后单独重跑通过。
  - 最新自测补充：本轮补 `跟进` 页防抖保存冲刷后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-followups.ts src/components/apps/builtin/wechat/use-wechat-automations.ts src/components/apps/builtin/wechat/AutomationsTab.tsx`、`npm run typecheck`、`npm run build` 通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮补微信同步 `starting / inFlight` 运行态判断后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-sync.ts src/components/apps/builtin/wechat/SyncBanner.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx`、`npm run typecheck`、`npm run build` 通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮补近期话题 `inFlight` 运行态后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-topics.ts src/components/apps/builtin/wechat/TopicPanel.tsx`、`npm run typecheck`、`npm run build` 通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮修正旧版 `数据授权` 页签文案后，`npm run lint -- src/components/apps/builtin/wechat/OverviewTab.tsx src/components/apps/builtin/wechat/SettingsTopicWhitelistDialog.tsx src/components/apps/builtin/wechat/SettingsExcludedDialog.tsx` 与 `npm run typecheck` 通过。
  - 最新自测补充：本轮补跟进多联系人保存后，`npm test -- src/lib/db/__tests__/migrations-wechat-assistant.test.ts src/lib/wechat-assistant/__tests__/todos.test.ts src/app/api/apps/builtin/wechat/todos/__tests__/route.test.ts` 通过 3 个测试文件 11 条，覆盖旧数据迁移兜底、手动跟进多联系人保存、编辑涉及联系人、AI 建议单联系人兼容，以及 todos 创建 / PATCH 路由的联系人数组透传；跟进页接入完整通讯录懒加载和 wxid 兜底后，目标 `lint`、`npm run typecheck`、`npm run build` 均通过，构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning；随后修正通讯录未 ready 空结果缓存问题后，相关 `lint` 与 `npm run typecheck` 再次通过。
  - 最新自测补充：本轮补自动化手动触发忙碌反馈后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-automations.ts src/components/apps/builtin/wechat/AutomationsTab.tsx`、`npm run typecheck`、`npm run build` 通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。随后又补顶部“正在触发”状态条并再次通过 `lint`、`typecheck` 和 `build`。
  - 最新自测补充：本轮去除自定义报表 mock 数据后，`npm test -- src/lib/wechat-assistant/__tests__/overview-compute.test.ts` 通过 7 条，新增覆盖从真实消息流计算 emoji、深夜聊天、疑似承诺和近 7 天活跃对话；`npm run lint -- src/components/apps/builtin/wechat/CustomReportCard.tsx src/components/apps/builtin/wechat/AddReportDialog.tsx src/components/apps/builtin/wechat/OverviewTab.tsx src/components/apps/builtin/wechat/custom-reports.ts`、`npm run typecheck` 和 `npm run build` 通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮清理未引用的微信助手 mock 残留并新增自定义报表 localStorage 持久化后，`rg` 已确认正式微信助手代码里不再引用 `mock-people / mock-relations / getMockState / RelationsAppState`，`npm run lint -- src/components/apps/builtin/wechat/relations-types.ts src/components/apps/builtin/wechat/WeChatAssistantApp.tsx` 与 `npm run typecheck` 通过；随后完整 `npm run build` 通过，构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮修正跟进分析 provider/model/prompt 设置不生效后，新增 `src/lib/wechat-assistant/__tests__/ai-runner.test.ts` 覆盖微信助手所选 provider/model/跟进提示词、全局默认 fallback、无可用模型错误；`npm test -- src/lib/wechat-assistant/__tests__/ai-runner.test.ts src/lib/wechat-assistant/__tests__/daily-summary.test.ts` 通过 2 个文件 7 条，`npm run lint -- src/lib/wechat-assistant/ai-runner.ts src/lib/wechat-assistant/ai-event-extractor.ts src/lib/wechat-assistant/__tests__/ai-runner.test.ts` 与 `npm run typecheck` 通过；随后完整 `npm run build` 通过，构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮继续补跟进分析的排除名单 / 分析窗口 / 灵敏度生效后，`npm test -- src/lib/wechat-assistant/__tests__/ai-runner.test.ts` 通过 4 条，新增覆盖被排除会话不会进入 LLM 输入、窗口天数会传给 crop、严格灵敏度会进入 system prompt；`npm run lint -- src/lib/wechat-assistant/ai-runner.ts src/lib/wechat-assistant/__tests__/ai-runner.test.ts` 与 `npm run typecheck` 通过。
  - 最新自测补充：本轮修正近期话题 provider 语义后，`npm test -- src/lib/wechat-assistant/__tests__/topic-extractor.test.ts` 通过 9 条，新增覆盖“跟随全局默认 provider”和“显式选择微信助手 provider/model”两条话题提取路径；`npm run lint -- src/lib/wechat-assistant/topic-extractor.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/components/apps/builtin/wechat/use-wechat-topics.ts src/components/apps/builtin/wechat/TopicPanel.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx`、`npm run typecheck` 和 `npm run build` 通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮最后又跑了更完整的微信助手测试集合：`npm test -- src/lib/wechat-assistant/__tests__ src/app/api/apps/builtin/wechat/agent/__tests__ 'src/app/api/apps/builtin/wechat/reports/[id]/download/__tests__'`，共 14 个测试文件 82 条通过。
  - 最新自测补充：本轮去掉状态接口 `Demo` 文案后，`npm run lint -- src/app/api/apps/builtin/wechat/status/route.ts`、`npm run typecheck` 和最终完整 `npm run build` 通过；构建仍只保留既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮临时启动生产版 Next 服务 `PORT=3012 HOSTNAME=127.0.0.1 npm run start` 做 HTTP 冒烟，`/apps/wechat-assistant` 返回 200，`/api/apps/builtin/wechat/status` 返回 ready 且微信读取授权/密钥/内置 MCP 可用，`/api/apps/builtin/wechat/sync` 返回 `inFlight:false / totalMessages:1832025`，`/api/apps/builtin/wechat/topics` 返回 personal/group 空闲态，`/api/apps/builtin/wechat/todos` 返回真实待办，`/api/apps/builtin/wechat/automations` 与 `/reports` 当前为空，`/api/apps/builtin/wechat/overview` 返回 ready 且 `activeChats:147 / messagesInWindow:58413`，同时 `reportInsights` 已返回真实统计摘要：emoji 8 个、深夜聊天总量 3243、深夜聊天行 6 条、疑似承诺 8 条、近 7 天活跃对话 8 条，首个 emoji 为 `🍊 / 115`；验证后已关闭临时 3012 服务。
  - 最新自测补充：本轮新增微信助手聊天记录搜索后，`npm test -- src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/search/__tests__/route.test.ts` 通过 2 个文件 12 条，覆盖真实 mirror 消息关键词检索、会话显示名 / wxid 命中、私聊/群聊/时间范围过滤、LIKE 通配符转义和 API 参数归一化；`npm run lint -- src/lib/wechat-assistant/mirror-store.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/search/route.ts src/app/api/apps/builtin/wechat/search/__tests__/route.test.ts src/components/apps/builtin/wechat/MessageSearchPanel.tsx src/components/apps/builtin/wechat/OverviewTab.tsx`、`npm run typecheck`、`npm run build` 均通过，构建路由清单已包含 `/api/apps/builtin/wechat/search`，仍只有既有 `compiled-module-loader.ts` dynamic require warning；随后临时启动 `PORT=3012 HOSTNAME=127.0.0.1 npm run start`，确认 `/apps/wechat-assistant` 返回 HTML，`/api/apps/builtin/wechat/search?q=微信&limit=3&days=all` 能从真实 mirror 返回 3 条微信消息，`q=%25` 也按字面 `%` 搜索而不是扩成任意匹配；验证后已关闭临时服务。追加会话名 / wxid 搜索和 `查看上下文` 补丁后，又重跑 `npm test -- src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/search/__tests__/route.test.ts src/app/api/apps/builtin/wechat/search/context/__tests__/route.test.ts`，共 3 个文件 17 条通过；目标 `lint`、`npm run typecheck` 和 `npm run build` 均通过，构建路由清单已包含 `/api/apps/builtin/wechat/search/context`。
  - 最新自测补充：本轮继续把消息搜索接入微信助手对话后，`npm test -- src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/search/__tests__/route.test.ts src/app/api/apps/builtin/wechat/search/context/__tests__/route.test.ts` 通过 4 个文件 29 条，覆盖助手对话搜索结果、无结果提示、`微信里有没有 X` 句式，以及既有自动化管理不被误伤；目标 `lint` 通过；`npm run build` 通过；随后单独重跑 `npm run typecheck` 通过。中间一次把 `typecheck` 与 `build` 并发运行导致 `.next/types` 被 build 重建时出现 TS6053 假失败，等 build 完成后单独重跑已确认通过。
  - 最新自测补充：本轮把微信助手对话 action 接到前端按钮后，`npm run lint -- src/components/apps/builtin/wechat/AssistantPanel.tsx src/components/apps/builtin/wechat/AutomationsTab.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/app/api/apps/builtin/wechat/agent/route.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过；`npm run typecheck` 通过；微信搜索 / agent 相关 4 个测试文件 29 条通过；`npm run build` 通过，仍只有既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮去掉自动化页重复 `快速创建` 入口后，`npm run lint -- src/components/apps/builtin/wechat/AutomationsTab.tsx src/components/apps/builtin/wechat/AssistantPanel.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx`、`npm run typecheck` 和 `npm run build` 均通过；构建仍只有既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮补跟进详情状态和提醒创建体验后，`npm test -- src/lib/db/__tests__/migrations-wechat-assistant.test.ts` 通过 5 条，覆盖旧待办表迁移到支持 `in_progress` 状态；`npm test -- src/lib/wechat-assistant/__tests__/automations.test.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过 2 个文件 21 条；目标 `lint` 覆盖 `FollowupDetail / FollowupsTab / use-wechat-followups / use-wechat-automations / WeChatAssistantApp / migrations-wechat-assistant / wechat-assistant db / todos route` 并通过；`npm run typecheck` 和完整 `npm run build` 通过，构建仍只有既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮补默认提醒时段接入和设置保存失败 `重试保存` 后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-settings.ts src/components/apps/builtin/wechat/SettingsTab.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx`、`npm run typecheck` 和完整 `npm run build` 均通过；构建仍只有既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮把概况页 `重新分析概况` 从同步流程中拆出后，`npm run lint -- src/components/apps/builtin/wechat/OverviewTab.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx` 和 `npm run typecheck` 通过；完整 `npm run build` 也通过，构建仍只有既有 `compiled-module-loader.ts` dynamic require warning。
  - 最新自测补充：本轮补近期话题每日归档、日期范围、来源分组和消息上下文入口后，`npm run lint -- src/lib/wechat-assistant/topic-time.ts src/lib/wechat-assistant/mirror-schema.ts src/lib/wechat-assistant/mirror-store.ts src/lib/wechat-assistant/topic-extractor.ts src/lib/scheduler/cron-engine.ts src/app/api/apps/builtin/wechat/topics/route.ts src/app/api/apps/builtin/wechat/topics/context/route.ts src/components/apps/builtin/wechat/use-wechat-topics.ts src/components/apps/builtin/wechat/OverviewTab.tsx src/components/apps/builtin/wechat/TopicPanel.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/lib/wechat-assistant/__tests__/topic-extractor.test.ts` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts` 通过 14 条，覆盖 04:00 业务日边界、每日归档按来源分组、话题消息上下文查找，以及 skipped 日归档不会永久阻止后续自动重试。本轮按用户要求未跑完整 build。
  - 最新自测补充：本轮修正底部全局 `微信助手` 对话框未接专用动作接口的问题后，`npm run lint -- src/components/apps/builtin/wechat/AssistantPanel.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/components/apps/builtin/wechat/MessageSearchPanel.tsx src/app/api/apps/builtin/wechat/agent/route.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过 12 条。
  - 最新自测补充：本轮修正 `AI 推荐` 内部 ID 泄漏源头与报告弹窗操作加固后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts` 通过 7 条，覆盖匿名 source 映射回真实会话、推荐标题/原文清理 `openim/chatroom/长数字 ID`、以及 LLM prompt 不再暴露真实内部 ID；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/ai-snapshot-crop.test.ts` 通过 5 条；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/ai-runner.test.ts` 通过 4 条；`npm test -- --runTestsByPath 'src/app/api/apps/builtin/wechat/reports/[id]/__tests__/route.test.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/download/__tests__/route.test.ts'` 通过 5 条；目标 `npm run lint -- src/lib/wechat-assistant/wechat-text.ts src/lib/wechat-assistant/ai-snapshot-crop.ts src/lib/wechat-assistant/ai-prompt.ts src/lib/wechat-assistant/ai-event-shape.ts src/lib/wechat-assistant/ai-event-extractor.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts src/components/apps/builtin/wechat/display-helpers.ts src/components/apps/builtin/wechat/AutomationsTab.tsx` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补群消息发言人链路后，`python3 -m py_compile resources/mcp-servers/wechat-export/macos/api.py` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/db/__tests__/migrations-wechat-assistant.test.ts src/lib/wechat-assistant/__tests__/todos.test.ts` 通过 5 个文件 45 条；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/search/__tests__/route.test.ts src/app/api/apps/builtin/wechat/search/context/__tests__/route.test.ts src/app/api/apps/builtin/wechat/todos/__tests__/route.test.ts` 通过 3 个文件 8 条；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/sync-engine.test.ts` 通过 4 条；目标 `npm run lint` 覆盖 mirror / sync / AI / db migration / 微信助手相关前端组件并通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补近期话题 / 聊天记录搜索可见内容清理和“生成所选结束日”语义后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-topics.ts src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/components/apps/builtin/wechat/TopicPanel.tsx src/components/apps/builtin/wechat/MessageSearchPanel.tsx src/lib/wechat-assistant/topic-prompt.ts src/lib/wechat-assistant/topic-extractor.ts` 通过；追加话题 prompt 不暴露 `openim/chatroom/长数字 ID` 的回归测试后，`npm run lint -- src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/topic-prompt.ts src/lib/wechat-assistant/topic-extractor.ts` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts src/lib/wechat-assistant/__tests__/ai-snapshot-crop.test.ts` 通过 3 个文件 29 条。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补近期话题白名单接口级过滤后，`npm run lint -- src/lib/wechat-assistant/mirror-store.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/topics/route.ts src/app/api/apps/builtin/wechat/topics/context/route.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 2 个文件 16 条；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 3 个文件 31 条；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补近期话题有效白名单计数和 skipped 空状态后，`npm run lint -- src/components/apps/builtin/wechat/TopicPanel.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx` 通过，`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补报告详情弹窗内删除入口、live 报告 tombstone 兜底和下载 Markdown 的 live 报告回查后，`npm run lint -- src/lib/wechat-assistant/report-archive.ts src/lib/wechat-assistant/__tests__/report-archive.test.ts 'src/app/api/apps/builtin/wechat/reports/[id]/route.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/download/route.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/__tests__/route.test.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/download/__tests__/route.test.ts'` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/report-archive.test.ts 'src/app/api/apps/builtin/wechat/reports/[id]/__tests__/route.test.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/download/__tests__/route.test.ts' src/lib/wechat-assistant/__tests__/reports.test.ts` 通过 4 个文件 14 条；`npm run typecheck` 通过；`npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 34 个测试文件 271 条。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补内部 ID 清理为空时的安全占位后，`npm run lint -- src/lib/wechat-assistant/wechat-text.ts src/lib/wechat-assistant/ai-event-shape.ts src/lib/wechat-assistant/topic-prompt.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts src/components/apps/builtin/wechat/display-helpers.ts src/components/apps/builtin/wechat/SuggestedFollowupCards.tsx src/components/apps/builtin/wechat/use-wechat-followups.ts src/components/apps/builtin/wechat/MessageSearchPanel.tsx src/components/apps/builtin/wechat/TopicPanel.tsx` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/ai-snapshot-crop.test.ts` 通过 3 个文件 30 条；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮拆开微信助手底部对话的后端成功与前端刷新失败反馈后，`npm run lint -- src/components/apps/builtin/wechat/AssistantPanel.tsx` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过 12 条。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补 `概况 / 近期话题` 前端接口失败防护后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-topics.ts src/components/apps/builtin/wechat/use-wechat-overview.ts` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/overview-compute.test.ts` 通过 2 个文件 22 条。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮补 `跟进 / AI 推荐` 前端响应体校验后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-followups.ts` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/todos/__tests__/route.test.ts src/lib/wechat-assistant/__tests__/todos.test.ts` 通过 2 个文件 5 条。本轮仍按用户要求未跑完整 build。
  - 最新自测补充：本轮继续补微信助手 UI 主链响应体校验后，`npm run lint -- src/components/apps/builtin/wechat/AssistantPanel.tsx src/components/apps/builtin/wechat/AutomationsTab.tsx src/components/apps/builtin/wechat/MessageSearchPanel.tsx src/components/apps/builtin/wechat/TopicPanel.tsx src/components/apps/builtin/wechat/use-wechat-followups.ts src/components/apps/builtin/wechat/use-wechat-automations.ts src/components/apps/builtin/wechat/use-wechat-sync.ts src/components/apps/builtin/wechat/use-wechat-contacts.ts src/components/apps/builtin/wechat/use-wechat-settings.ts src/components/apps/builtin/wechat/use-wechat-topics.ts src/components/apps/builtin/wechat/WeChatAssistantApp.tsx` 通过；追加同步状态失败条、刷新失败条、状态接口结构校验和首次自动同步触发条件收紧后，`npm run lint -- src/components/apps/builtin/wechat/SyncBanner.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx` 通过；补近期话题 failed 日归档冷却重试和 stale running 解锁后，`npm run lint -- src/lib/wechat-assistant/mirror-store.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts` 通过，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts` 通过 16 条；补近期话题失败 / 中断 / 部分失败 UI 后，`npm run lint -- src/components/apps/builtin/wechat/TopicPanel.tsx` 通过；补日期范围前端归一化后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-topics.ts` 通过；补报告列表后端单条坏执行记录隔离后，`npm run lint -- src/lib/wechat-assistant/reports.ts src/lib/wechat-assistant/__tests__/reports.test.ts` 通过，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/reports.test.ts` 通过 2 条；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续收口近期话题默认日期和群成员展示后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 3 个文件 34 条，覆盖上一完整业务日默认范围、群聊话题参与人使用可见成员名、日归档、来源分组、消息上下文和白名单过滤；`npm run lint -- src/lib/wechat-assistant/topic-time.ts src/lib/wechat-assistant/topic-extractor.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/app/api/apps/builtin/wechat/topics/route.ts` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补 `AI 推荐` 的 `analyze_snapshot` 发言人传递后，`python3 -m py_compile resources/mcp-servers/wechat-export/macos/api.py` 通过；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/ai-analysis/__tests__/route.test.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts src/lib/wechat-assistant/__tests__/ai-snapshot-crop.test.ts` 通过 3 个文件 16 条，覆盖 Python 返回的 `sender_wxid / sender_display` 在 Next 归一化后保留，并继续覆盖待办候选可读性和 prompt 发言人展示；目标 `npm run lint` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮给微信 IM monitor 调试日志增加轮转上限、终端默认静默和默认不打印逐轮轮询日志回归后，`npm test -- --runTestsByPath src/lib/im/providers/wechat/__tests__/monitor.test.ts` 通过 13 条；`npm run lint -- src/lib/im/providers/wechat/monitor.ts src/lib/im/providers/wechat/__tests__/monitor.test.ts` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮把微信助手底部输入框从自写 `Textarea + 发送按钮` 切到全局 `PromptInput` 组件族，并复核报告详情弹窗滚动区 / 底部按钮层级后，`npx eslint src/components/apps/builtin/wechat/AssistantPanel.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/components/apps/builtin/wechat/AutomationsTab.tsx` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补 `AI 推荐` 采纳后跟进详情的来源清理后，`npx eslint src/components/apps/builtin/wechat/use-wechat-followups.ts src/components/apps/builtin/wechat/SuggestedFollowupCards.tsx src/components/apps/builtin/wechat/FollowupDetail.tsx` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补微信助手 AI 文本生成服务商解析和设置页不可用服务商提示后，`npx eslint src/lib/wechat-assistant/provider-options.ts src/lib/wechat-assistant/ai-runner.ts src/lib/wechat-assistant/daily-summary.ts src/lib/wechat-assistant/topic-extractor.ts src/lib/wechat-assistant/__tests__/provider-options.test.ts src/lib/wechat-assistant/__tests__/ai-runner.test.ts src/lib/wechat-assistant/__tests__/daily-summary.test.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts` 通过；`npx eslint src/components/apps/builtin/wechat/SettingsTab.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/components/apps/builtin/wechat/app-settings.ts` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/provider-options.test.ts src/lib/wechat-assistant/__tests__/ai-runner.test.ts src/lib/wechat-assistant/__tests__/daily-summary.test.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts` 通过 4 个文件 34 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮复核微信助手 `近期话题` 与报告查看链路后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 3 个文件 35 条；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/reports.test.ts src/lib/wechat-assistant/__tests__/report-archive.test.ts 'src/app/api/apps/builtin/wechat/reports/[id]/__tests__/route.test.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/download/__tests__/route.test.ts'` 通过 4 个文件 12 条；`npx eslint src/components/apps/builtin/wechat/AutomationsTab.tsx src/lib/wechat-assistant/reports.ts src/lib/wechat-assistant/report-archive.ts 'src/app/api/apps/builtin/wechat/reports/[id]/route.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/download/route.ts'` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮把微信助手对话里的微信消息搜索回复也做了内部 ID 清洗后，`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过 13 条；`npx eslint src/app/api/apps/builtin/wechat/agent/route.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过；`npm run typecheck` 通过；`npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 33 个测试文件 255 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮把 contacts API、近期话题白名单弹窗和排除名单弹窗的 fallback 展示名也接到内部 ID 清洗后，`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/contacts/__tests__/route.test.ts` 通过 2 条；`npx eslint src/app/api/apps/builtin/wechat/contacts/route.ts src/app/api/apps/builtin/wechat/contacts/__tests__/route.test.ts src/components/apps/builtin/wechat/SettingsTopicWhitelistDialog.tsx src/components/apps/builtin/wechat/SettingsExcludedDialog.tsx` 通过；`npm run typecheck` 通过；`npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 34 个测试文件 257 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮把概况页数据源的展示名和承诺文本也接到内部 ID 清洗后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/overview-compute.test.ts` 通过 8 条；`npx eslint src/lib/wechat-assistant/overview-compute.ts src/lib/wechat-assistant/__tests__/overview-compute.test.ts` 通过；`npm run typecheck` 通过；`npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 34 个测试文件 258 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮把旧 `/api/apps/builtin/wechat/analysis` 兼容分析模块的可见字段也接入内部 ID 清洗后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/analysis.test.ts` 通过 3 条；`npx eslint src/lib/wechat-assistant/analysis.ts src/lib/wechat-assistant/__tests__/analysis.test.ts` 通过；`npm run typecheck` 通过；`npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 34 个测试文件 259 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补 `近期话题` 生成前同步、本轮话题 / 日报 / 旧归档可见文本内部 ID 再清洗后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/daily-summary.test.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts` 通过 3 个文件 43 条；报告查看 / 删除 / 下载相关 `npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/reports.test.ts src/lib/wechat-assistant/__tests__/report-archive.test.ts 'src/app/api/apps/builtin/wechat/reports/[id]/__tests__/route.test.ts' 'src/app/api/apps/builtin/wechat/reports/[id]/download/__tests__/route.test.ts'` 通过 4 个文件 12 条；微信助手 agent / 联系人 / 搜索 / 上下文接口 `npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts src/app/api/apps/builtin/wechat/contacts/__tests__/route.test.ts src/app/api/apps/builtin/wechat/search/__tests__/route.test.ts src/app/api/apps/builtin/wechat/search/context/__tests__/route.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 5 个文件 24 条；changed-files `npx eslint ...` 通过；`npm run typecheck` 通过；随后完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 34 个测试文件 263 条；追加默认跟进识别提示词收敛和新建跟进占位文案清理后，`npx eslint src/components/apps/builtin/wechat/default-prompts.ts src/components/apps/builtin/wechat/FollowupNewDialog.tsx` 通过，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/ai-runner.test.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts src/lib/wechat-assistant/__tests__/settings-store.test.ts src/lib/wechat-assistant/__tests__/provider-options.test.ts` 通过 4 个文件 28 条，追加 `npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补自动化 / 调度任务删除前的运行中执行确认后，`npm test -- src/lib/wechat-assistant/__tests__/automations.test.ts` 通过 10 条；changed-files `npx eslint src/lib/workflow/schedule-run-control.ts src/lib/wechat-assistant/automations.ts 'src/app/api/apps/builtin/wechat/automations/[id]/route.ts' src/app/api/apps/builtin/wechat/agent/route.ts 'src/app/api/workflow/schedules/[id]/route.ts' src/app/api/workflow/schedules/batch-delete/route.ts src/lib/wechat-assistant/__tests__/automations.test.ts` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补 AI 待办建议入库前内部 ID 清洗，并追加旧 AI 待办读取兜底清洗后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/todos.test.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts` 通过 2 个文件 15 条；`npx eslint src/lib/wechat-assistant/db.ts src/lib/wechat-assistant/db-mappers.ts src/lib/wechat-assistant/__tests__/todos.test.ts` 通过；`npm run typecheck` 通过；完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 34 个测试文件 266 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续调整 `AI 推荐` 来源名优先级后，`npx eslint src/components/apps/builtin/wechat/SuggestedFollowupCards.tsx` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续补 `近期话题 / 聊天记录搜索` 读层展示名清洗和话题手动生成文案后，`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 3 个文件 40 条；`npm run lint -- src/lib/wechat-assistant/mirror-store.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/components/apps/builtin/wechat/TopicPanel.tsx` 通过；`npm run typecheck` 通过；完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat` 通过 34 个测试文件 269 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮把 `AI 推荐` 卡片改成 `会话 / 发言人 / 相关消息` 三段式来源展示，并把来源整理函数独立成可测试逻辑；`npm test -- --runTestsByPath src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts src/lib/wechat-assistant/__tests__/todos.test.ts src/lib/wechat-assistant/__tests__/ai-event-extractor.test.ts` 通过 3 个文件 17 条，覆盖群名、发言人和相关消息不泄露 `openim / 长数字群 ID`；`npm run lint -- src/components/apps/builtin/wechat/SuggestedFollowupCards.tsx src/components/apps/builtin/wechat/suggested-followup-source.ts src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts` 通过；随后 `npm run typecheck` 通过，`npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts` 通过 35 个文件 273 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续加固报告预览与弹窗正文换行 / 链接 encode 后，`npm run lint -- src/components/apps/builtin/wechat/AutomationsTab.tsx` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮把报告 UI 的链接 / 预览 / 下载名逻辑独立到 `report-ui.ts` 后，`npm test -- --runTestsByPath src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts` 通过 3 条；`npm run lint -- src/components/apps/builtin/wechat/AutomationsTab.tsx src/components/apps/builtin/wechat/report-ui.ts src/components/apps/builtin/wechat/automations-types.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts` 通过；`npm run typecheck` 通过；随后 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts` 通过 36 个文件 276 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续做 `近期话题` 与设置页可见 ID 收口后，`npm run lint -- src/components/apps/builtin/wechat/FollowupNewDialog.tsx src/components/apps/builtin/wechat/SettingsTab.tsx src/components/apps/builtin/wechat/TopicPanel.tsx` 通过；`npm run typecheck` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 3 个文件 40 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补 `近期话题` 自动归档运行态轮询后，`npm run lint -- src/components/apps/builtin/wechat/use-wechat-topics.ts` 通过；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补 `最近结果` 完整报告正文搜索字段后，`npm run lint -- src/lib/wechat-assistant/reports.ts src/lib/wechat-assistant/__tests__/reports.test.ts src/components/apps/builtin/wechat/AutomationsTab.tsx src/components/apps/builtin/wechat/automations-types.ts` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/reports.test.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts` 通过 2 个文件 5 条；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续复核微信助手 `AI 推荐` 来源展示、`近期话题` 日期归档 / 来源分组 / 消息上下文、每日总结报告查看 / 删除 / 下载 / 搜索、微信 IM monitor 静默日志等链路后，完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts` 通过 36 个文件 276 条；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补微信助手底部对话的跟进任务新增 / 列表 / 完成入口后，`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过 16 条；`npm run lint -- src/app/api/apps/builtin/wechat/agent/route.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过；`npm run typecheck` 通过；完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts` 通过 36 个文件 279 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮补底部微信助手对话后的 `跟进 / 自动化 / 概况` 同步刷新，让历史消息里的 `去跟进页` 动作也会打开后刷新列表，并让新增/完成跟进后的动作携带目标跟进 ID 以便切页后选中对应任务；`npm test -- --runTestsByPath src/components/apps/builtin/wechat/__tests__/assistant-refresh.test.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过 2 个文件 17 条；`npm run lint -- src/app/api/apps/builtin/wechat/agent/route.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts src/components/apps/builtin/wechat/AssistantPanel.tsx src/components/apps/builtin/wechat/assistant-refresh.ts src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/components/apps/builtin/wechat/__tests__/assistant-refresh.test.ts` 通过；`npm run typecheck` 通过；随后完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts src/components/apps/builtin/wechat/__tests__/assistant-refresh.test.ts` 通过 37 个文件 280 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续收口底部 AI 对话框一致性细节后，`npm run lint -- src/components/layout/BottomChatPanel.tsx src/components/apps/builtin/wechat/AssistantPanel.tsx src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/app/api/apps/builtin/wechat/agent/route.ts src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts` 通过；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/agent/__tests__/route.test.ts src/components/apps/builtin/wechat/__tests__/assistant-refresh.test.ts` 通过 2 个文件 17 条；`npm run typecheck` 通过；随后完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts src/components/apps/builtin/wechat/__tests__/assistant-refresh.test.ts` 通过 37 个文件 280 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮优化近期话题 `消息` 入口布局后，`npm run lint -- src/components/apps/builtin/wechat/TopicPanel.tsx src/components/apps/builtin/wechat/OverviewTab.tsx src/components/apps/builtin/wechat/use-wechat-topics.ts` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/topic-extractor.test.ts src/lib/wechat-assistant/__tests__/mirror-store.test.ts src/app/api/apps/builtin/wechat/topics/context/__tests__/route.test.ts` 通过 3 个文件 40 条；`npm run typecheck` 通过；随后完整微信助手相关集合 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/im/providers/wechat src/components/apps/builtin/wechat/__tests__/suggested-followup-source.test.ts src/components/apps/builtin/wechat/__tests__/automations-report-ui.test.ts src/components/apps/builtin/wechat/__tests__/assistant-refresh.test.ts` 通过 37 个文件 280 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：按用户反馈“底部 AI 对话框要跟资料库 / 工作流编辑底部用一套”，本轮已把微信助手底部从自定义 `AssistantPanel` 切到完整 `ChatView / MessageInput`，新增微信助手专属会话创建面板和只在该会话挂载的 `lumos-wechat-assistant` 内置工具；底部对话走全局 Agent Chat provider 路径，微信助手设置里的轻量文本生成 provider 仍只用于日报 / 话题 / 跟进分析，避免误绑到不支持 Agent Chat 的服务商。工具覆盖本机微信消息搜索、跟进列表 / 新增 / 完成、自动化列表 / 立即运行 / 暂停开启。目标 `npm run lint -- src/components/apps/builtin/wechat/WeChatAssistantApp.tsx src/components/apps/builtin/wechat/WeChatChatPanel.tsx src/app/api/apps/builtin/wechat/chat/session/route.ts src/app/api/apps/builtin/wechat/chat/session/__tests__/route.test.ts src/app/api/chat/route.ts src/lib/tools/wechat-assistant-mcp-server.ts src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts` 通过；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/chat/session/__tests__/route.test.ts src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts` 通过 2 个文件 3 条；`npm run typecheck` 通过。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续补齐微信助手底部 Agent 能力和提示词管理后，`设置 > AI 提示词` 已新增 `微信助手对话` 可编辑提示词，新建微信助手 ChatView 会话会使用该提示词；`lumos-wechat-assistant` 工具新增更新自动化时间 / 提醒或报告内容、诊断自动化、批量暂停 / 开启、删除自动化。目标 `npm run lint -- src/components/apps/builtin/wechat/default-prompts.ts src/components/apps/builtin/wechat/SettingsPrompts.tsx src/components/apps/builtin/wechat/SettingsTab.tsx src/lib/wechat-assistant/settings-merge.ts src/lib/wechat-assistant/__tests__/settings-store.test.ts src/lib/chat/wechat-assistant-session.ts src/app/api/apps/builtin/wechat/chat/session/route.ts src/app/api/apps/builtin/wechat/chat/session/__tests__/route.test.ts src/lib/tools/wechat-assistant-mcp-server.ts src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts src/app/api/chat/route.ts` 通过；`npm test -- --runTestsByPath src/lib/wechat-assistant/__tests__/settings-store.test.ts src/app/api/apps/builtin/wechat/chat/session/__tests__/route.test.ts src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts` 通过 3 个文件 15 条；`npm run typecheck` 通过；完整微信助手相关回归 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts` 通过 27 个文件 165 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 最新自测补充：本轮继续补齐微信助手底部 Agent 的真实管理能力后，微信助手 ChatView 会话会复用已有 session 并刷新当前 `微信助手对话` 提示词，提示词变化时会清掉旧 Claude SDK 续聊 id；`lumos-wechat-assistant` 工具新增状态读取、跟进 / 自动化名称解析、跟进删除、自动化创建或同名更新，并把微信助手内置工具加入 Claude 客户端免二次权限确认列表。目标 `npm run lint -- src/components/apps/builtin/wechat/WeChatChatPanel.tsx src/components/apps/builtin/wechat/default-prompts.ts src/app/api/apps/builtin/wechat/chat/session/route.ts src/app/api/apps/builtin/wechat/chat/session/__tests__/route.test.ts src/lib/tools/wechat-assistant-mcp-server.ts src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts src/lib/claude-client.ts` 通过；`npm test -- --runTestsByPath src/app/api/apps/builtin/wechat/chat/session/__tests__/route.test.ts src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts src/lib/wechat-assistant/__tests__/settings-store.test.ts` 通过 3 个文件 20 条；`npm run typecheck` 通过；完整微信助手相关回归 `npm test -- src/lib/wechat-assistant src/app/api/apps/builtin/wechat src/lib/tools/__tests__/wechat-assistant-mcp-server.test.ts` 通过 27 个文件 170 条。本轮仍按用户要求未跑完整 build，也未启动 Electron 预览。
  - 当前缺口：当前仍不是“AI 自动创建应用”完整主链；真实 AI 对话已接上第一段服务端工具循环和文件生成/修改路径，第一版模板库也已落地，应用内 `ai.complete` 预览主路径已接真实文本生成，但完整 Claude SDK AppBuilder agent runtime、应用内 workflow bridge 真实执行、更完整发布治理、模板扩展治理和真实 AI 额度环境下的完整 UI 端到端验收还未完成，因此严格状态仍是 `主链状态：未打通`。内置 `微信助手` 目前也只是部分真实主链：同步 / 概况 / 话题 / 跟进持久化 / 自动化持久化 / 自动化调度创建 / 手动触发第一路径已经推进，`每日微信总结` 已能生成带报告节点的 Workflow，微信助手内也有第一版最近结果面板、多条历史记录 API、关键词/状态筛选和报告归档；底部统一 `ChatView` 目前已接第一版专属工具，可对话式搜索微信消息、查看状态、按名称解析候选、查看 / 新增 / 完成 / 删除跟进、查看 / 创建 / 运行 / 暂停 / 开启 / 删除自动化、修改自动化时间或内容、诊断状态和批量启停；一次性提醒的底层改期和防重复触发已补回归，LLM 增强日报也已接入第一版可回退链路，概况页和微信助手对话也已有第一版本机消息关键词搜索；但还缺用户 UI 下的到点自动执行长期验收、真实服务商下的日报质量验收，以及微信助手 Agent 的复杂多轮自然语言消歧、批量删除、跨页定位和更深失败诊断；报告归档目前是第一版保留和展示，尚未扩到完整搜索、导出和长期归档治理；分析仍主要依赖本地统计、确定性报告和单轮 AI 待办抽取，不是真正的社交传播模型；虽然默认会读取本机可访问的微信消息表，但全历史搜索还只是关键词检索第一版，尚未做全文索引、语义搜索、结果导出或搜索权限治理，也还没有 Windows 微信读取、真实关系图谱、传播效果反馈闭环、主题分发效果回流和大库后台索引治理，因此不能标为完整微信应用。
- `知识库 / 跨平台索引`
  - 文档完整度：`部分完成`
  - 主链状态：`未打通`
  - 当前进展：长期问题已定位到打包版 embedding runtime 初始化，而不是模型文件缺失。Windows exe 报错 `Cannot read properties of undefined (reading 'create')` 的同时，模型诊断显示 `config.json / tokenizer.json / vocab.txt / onnx/model_quantized.onnx` 已存在于 `C:\Users\Administrator\.lumos\runtime\embedding-models` 和 `C:\Program Files\Lumos\resources...`，因此后续排查要优先看 `@huggingface/transformers` 与 `onnxruntime-web / onnxruntime-node` 的 packaged runtime 选择。当前代码修复方向已改为 portable embedder 在 Electron packaged server 中强制走本地 `onnxruntime-web` WASM，并把 `wasmPaths` 指向安装包内 `ort-wasm-simd-threaded.mjs/wasm`。
  - UI 可验收范围：暂未进入用户可验收；现有 Windows exe 仍可能复现旧错误，必须等新打包版本安装后在知识库页面真实导入文件并看到“向量化完成 / 搜索命中”。
  - 当前缺口：还需要完成正式打包和跨平台安装包验收；最低验收必须覆盖 macOS 与 Windows exe，两端都要验证“导入内容 -> 向量化成功 -> 搜索命中”，并确认失败诊断会同时报告模型文件与 onnxruntime-web WASM 文件状态。
- `桌面端更新 / 资源拆包`
  - 文档完整度：`未开始`
  - 主链状态：`未打通`
  - 当前进展：已确认当前 electron-updater 路径仍主要下载完整安装包，macOS `.dmg` 约 406MB、Windows `.exe` 约 486MB；同时已开始第一阶段前置实现，新增运行时资源解析层，让 Node runtime / Python runtime / Git Bash / 本地模型等资源可优先从外置缓存目录读取，并新增 `runtime-resources:manifest` 脚本生成稳定资源文件清单。当前这只是拆包前置能力，不代表用户更新已经变成差分或小包下载。
  - UI 可验收范围：暂未进入用户可验收；用户现在看到的更新体验仍是下载完整安装包。
  - 当前缺口：还需要补资源包上传/托管、客户端资源 manifest 获取、断点/校验/解压、缺资源恢复提示、首次安装兜底、正式包移除稳定大资源、macOS/Windows 安装包和自动更新回归；这些完成前不能标为完整实现。
- `Workflow 编辑体验`
  - 文档完整度：`部分完成`
  - 主链状态：`已打通`
  - 当前进展：正式 `Workflow` 详情编辑页与新建构建器的页级真源、JSON 文本和保存载荷现已统一使用原生 `v3 nodes/edges`；旧 `steps / dependsOn` 形状当前只保留在图表/可视化子组件边界，并通过适配器做进出转换，不再继续作为页面级保存真源；同时编辑器保真元数据也已移到隐藏存储，不再泄漏到用户可见 JSON 文本里；最新列表页已补上批量导出 / 导入第一版，用户可多选导出或导出全部，导入可识别旧单包、新批量包，以及一次选择多个旧单包文件，并会在同一批导入中共享代理预设映射。
  - 当前缺口：图表、画布、步骤属性编辑等子组件内部仍建立在旧 `steps / dependsOn` 视图模型上，适配层仍然存在；这意味着“双轨模型”已从正式页主状态退到组件边界，但还没有被彻底删除，因此还不能按“完整实现”标准说编辑器已经完全收敛；批量导入 / 导出目前已完成列表页第一版主链，但还未补更细的冲突预览、导入前清单确认、部分失败回滚和更系统的 UI 验收。
- `图片生成模块`
  - 文档完整度：`部分完成`
  - 主链状态：`已打通`
  - 当前进展：已新增 `Nano banana2（ToAPIs）` 独立图片服务商类型，并接到统一图片生成主链；当前会按 ToAPIs 协议执行“上传参考图 -> 创建异步任务 -> 轮询状态 -> 下载结果”，避免继续把第三方网关误走 Google 官方 Gemini 原生协议；同时已补上 `gemini-3.1-flash-image-preview` 预设、`resolution` 元数据透传、多参考图上传、极端宽高比 body 映射，以及针对 provider 注册/预设暴露/异步轮询的单测；正式聊天里原先只接到旧确认卡片的服务商参数面板，现在已经补到真实消息输入区，用户输入明显的做图/改图诉求时会自动展开“图片参数”，也可以手动点开修改本次比例、分辨率、张数和高级参数；另外图片服务商编辑弹窗里已新增“默认图片参数”，可直接保存默认比例/分辨率/张数，这些默认值会被真实图片生成运行时自动读取，而不再只是 UI 摆设；最新还已补一份正式 `Gemini / Nano Banana` 电商商品图生成 SOP 文档，明确图片顺序规则、AI 自动生成场景卡、三阶段出图和终版精修流程，便于后续产品化接线与团队复用；Pro 图片生成工具已移除旧的每会话 10 张限制，tool result 现在只回传本次生成张数和 `per_image` 计费模式，不再向模型暴露可被误算为“剩余张数”的会话上限字段；同时 OpenAI-compatible 图片服务商的参考图编辑现已改用重复 `image` multipart 字段，并且生图业务失败不再作为 MCP transport error 触发并行 sibling abort，减少工作流批量生图时的错误噪声
  - 当前进展补充：图片生成的 Lumos 云配额扣费请求已补 Node 侧代理支持；桌面端/工作流进程设置 `HTTPS_PROXY / HTTP_PROXY` 时会显式走代理，并遵守 `NO_PROXY`，减少扣费前 `fetch failed / ECONNRESET / ENOTFOUND` 导致的失败。
  - 当前进展补充：针对 `generate-scenes` 场景图生成节点，已把原本自动注入给 Claude 兼容模型的上游多图二进制改为跳过，只保留文件路径给 `generate_image.reference_image_paths` 使用，减少模型首轮请求在网关侧 500 的风险；同时执行 trace 会显示实际解析模型，便于继续定位 provider/model 问题。
  - UI 可验收范围：用户可在正式聊天入口调整图片参数并生成图片，也可在图片服务商设置里保存默认比例 / 分辨率 / 张数；针对失败的场景图工作流，用户现在应从正式 `Workflow` 执行记录页点击“从失败节点重跑”或选中 `generate-scenes` 后“从此节点重跑”，新执行记录应复用上游抠图/分析/方向结果，只重跑 `generate-scenes` 及其下游。
  - 当前缺口：目前还只是第一阶段接入；更细的局部区域编辑、电商定制参数映射、更多 Nano Banana / ToAPIs 模型预设、结果卡的一键重生成参数编辑、更完整的正式 UI 文案与更系统的人工验收还未完成；`745879e0` 这类场景图工作流还需要真实从失败节点重跑通过，才能说该具体工作流已恢复，因此不能按“完整实现”标准宣称图片模块已全部完成
- `支付 / 余额充值`
  - 文档完整度：`部分完成`
  - 主链状态：`已打通`
  - 当前进展：支付主线已明确只做余额充值，不做月卡或订阅；桌面端充值入口已切到金额输入 + 支付宝二维码 + 自动检测到账 + 手动刷新余额，创建订单时会使用当前 Lumos 云账户会话转发到 `lumos-web`；后台已接入易支付/ZPay `submit.php` 参数与 MD5 签名规则，创建 `balance_topup` 订单，并在支付回调里校验签名、`TRADE_SUCCESS`、订单金额后给用户的 new-api token 增加额度，回调会写入支付单号、原始通知和到账状态；最新已把后台默认提交网关切到 `https://zpayz.cn/submit.php`，`lumos-web` 生产站点已用 Let's Encrypt + Nginx 切到 `https://lumos.miki.zj.cn`，HTTP 会自动跳转 HTTPS，生产 `LUMOS_WEB_PUBLIC_URL` 也已切到 HTTPS。
  - UI 可验收范围：用户现在能在侧边栏或设置里的“充值”入口打开“充值余额”弹窗，选择 ¥10 / ¥50 / ¥100 / ¥200 或“其他金额”后输入自定义金额，当前仅展示并使用支付宝支付；Lumos 客户端创建订单后会直接在弹窗中展示支付宝扫码二维码，不再展示“打开支付页”按钮；支付完成且后台已入账后，弹窗会自动切换到“支付成功 / 余额已到账”，手动点击“刷新余额”也能触发同样的到账状态；同时 `lumos-web` 普通用户后台已把旧“套餐管理”入口改为“余额充值”，旧个人版/专业版卡片已从普通用户可见页面移除，公开首页价格区也已收敛为免费注册与余额充值；`账单记录` 页已接入当前用户自己的充值订单列表，可查看订单号、金额、支付方式、支付状态、到账状态和到账时间。
  - 当前缺口：已完成一笔真实支付成功验收，生产 HTTPS 支付回调与到账主链已确认；后台对账、退款、管理员订单检索与异常补单还未做，因此不能按“完整实现”标准验收支付模块。
- `Lumos Cloud 计费`
  - 文档完整度：`部分完成`
  - 主链状态：`已打通`
  - UI 可验收范围：用户可在 Lumos Cloud 账户信息里刷新余额/已使用额度，当前 DeepSeek 普通输入、输出和缓存命中都按生产 new-api 新比例计费；本次受影响账户已返还 `108,275,040` quota（¥216.5501），token 4 余额已从 `364,988,757` 增至 `466,878,222`，token 8 余额已从 `1,414,443` 增至 `7,800,018`。
  - 当前缺口：仍缺正式的后台对账页、按 provider/model 的缓存命中差额报表、可审计的管理员补偿操作记录和自动化计费回归；尤其需要把后台服务商加价、new-api `ModelRatio / CompletionRatio / CacheRatio` 和上游官方价做成自动校验，当前只能说 DeepSeek 当前生产配置和本次历史账已收口，不能说 Cloud 计费体系已经完整完成。
- `微信导出 / 本地微信读取`
  - 文档完整度：`部分完成`
  - 主链状态：`已打通`
  - UI 可验收范围：用户进入 `扩展 / 能力 > 微信` 后，不应再因为本机缺少 `message_0.db` 而看到 `FileNotFoundError: 未找到 WeChat 数据目录`；当前可重新检查并进入微信聊天浏览，看到本机可解密的会话列表，并点击可解密会话查看消息详情。若左侧摘要比详情更新，详情区会显示“消息库可读数量 / 未解密消息库 / 详情可能不完整”的提示，而不是静默显示成“没有消息记录”；微信页面也会显示“修复消息读取不完整”卡片，用户可在 UI 内完成临时放开微信读取保护、重新提取消息库密钥和重新检测。已通过本机 `diagnostics`、`list_sessions` 和 `read_chat` 只读查询确认可以返回会话、消息和分片诊断。
  - 当前缺口：这次只修复 Lumos 对微信消息分片、分片密钥选择、缺失诊断和产品内修复入口的误判；Lumos 不会修复微信原始数据库损坏，也不能恢复微信已丢失的聊天记录。当前本机 `message_2.db ~ message_5.db` 缺少已保存密钥映射，导致详情仍只能展示 `message_1.db` 里的历史消息；需要用户在正式 UI 中点击修复并完成重新提取后，再重新检测确认这些分片能解密，才能说本机消息详情恢复正常。如果微信自身继续提示数据库损坏，仍应先备份 `xwechat_files`，不要把 Lumos 重试当作数据库修复。
- 总体结论
  - 最小闭环：`已完成`
  - 按完整实现标准的总体验收：`未通过`
  - 最新工程稳定性：Electron 版本升级启动链路已补单实例保护，并把升级维护收敛为只做 best-effort HTTP cache 清理，不再在普通启动/升级时删除 Chromium `serviceworkers` / `cachestorage` 等浏览器运行态数据库；当前已通过 `npm run build`、`npm run typecheck` 和 Electron 主进程构建脚本验证，但真实重启验收仍需先退出旧 Electron 进程后再确认启动日志；最新还已修正 GitHub Actions / Next build 阶段多个 page-data worker 竞争同一个 SQLite 运行库的问题，构建期现在显式启用 `LUMOS_BUILD_PHASE=1` 并为每个 worker 使用隔离临时 DB，避免 `/api/task-management/list` 收集 page data 时因 `SQLITE_BUSY` 中断发布打包。
  - 当前优先级：在继续收 `03 / 04 / 05 / 06` 验收尾项的同时，主 Agent 管家先沿只读诊断 / 历史搜索 / 小白引导继续补齐；`08` DeepSearch 继续按独立模块主线补站点登录态、独立 UI 与可恢复执行；`07` 继续保持与主链并行的独立能力建设路线；应用平台先暂停追求 AI 自由生成应用，改为清理测试数据并开发可稳定验收的内置应用。
