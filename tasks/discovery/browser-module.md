# 需求澄清卡

## 基本信息

- 模块名：浏览器功能模块
- 当前状态：`clarifying`
- 对应 `spec` 分支：`spec/browser-module`
- 对应 `spec` worktree：`/Users/zhangjun/私藏/lumos-worktrees/spec-browser-module`

## 你现在已知的事

- 当前问题是什么：需要设计和开发浏览器能力模块。
- 谁会受到影响：嵌入式浏览器、页面操作、AI 辅助浏览、Bridge/MCP 集成。
- 当前代码大致在哪些目录：`src/app/browser`、`src/components/browser`、`electron/browser`、`src/lib/chrome-mcp.ts`。

## 这次要先讨论清楚什么

- [ ] 浏览器模块的核心场景
- [ ] 是内容浏览优先，还是 AI 代理操作优先
- [ ] 与 Bridge / MCP / 主 Agent 的关系
- [ ] 最小可验收能力集合

## 未决问题

- 目标是“嵌入浏览器”还是“可被 Agent 驱动的浏览器”？
- 是否需要 tab、history、cookie、权限管理的一整套设计？
- 哪些能力必须首批上线，哪些可以推迟？

## 暂定方案

- 方案 A：先做可用的浏览容器和 AI 驱动入口。
- 方案 B：先把浏览器能力抽成 Agent 可调用工具。
- 倾向方案：先定使用场景，再决定 UI 先行还是 runtime 先行。

## 当前限制

- 是否允许改正式代码：不允许
- 是否允许写原型：按需，且只限小范围验证
- 是否允许改全局配置：不允许

## 冻结条件

- [ ] 浏览器模块目标明确
- [ ] 非目标明确
- [ ] 允许修改目录明确
- [ ] 禁止修改文件明确
- [ ] 验收标准明确

## 输出给开发会话的内容

- 一段清晰目标描述
- 允许修改目录
- 禁止修改文件
- 验收标准
- 还剩哪些风险
